<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>d12frosted</title>
    <link href="https://d12frosted.io/atom.xml" rel="self" />
    <link href="https://d12frosted.io" />
    <id>https://d12frosted.io/atom.xml</id>
    <author>
        <name>Boris Buliga</name>
        <email>boris@d12frosted.io</email>
    </author>
    <updated>2021-01-24T00:00:00Z</updated>
    <entry>
    <title>Task management with org-roam Vol. 6: Select a person and view related tasks</title>
    <link href="https://d12frosted.io/posts/2021-01-24-task-management-with-roam-vol6.html" />
    <id>https://d12frosted.io/posts/2021-01-24-task-management-with-roam-vol6.html</id>
    <published>2021-01-24T00:00:00Z</published>
    <updated>2021-01-24T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Task management with org-roam Vol. 6: Select a person and view related tasks</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="How to select a person and list all related tasks" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="How to select a person and list all related tasks">
    <meta property="og:description" content="How to select a person and list all related tasks">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Task management with org-roam Vol. 6: Select a person and view related tasks</h1>
    <div class="date"><span>January 24, 2021</span></div>
    <div class="tags"><span>(<a href="../tags/emacs.html">emacs</a>, <a href="../tags/org-roam.html">org-roam</a>, <a href="../tags/org-mode.html">org-mode</a>)</span></div>
    <article>
      <section>
        <p>In one of the previous articles (<a href="https://d12frosted.io/posts/2020-06-25-task-management-with-roam-vol3.html">Vol 3</a> to be precise) we talked about automatic setup of <code>FILETAGS</code>, so each of the task in note related to a person is automatically tagged thanks to <a href="https://orgmode.org/manual/Tag-Inheritance.html">tag inheritance</a>. Then, in <a href="https://d12frosted.io/posts/2020-07-07-task-management-with-roam-vol4.html">Vol 4</a> we talked about automatic tagging of tasks whenever a person is mentioned either in the title or the body of some task. This all makes <code>org-agenda</code> <a href="https://orgmode.org/manual/Matching-tags-and-properties.html#Matching-tags-and-properties">matching capabilities</a> really useful for when we want to see the list of all tasks related to specific person.</p>
<p>In this article, we are going to write a small utility function that asks user to select a person and then presents and <code>org-agenda</code> buffer with tasks related to selected person.</p>
<p>Believe me, intro is longer than the content!</p>
<div class="post-video">
<video autoplay loop>
  <source src="../images/org-notes-person-agenda.mp4" type="video/mp4">
  Your browser does not support the video tag.
</video>
</div>

<p><strong>Change Log:</strong></p>
<ul>
<li><code>[2021-03-02 Tue]</code>: Update naming convention to match <a href="https://github.com/d12frosted/environment/tree/master/emacs">personal configurations</a>.</li>
</ul>
<!--more-->

<p>Long story short, here is the function you can bind or call by name using <code>M-x</code>:</p>
<div class="sourceCode" id="cb1" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb1-1"><a href="#cb1-1"></a>(<span class="kw">defun</span><span class="fu"> vulpea-agenda-person </span>()</span>
<span id="cb1-2"><a href="#cb1-2"></a>  <span class="st">&quot;Show main `org-agenda' view.&quot;</span></span>
<span id="cb1-3"><a href="#cb1-3"></a>  (interactive)</span>
<span id="cb1-4"><a href="#cb1-4"></a>  (<span class="kw">let*</span> ((person (vulpea-select</span>
<span id="cb1-5"><a href="#cb1-5"></a>                  <span class="st">&quot;Person&quot;</span></span>
<span id="cb1-6"><a href="#cb1-6"></a>                  :filter-fn</span>
<span id="cb1-7"><a href="#cb1-7"></a>                  (<span class="kw">lambda</span> (note)</span>
<span id="cb1-8"><a href="#cb1-8"></a>                    (seq-contains-p (vulpea-note-tags note)</span>
<span id="cb1-9"><a href="#cb1-9"></a>                                    <span class="st">&quot;people&quot;</span>))))</span>
<span id="cb1-10"><a href="#cb1-10"></a>         (names (seq-map</span>
<span id="cb1-11"><a href="#cb1-11"></a>                 #'car</span>
<span id="cb1-12"><a href="#cb1-12"></a>                 (org-roam-db-query</span>
<span id="cb1-13"><a href="#cb1-13"></a>                  [:select title</span>
<span id="cb1-14"><a href="#cb1-14"></a>                   :from titles</span>
<span id="cb1-15"><a href="#cb1-15"></a>                   :where (<span class="op">=</span> file $s1)]</span>
<span id="cb1-16"><a href="#cb1-16"></a>                  (vulpea-note-path person))))</span>
<span id="cb1-17"><a href="#cb1-17"></a>         (tags (seq-map #'vulpea--title-to-tag names))</span>
<span id="cb1-18"><a href="#cb1-18"></a>         (query (string-join tags <span class="st">&quot;|&quot;</span>)))</span>
<span id="cb1-19"><a href="#cb1-19"></a>    (dlet ((org-agenda-overriding-arguments (<span class="kw">list</span> <span class="kw">t</span> query)))</span>
<span id="cb1-20"><a href="#cb1-20"></a>      (org-agenda <span class="kw">nil</span> <span class="st">&quot;M&quot;</span>))))</span></code></pre></div>
<p>Now some explanations.</p>
<ol>
<li>This code uses <a href="https://github.com/d12frosted/vulpea">vulpea</a> library<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> to select a person. You can achieve the same result without <code>vulpea</code>, of course, but it saves some effort. <code>vulpea-select</code> asks the user to select a note and accepts a predicate to show only subset of <code>org-roam</code> notes. In this case, we simply present only people notes.</li>
<li>Once we have a selected <code>vulpea-note</code>, we can get all titles on that file (e.g. main title and aliases). This is important for alias users. For example, in some notes I want use Mr. Frodo instead of Frodo Baggins, but I want to see tasks tagged as <code>@Mr.Forod</code> and <code>@FrodoBaggins</code> at the same time. It’s the same person after all (don’t ask me about Gollum, better use <code>M-x
  doctor</code>)!</li>
<li>Now we simply convert those names into tags using <code>vulpea--title-to-tag</code> from <a href="https://d12frosted.io/posts/2020-07-07-task-management-with-roam-vol4.html">Vol 4</a>.</li>
<li>Then we join the tags using <code>|</code> separator into single query string.</li>
<li>The last step is to execute <code>org-agenda</code> with <code>M</code> argument (match for tags, but list only <code>TODO</code> items). In order to pass a query to relevant agenda function, we use <code>org-agenda-overriding-arguments</code>. Not sure if it’s documented anywhere, but you can read the sources of <code>org-agenda</code> to figure out how to use it. <code>dlet</code> here is used for dynamic binding. If you are not using lexical scope, you can use regular <code>let</code> here.</li>
</ol>
<p>That’s it! Now see it in action, again.</p>
<div class="post-video">
<video autoplay loop>
  <source src="../images/org-notes-person-agenda.mp4" type="video/mp4">
  Your browser does not support the video tag.
</video>
</div>

<h1 id="references">References</h1>
<ul>
<li><a href="https://orgmode.org/worg/dev/org-element-api.html">Org Element API</a></li>
<li><a href="https://github.com/skeeto/emacsql">skeeto/emacsql</a></li>
<li>Code from this article is available as <a href="https://gist.github.com/d12frosted/a60e8ccb9aceba031af243dff0d19b2e">GitHub Gist</a></li>
</ul>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>Yikes, I advertise my own libraries on this blog!<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2021-01-24-task-management-with-roam-vol6.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Task management with org-roam Vol. 5: Dynamic and fast agenda</title>
    <link href="https://d12frosted.io/posts/2021-01-16-task-management-with-roam-vol5.html" />
    <id>https://d12frosted.io/posts/2021-01-16-task-management-with-roam-vol5.html</id>
    <published>2021-01-16T00:00:00Z</published>
    <updated>2021-01-16T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Task management with org-roam Vol. 5: Dynamic and fast agenda</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Dynamically building org-agenda-files with only relevant files" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Dynamically building org-agenda-files with only relevant files">
    <meta property="og:description" content="Dynamically building org-agenda-files with only relevant files">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Task management with org-roam Vol. 5: Dynamic and fast agenda</h1>
    <div class="date"><span>January 16, 2021</span></div>
    <div class="tags"><span>(<a href="../tags/emacs.html">emacs</a>, <a href="../tags/org-roam.html">org-roam</a>, <a href="../tags/org-mode.html">org-mode</a>)</span></div>
    <article>
      <section>
        <p>In previous articles (<a href="https://d12frosted.io/posts/2020-06-23-task-management-with-roam-vol1.html">Vol 1</a> and <a href="https://d12frosted.io/posts/2020-06-24-task-management-with-roam-vol2.html">Vol 2</a>) we talked about moving tasks from regular <code>org-mode</code> files to <code>org-roam</code> notes. This relied upon adding all <code>org-roam</code> files to <code>org-agenda-files</code>, which doesn’t scale well, as when you build an agenda buffer, it needs to traverse each file. Once you have more than 1k notes, things become sluggish.</p>
<p>In my experience, once I reached 1200 note files, <code>org-agenda</code> constantly took more than 50 seconds to build, rendering this tool completely useless. But then I realised that only 3% of those files actually contain any <code>TODO</code> entries, so there is no need to traverse whole <code>org-roam-directory</code>!</p>
<p>In this article we are going to optimise <code>org-agenda</code> back to less than 1 second by dynamically building <code>org-agenda-files</code> list to include only files with <code>TODO</code> entries. All thanks to the power of <code>org-roam</code> and some hooks I am going to describe.</p>
<p><strong>Change Log:</strong></p>
<ul>
<li><code>[2021-03-02 Tue]</code>: Update naming convention to match <a href="https://github.com/d12frosted/environment/tree/master/emacs">personal configurations</a>.</li>
<li><code>[2021-03-08 Mon]</code>: <a href="https://github.com/Whil-">Gustav</a> shared that <code>org-element-map</code> has an optional parameter <code>first-match</code> that works like <code>seq-find</code>, meaning that <code>vulpea-project-p</code> can be optimised.</li>
</ul>
<!--more-->

<p>The core idea is very simple - optimising reads during writes. So every time a file is modified, we check if it contains any <code>TODO</code> entries, and depending on that we either add or remove a <code>Project</code> tag from <code>ROAM_TAGS</code> property. And then, before calling <code>org-agenda</code>, we simply <code>org-roam-db-query</code> for files that have a <code>Project</code> tag.</p>
<div class="post-image">
<img src="../images/org-notes-project-tag-update.gif" />
</div>

<h1 id="marking-a-project">Marking a Project</h1>
<p>In order to mark a note as a <code>Project</code>, we need to check if it contains any <code>TODO</code> entries. One of the way to do it is to use <a href="https://orgmode.org/worg/dev/org-element-api.html">Org Element API</a>, a set of parsing functions.</p>
<div class="sourceCode" id="cb1" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb1-1"><a href="#cb1-1"></a>(<span class="kw">defun</span><span class="fu"> vulpea-project-p </span>()</span>
<span id="cb1-2"><a href="#cb1-2"></a>  <span class="st">&quot;Return non-nil if current buffer has any todo entry.</span></span>
<span id="cb1-3"><a href="#cb1-3"></a></span>
<span id="cb1-4"><a href="#cb1-4"></a><span class="st">TODO entries marked as done are ignored, meaning the this</span></span>
<span id="cb1-5"><a href="#cb1-5"></a><span class="st">function returns nil if current buffer contains only completed</span></span>
<span id="cb1-6"><a href="#cb1-6"></a><span class="st">tasks.&quot;</span></span>
<span id="cb1-7"><a href="#cb1-7"></a>  (org-element-map                          <span class="co">; (2)</span></span>
<span id="cb1-8"><a href="#cb1-8"></a>       (org-element-parse-buffer 'headline) <span class="co">; (1)</span></span>
<span id="cb1-9"><a href="#cb1-9"></a>       'headline</span>
<span id="cb1-10"><a href="#cb1-10"></a>     (<span class="kw">lambda</span> (h)</span>
<span id="cb1-11"><a href="#cb1-11"></a>       (<span class="kw">eq</span> (org-element-property :todo-type h)</span>
<span id="cb1-12"><a href="#cb1-12"></a>           'todo))</span>
<span id="cb1-13"><a href="#cb1-13"></a>     <span class="kw">nil</span> 'first-match))                     <span class="co">; (3)</span></span></code></pre></div>
<p>This might look a little bit too much, so let me explain the code step by step.</p>
<ol>
<li>We parse the buffer using <code>org-element-parse-buffer</code>. It returns an abstract syntax tree of the current Org buffer. But sine we care only about headings, we ask it to return only them by passing a <code>GRANULARITY</code> parameter - <code>'headline</code>. This makes things faster.</li>
<li>Then we extract information about <code>TODO</code> keyword from <code>headline</code> AST, which <a href="https://orgmode.org/worg/dev/org-element-api.html#org658999f">contains a property</a> we are interested in - <code>:todo-type</code>, which returns the type of <code>TODO</code> keyword according to <code>org-todo-keywords</code> - <code>'done</code>, <code>'todo</code> or <code>nil</code> (when keyword is not present).</li>
<li>Now all we have to do is to check if the buffer list contains at least one keyword with <code>'todo</code> type. We could use <code>seq=find</code> on the result of <code>org-element-map</code>, but it turns out that it provides an optional <code>first-match</code> argument that can be used for our needs. Thanks <a href="https://github.com/Whil-">Gustav</a> for pointing that out.</li>
</ol>
<p>Now we need to use this function to add or to remove <code>Project</code> tag from a note. I think that it should be done in two places - when visiting a note and in <code>before-save-hook</code>. This way you leave no room for missing a file with <code>TODO</code> entries.</p>
<div class="sourceCode" id="cb2" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb2-1"><a href="#cb2-1"></a>(add-hook 'find-file-hook #'vulpea-project-update-tag)</span>
<span id="cb2-2"><a href="#cb2-2"></a>(add-hook 'before-save-hook #'vulpea-project-update-tag)</span>
<span id="cb2-3"><a href="#cb2-3"></a></span>
<span id="cb2-4"><a href="#cb2-4"></a>(<span class="kw">defun</span><span class="fu"> vulpea-project-update-tag </span>()</span>
<span id="cb2-5"><a href="#cb2-5"></a>  <span class="st">&quot;Update PROJECT tag in the current buffer.&quot;</span></span>
<span id="cb2-6"><a href="#cb2-6"></a>  (<span class="kw">when</span> (<span class="kw">and</span> (<span class="kw">not</span> (active-minibuffer-window))</span>
<span id="cb2-7"><a href="#cb2-7"></a>             (vulpea-buffer-p))</span>
<span id="cb2-8"><a href="#cb2-8"></a>    (<span class="kw">let*</span> ((file (buffer-file-name (buffer-base-buffer)))</span>
<span id="cb2-9"><a href="#cb2-9"></a>           (all-tags (org-roam--extract-tags file))</span>
<span id="cb2-10"><a href="#cb2-10"></a>           (prop-tags (org-roam--extract-tags-prop file))</span>
<span id="cb2-11"><a href="#cb2-11"></a>           (tags prop-tags))</span>
<span id="cb2-12"><a href="#cb2-12"></a>      (<span class="kw">if</span> (vulpea-project-p)</span>
<span id="cb2-13"><a href="#cb2-13"></a>          (<span class="kw">setq</span> tags (<span class="kw">cons</span> <span class="st">&quot;Project&quot;</span> tags))</span>
<span id="cb2-14"><a href="#cb2-14"></a>        (<span class="kw">setq</span> tags (<span class="kw">remove</span> <span class="st">&quot;Project&quot;</span> tags)))</span>
<span id="cb2-15"><a href="#cb2-15"></a>      (<span class="kw">unless</span> (<span class="kw">eq</span> prop-tags tags)</span>
<span id="cb2-16"><a href="#cb2-16"></a>        (org-roam--set-global-prop</span>
<span id="cb2-17"><a href="#cb2-17"></a>         <span class="st">&quot;ROAM_TAGS&quot;</span></span>
<span id="cb2-18"><a href="#cb2-18"></a>         (combine-and-quote-strings (seq-uniq tags)))))))</span>
<span id="cb2-19"><a href="#cb2-19"></a></span>
<span id="cb2-20"><a href="#cb2-20"></a>(<span class="kw">defun</span><span class="fu"> vulpea-buffer-p </span>()</span>
<span id="cb2-21"><a href="#cb2-21"></a>  <span class="st">&quot;Return non-nil if the currently visited buffer is a note.&quot;</span></span>
<span id="cb2-22"><a href="#cb2-22"></a>  (<span class="kw">and</span> buffer-file-name</span>
<span id="cb2-23"><a href="#cb2-23"></a>       (string-prefix-p</span>
<span id="cb2-24"><a href="#cb2-24"></a>        (expand-file-name (file-name-as-directory org-roam-directory))</span>
<span id="cb2-25"><a href="#cb2-25"></a>        (file-name-directory buffer-file-name))))</span></code></pre></div>
<p>That’s it. Now whenever we modify or visit a notes buffer, this code will update the presence of <code>Project</code> tag. See it in action:</p>
<div class="post-image">
<img src="../images/org-notes-project-tag-update.gif" />
</div>

<h1 id="building-agenda" id="1388e376-45f5-4b43-b172-52e98b240732">Building agenda</h1>
<p>In order to dynamically build <code>org-agenda-files</code>, we need to query all files containing <code>Project</code> tag. <code>org-roam</code> uses uses <a href="https://github.com/skeeto/emacsql">skeeto/emacsql</a>, and provides a convenient function <code>org-roam-db-query</code> to execute SQL statements against <code>org-roam-db-location</code> file.</p>
<div class="sourceCode" id="cb3" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb3-1"><a href="#cb3-1"></a>(<span class="kw">defun</span><span class="fu"> vulpea-project-files </span>()</span>
<span id="cb3-2"><a href="#cb3-2"></a>  <span class="st">&quot;Return a list of note files containing Project tag.&quot;</span></span>
<span id="cb3-3"><a href="#cb3-3"></a>  (seq-map</span>
<span id="cb3-4"><a href="#cb3-4"></a>   #'car</span>
<span id="cb3-5"><a href="#cb3-5"></a>   (org-roam-db-query</span>
<span id="cb3-6"><a href="#cb3-6"></a>    [:select file</span>
<span id="cb3-7"><a href="#cb3-7"></a>     :from tags</span>
<span id="cb3-8"><a href="#cb3-8"></a>     :where (like tags (<span class="kw">quote</span> <span class="st">&quot;%</span><span class="sc">\&quot;</span><span class="st">Project</span><span class="sc">\&quot;</span><span class="st">%&quot;</span>))])))</span></code></pre></div>
<p>This function simply returns a list of files containing <code>Project</code> tag. Sure enough it can be generalised for other needs, but it’s good enough for our simple use case. The query is run against the following scheme:</p>
<div class="sourceCode" id="cb4" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb4-1"><a href="#cb4-1"></a>(tags</span>
<span id="cb4-2"><a href="#cb4-2"></a> [(file :unique :primary-key)</span>
<span id="cb4-3"><a href="#cb4-3"></a>  (tags)])</span></code></pre></div>
<p>Now we can set the list of agenda files:</p>
<div class="sourceCode" id="cb5" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb5-1"><a href="#cb5-1"></a>(<span class="kw">setq</span> org-agenda-files (vulpea-project-files))</span></code></pre></div>
<p>But the real question is - when to do it? Some might put it in the <code>init.el</code> file and call it a day, but unless you are restarting Emacs like crazy, I would argue that it’s not the best place to do it. Because we need an up to date list of files exactly when we build agenda.</p>
<div class="sourceCode" id="cb6" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb6-1"><a href="#cb6-1"></a>(<span class="kw">defun</span><span class="fu"> vulpea-agenda-files-update </span>(&amp;<span class="kw">rest</span> _)</span>
<span id="cb6-2"><a href="#cb6-2"></a>  <span class="st">&quot;Update the value of `org-agenda-files'.&quot;</span></span>
<span id="cb6-3"><a href="#cb6-3"></a>  (<span class="kw">setq</span> org-agenda-files (vulpea-project-files)))</span>
<span id="cb6-4"><a href="#cb6-4"></a></span>
<span id="cb6-5"><a href="#cb6-5"></a>(advice-add 'org-agenda :before #'vulpea-agenda-files-update)</span></code></pre></div>
<p>And that’s all. You <code>org-agenda</code> is up to date and fast again!</p>
<h1 id="migration">Migration</h1>
<p>So far we covered what to do with notes we edit. But when you have more than 10 notes it becomes tedious to visit each of them and make sure that they have update state of <code>Project</code> tag. Fortunately, this task is easily automated.</p>
<div class="sourceCode" id="cb7" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb7-1"><a href="#cb7-1"></a>(<span class="kw">dolist</span> (file (org-roam--list-all-files))</span>
<span id="cb7-2"><a href="#cb7-2"></a>  (message <span class="st">&quot;processing %s&quot;</span> file)</span>
<span id="cb7-3"><a href="#cb7-3"></a>  (with-current-buffer (<span class="kw">or</span> (find-buffer-visiting file)</span>
<span id="cb7-4"><a href="#cb7-4"></a>                           (find-file-noselect file))</span>
<span id="cb7-5"><a href="#cb7-5"></a>    (vulpea-project-update-tag)</span>
<span id="cb7-6"><a href="#cb7-6"></a>    (save-buffer)))</span></code></pre></div>
<p>This will visit each of your files and update the presence of <code>Project</code> tag according to presence of <code>TODO</code> entry. Now you are ready to go.</p>
<h1 id="result">Result</h1>
<p>With little amount of <code>emacs-lisp</code> code we dramatically optimized <code>org-agenda</code> loading from <span class="math inline">\(&gt; 50\)</span> seconds to <span class="math inline">\(&lt; 1\)</span> second. Effectiveness of this approach depends on amount of files with <code>TODO</code> entries (the more you have, the less effective this approach becomes). One of the drawbacks is small (in my experience, neglectable) performance degradation of note visiting and note saving. Obviously, if a file contains thousands of headings, it affects performance. In defence, I would argue that such files are against the philosophy of <code>org-roam</code>, where you keep lots of small files as opposed to few huge files.</p>
<p>For you convenience, the full code is displayed below. It is also available as <a href="https://gist.github.com/d12frosted/a60e8ccb9aceba031af243dff0d19b2e">GitHub Gist</a>.</p>
<div class="sourceCode" id="cb8" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb8-1"><a href="#cb8-1"></a>(<span class="kw">defun</span><span class="fu"> vulpea-project-p </span>()</span>
<span id="cb8-2"><a href="#cb8-2"></a>  <span class="st">&quot;Return non-nil if current buffer has any todo entry.</span></span>
<span id="cb8-3"><a href="#cb8-3"></a></span>
<span id="cb8-4"><a href="#cb8-4"></a><span class="st">TODO entries marked as done are ignored, meaning the this</span></span>
<span id="cb8-5"><a href="#cb8-5"></a><span class="st">function returns nil if current buffer contains only completed</span></span>
<span id="cb8-6"><a href="#cb8-6"></a><span class="st">tasks.&quot;</span></span>
<span id="cb8-7"><a href="#cb8-7"></a>  (seq-find                                 <span class="co">; (3)</span></span>
<span id="cb8-8"><a href="#cb8-8"></a>   (<span class="kw">lambda</span> (<span class="kw">type</span>)</span>
<span id="cb8-9"><a href="#cb8-9"></a>     (<span class="kw">eq</span> <span class="kw">type</span> 'todo))</span>
<span id="cb8-10"><a href="#cb8-10"></a>   (org-element-map                         <span class="co">; (2)</span></span>
<span id="cb8-11"><a href="#cb8-11"></a>       (org-element-parse-buffer 'headline) <span class="co">; (1)</span></span>
<span id="cb8-12"><a href="#cb8-12"></a>       'headline</span>
<span id="cb8-13"><a href="#cb8-13"></a>     (<span class="kw">lambda</span> (h)</span>
<span id="cb8-14"><a href="#cb8-14"></a>       (org-element-property :todo-type h)))))</span>
<span id="cb8-15"><a href="#cb8-15"></a></span>
<span id="cb8-16"><a href="#cb8-16"></a>(<span class="kw">defun</span><span class="fu"> vulpea-project-update-tag </span>()</span>
<span id="cb8-17"><a href="#cb8-17"></a>  <span class="st">&quot;Update PROJECT tag in the current buffer.&quot;</span></span>
<span id="cb8-18"><a href="#cb8-18"></a>  (<span class="kw">when</span> (<span class="kw">and</span> (<span class="kw">not</span> (active-minibuffer-window))</span>
<span id="cb8-19"><a href="#cb8-19"></a>             (vulpea-buffer-p))</span>
<span id="cb8-20"><a href="#cb8-20"></a>    (<span class="kw">let*</span> ((file (buffer-file-name (buffer-base-buffer)))</span>
<span id="cb8-21"><a href="#cb8-21"></a>           (all-tags (org-roam--extract-tags file))</span>
<span id="cb8-22"><a href="#cb8-22"></a>           (prop-tags (org-roam--extract-tags-prop file))</span>
<span id="cb8-23"><a href="#cb8-23"></a>           (tags prop-tags))</span>
<span id="cb8-24"><a href="#cb8-24"></a>      (<span class="kw">if</span> (vulpea-project-p)</span>
<span id="cb8-25"><a href="#cb8-25"></a>          (<span class="kw">setq</span> tags (<span class="kw">cons</span> <span class="st">&quot;Project&quot;</span> tags))</span>
<span id="cb8-26"><a href="#cb8-26"></a>        (<span class="kw">setq</span> tags (<span class="kw">remove</span> <span class="st">&quot;Project&quot;</span> tags)))</span>
<span id="cb8-27"><a href="#cb8-27"></a>      (<span class="kw">unless</span> (<span class="kw">eq</span> prop-tags tags)</span>
<span id="cb8-28"><a href="#cb8-28"></a>        (org-roam--set-global-prop</span>
<span id="cb8-29"><a href="#cb8-29"></a>         <span class="st">&quot;ROAM_TAGS&quot;</span></span>
<span id="cb8-30"><a href="#cb8-30"></a>         (combine-and-quote-strings (seq-uniq tags)))))))</span>
<span id="cb8-31"><a href="#cb8-31"></a></span>
<span id="cb8-32"><a href="#cb8-32"></a>(<span class="kw">defun</span><span class="fu"> vulpea-buffer-p </span>()</span>
<span id="cb8-33"><a href="#cb8-33"></a>  <span class="st">&quot;Return non-nil if the currently visited buffer is a note.&quot;</span></span>
<span id="cb8-34"><a href="#cb8-34"></a>  (<span class="kw">and</span> buffer-file-name</span>
<span id="cb8-35"><a href="#cb8-35"></a>       (string-prefix-p</span>
<span id="cb8-36"><a href="#cb8-36"></a>        (expand-file-name (file-name-as-directory org-roam-directory))</span>
<span id="cb8-37"><a href="#cb8-37"></a>        (file-name-directory buffer-file-name))))</span>
<span id="cb8-38"><a href="#cb8-38"></a></span>
<span id="cb8-39"><a href="#cb8-39"></a>(<span class="kw">defun</span><span class="fu"> vulpea-project-files </span>()</span>
<span id="cb8-40"><a href="#cb8-40"></a>  <span class="st">&quot;Return a list of note files containing Project tag.&quot;</span></span>
<span id="cb8-41"><a href="#cb8-41"></a>  (seq-map</span>
<span id="cb8-42"><a href="#cb8-42"></a>   #'car</span>
<span id="cb8-43"><a href="#cb8-43"></a>   (org-roam-db-query</span>
<span id="cb8-44"><a href="#cb8-44"></a>    [:select file</span>
<span id="cb8-45"><a href="#cb8-45"></a>     :from tags</span>
<span id="cb8-46"><a href="#cb8-46"></a>     :where (like tags (<span class="kw">quote</span> <span class="st">&quot;%</span><span class="sc">\&quot;</span><span class="st">Project</span><span class="sc">\&quot;</span><span class="st">%&quot;</span>))])))</span>
<span id="cb8-47"><a href="#cb8-47"></a></span>
<span id="cb8-48"><a href="#cb8-48"></a>(<span class="kw">defun</span><span class="fu"> vulpea-agenda-files-update </span>(&amp;<span class="kw">rest</span> _)</span>
<span id="cb8-49"><a href="#cb8-49"></a>  <span class="st">&quot;Update the value of `org-agenda-files'.&quot;</span></span>
<span id="cb8-50"><a href="#cb8-50"></a>  (<span class="kw">setq</span> org-agenda-files (vulpea-project-files)))</span>
<span id="cb8-51"><a href="#cb8-51"></a></span>
<span id="cb8-52"><a href="#cb8-52"></a>(add-hook 'find-file-hook #'vulpea-project-update-tag)</span>
<span id="cb8-53"><a href="#cb8-53"></a>(add-hook 'before-save-hook #'vulpea-project-update-tag)</span>
<span id="cb8-54"><a href="#cb8-54"></a></span>
<span id="cb8-55"><a href="#cb8-55"></a>(advice-add 'org-agenda :before #'vulpea-agenda-files-update)</span></code></pre></div>
<p>Thank you for your patience.</p>
<h1 id="references">References</h1>
<ul>
<li><a href="https://orgmode.org/worg/dev/org-element-api.html">Org Element API</a></li>
<li><a href="https://github.com/skeeto/emacsql">skeeto/emacsql</a></li>
<li>Code from this article is available as <a href="https://gist.github.com/d12frosted/a60e8ccb9aceba031af243dff0d19b2e">GitHub Gist</a></li>
</ul>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2021-01-16-task-management-with-roam-vol5.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Task management with org-roam Vol. 4: Automatic tagging</title>
    <link href="https://d12frosted.io/posts/2020-07-07-task-management-with-roam-vol4.html" />
    <id>https://d12frosted.io/posts/2020-07-07-task-management-with-roam-vol4.html</id>
    <published>2020-07-07T00:00:00Z</published>
    <updated>2020-07-07T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Task management with org-roam Vol. 4: Automatic tagging</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Automatic tagging upon link insertion" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Automatic tagging upon link insertion">
    <meta property="og:description" content="Automatic tagging upon link insertion">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Task management with org-roam Vol. 4: Automatic tagging</h1>
    <div class="date"><span>July  7, 2020</span></div>
    <div class="tags"><span>(<a href="../tags/emacs.html">emacs</a>, <a href="../tags/org-roam.html">org-roam</a>, <a href="../tags/org-mode.html">org-mode</a>)</span></div>
    <article>
      <section>
        <p>In the <a href="https://d12frosted.io/posts/2020-06-25-task-management-with-roam-vol3.html">previous article</a> we covered automatic tagging of notes related to a specific person, and today we are going to cover automatic tagging of an org-mode heading upon insertion of link related to a person. To put it simple, when I mention someone in the task, I would love this task to be automatically tagged with that persons name. As they say, it’s better to see once, than imagine multiple times, so here is a screencast.</p>
<div class="post-image">
<img src="../images/org-notes-insert.gif" />
</div>

<p><strong>Change Log:</strong></p>
<ul>
<li><code>[2021-01-24 Sun]</code>: Since some of the functionality mentioned in the original article was merged to <code>org-roam</code>, all code is updated to reflect the current state of affairs.</li>
<li><code>[2021-03-02 Tue]</code>: Update naming convention to match <a href="https://github.com/d12frosted/environment/tree/master/emacs">personal configurations</a>.</li>
</ul>
<!--more-->

<p>The solution is based on the relatively recent <a href="https://github.com/org-roam/org-roam/pull/839">pull request</a> to the org-roam. Instead of using <code>org-roam-insert</code> directly, we can write a wrapper for it leveraging the return value, name manipulation from the <a href="https://d12frosted.io/posts/2020-06-25-task-management-with-roam-vol3.html">previous article</a> and tags lookup from <a href="http://localhost:8000/posts/2020-06-10-org-roam-tags.html">Org-roam tags</a> article.</p>
<div class="sourceCode" id="cb1" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb1-1"><a href="#cb1-1"></a>(<span class="kw">defun</span><span class="fu"> vulpea-insert </span>()</span>
<span id="cb1-2"><a href="#cb1-2"></a>  <span class="st">&quot;Insert a link to the note.&quot;</span></span>
<span id="cb1-3"><a href="#cb1-3"></a>  (interactive)</span>
<span id="cb1-4"><a href="#cb1-4"></a>  (when-let*</span>
<span id="cb1-5"><a href="#cb1-5"></a>      ((res (org-roam-insert))</span>
<span id="cb1-6"><a href="#cb1-6"></a>       (path (plist-get res :path))</span>
<span id="cb1-7"><a href="#cb1-7"></a>       (title (plist-get res :title))</span>
<span id="cb1-8"><a href="#cb1-8"></a>       (roam-tags (org-roam-with-file path <span class="kw">nil</span></span>
<span id="cb1-9"><a href="#cb1-9"></a>                    (org-roam--extract-tags path))))</span>
<span id="cb1-10"><a href="#cb1-10"></a>    (<span class="kw">when</span> (seq-contains-p roam-tags <span class="st">&quot;People&quot;</span>)</span>
<span id="cb1-11"><a href="#cb1-11"></a>      (save-excursion</span>
<span id="cb1-12"><a href="#cb1-12"></a>        (<span class="kw">ignore-errors</span></span>
<span id="cb1-13"><a href="#cb1-13"></a>          (org-back-to-heading)</span>
<span id="cb1-14"><a href="#cb1-14"></a>          (org-set-tags</span>
<span id="cb1-15"><a href="#cb1-15"></a>           (seq-uniq</span>
<span id="cb1-16"><a href="#cb1-16"></a>            (<span class="kw">cons</span></span>
<span id="cb1-17"><a href="#cb1-17"></a>             (+org-notes--title-to-tag title)</span>
<span id="cb1-18"><a href="#cb1-18"></a>             (org-get-tags <span class="kw">nil</span> <span class="kw">t</span>)))))))))</span></code></pre></div>
<p>The implementation is straight-forward. We start with calling <code>org-roam-insert</code> that asks for the note to insert. Then we parse result and query the roam tags to understand if the inserted note is related to a person. And if the answer is yes, we use <code>org-set-tags</code> to automatically tag the heading.</p>
<p>In my case, implementation of <code>vulpea--title-to-tag</code> is the following.</p>
<div class="sourceCode" id="cb2" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb2-1"><a href="#cb2-1"></a>(<span class="kw">defun</span><span class="fu"> vulpea--title-to-tag </span>(title)</span>
<span id="cb2-2"><a href="#cb2-2"></a>  <span class="st">&quot;Convert TITLE to tag.&quot;</span></span>
<span id="cb2-3"><a href="#cb2-3"></a>  (concat <span class="st">&quot;@&quot;</span> (s-replace <span class="st">&quot; &quot;</span> <span class="st">&quot;&quot;</span> title)))</span></code></pre></div>
<p>And that’s it! Keep in mind, that with this approach automatic tagging does not happen when mentioning people using company interface (e.g. using <code>org-roam-company</code>) and when using <code>vulpea-insert</code> to mention someone unknown to your system (e.g. when you create a new file). Both of the ‘features’ can be implemented using hooks.</p>
<p>P.S. <code>vulpea-insert</code> can be modified to skip tagging when called with universal argument. Feel free to reach me if you need help with that!</p>
<h1 id="references">References</h1>
<ul>
<li><code>org-roam</code> documentation on <a href="https://github.com/org-roam/org-roam">GitHub</a>.</li>
<li><code>org-mode</code> documentation on the <a href="https://orgmode.org">official site</a>.</li>
<li>Org-roam tags on <a href="https://d12frosted.io/posts/2020-06-10-org-roam-tags.html">d12frosted.io</a>.</li>
<li>personal configurations on <a href="https://github.com/d12frosted/environment/blob/master/emacs/lisp/%2Borg-notes.el">GitHub</a>.</li>
</ul>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2020-07-07-task-management-with-roam-vol4.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Task management with org-roam Vol. 3: FILETAGS</title>
    <link href="https://d12frosted.io/posts/2020-06-25-task-management-with-roam-vol3.html" />
    <id>https://d12frosted.io/posts/2020-06-25-task-management-with-roam-vol3.html</id>
    <published>2020-06-25T00:00:00Z</published>
    <updated>2020-06-25T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Task management with org-roam Vol. 3: FILETAGS</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Automatic FILETAGS application in org-roam" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Automatic FILETAGS application in org-roam">
    <meta property="og:description" content="Automatic FILETAGS application in org-roam">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Task management with org-roam Vol. 3: FILETAGS</h1>
    <div class="date"><span>June 25, 2020</span></div>
    <div class="tags"><span>(<a href="../tags/emacs.html">emacs</a>, <a href="../tags/org-roam.html">org-roam</a>, <a href="../tags/org-mode.html">org-mode</a>)</span></div>
    <article>
      <section>
        <p>In the previous articles (<a href="https://d12frosted.io/posts/2020-06-23-task-management-with-roam-vol1.html">vol1</a> and <a href="https://d12frosted.io/posts/2020-06-24-task-management-with-roam-vol2.html">vol2</a>) we walked the path to <a href="https://github.com/org-roam/org-roam">org-roam</a> and solved the issue with garbage in the category column of agenda. Today we are going to explore meta projects dedicated to specific person, tag inheritance and moving such projects to separate <code>org-roam</code> files. As result, we will have code for automatic tagging based on the title.</p>
<p>Aside from regular meta projects (like personal blog) I also create meta projects for people and locations. This is helpful, because some of the tasks are really related to someone specifically. For example, when I need to return a borrowed book, I just create a task for this.</p>
<pre class="org"><code>* Frodo Baggins                                               :@FrodoBaggins:

** TODO Return 'The Lord of the Rings' book

** TODO Farewell party                                             :PROJECT:

It feels like Mr. Frodo is about to live Shire. So we are going to setup a
farewell party for him.

*** TODO Talk to Samwise Gamgee                            :@SamwiseGamgee:

*** TODO Talk to Meriadoc Brandybuck                  :@MeriadocBrandybuck:

*** TODO Talk to Peregrin Took                              :@PeregrinTook:

*** TODO Tie a pair of wool socks

I am not sure where he is going, so a pair of warm wool socks should be good.
At least they can be used to protect bottles of wine during journey. That is in
case Frodo doesn't wear socks. But how could it be? Everyone does!
</code></pre>
<p><strong>Change Log:</strong></p>
<ul>
<li><code>[2021-01-24 Sun]</code>: Since some of the functionality mentioned in the original article was merged to <code>org-roam</code>, all code is updated to reflect the current state of affairs.</li>
<li><code>[2021-03-02 Tue]</code>: Update naming convention to match <a href="https://github.com/d12frosted/environment/tree/master/emacs">personal configurations</a>.</li>
</ul>
<!--more-->

<p>Now, apart from some misconception about hobbits, there are few important points to note.</p>
<ol>
<li>Due to <a href="https://orgmode.org/manual/Tag-Inheritance.html">tags inheritance</a>, all of the subheadings of <code>Frodo Baggins</code> have <code>@FrodoBaggins</code> tag.</li>
<li>Tasks tagged with other people also have the <code>@FrodoBaggins</code> tag.</li>
</ol>
<p>Thanks to inheritance, it’s easy to find all tasks related to Frodo Baggins via <code>org-agenda</code>. It even enables the search of overlapping tasks. For example, tasks related to Frodo and Samwise. For more information, take a look at the <a href="https://orgmode.org/manual/Matching-tags-and-properties.html#Matching-tags-and-properties">matching tags and properties</a> section of the manual.</p>
<div class="post-image post-image-split">
<img src="../images/org-roam-task-management-vol3-1.png" /><img src="../images/org-roam-task-management-vol3-2.png" />
</div>

<p>With <code>org-roam</code>, each person has its own file.</p>
<pre class="org"><code>#+TITLE: Frodo Baggins
#+FILETAGS: @FrodoBaggins
#+ROAM_TAGS: People

* Tasks
** TODO Return 'The Lord of the Rings' book

** TODO Farewell party                                             :PROJECT:

It feels like Mr. Frodo is about to live Shire. So we are going to setup a
farewell party for him.

*** TODO Talk to Samwise Gamgee                            :@SamwiseGamgee:

*** TODO Talk to Meriadoc Brandybuck                  :@MeriadocBrandybuck:

*** TODO Talk to Peregrin Took                              :@PeregrinTook:

*** TODO Tie a pair of wool socks

I am not sure where he is going, so a pair of warm wool socks should be good.
At least they can be used to protect bottles of wine during journey. That is in
case Frodo doesn't wear socks. But how could it be? Everyone does!
</code></pre>
<p>In order to maintain the feature where <code>@FrodoBaggins</code> tag is applied to all TODO items we have to use <code>FILETAGS</code> property. I am also using <a href="https://d12frosted.io/posts/2020-06-10-org-roam-tags.html">org-roam tags</a> to mark Frodo Baggins as a person. This helps me in two ways. First of all, it gives me clear understanding that this entity is a person (some people do have strange names). Secondly, it serves me in automation and filtering.</p>
<p>Now, when I see a headline with title and tag being literally the same (with few programmable exceptions) or the file with <code>TITLE</code> and <code>FILETAGS</code> being the same (with few programmable exceptions), I am feeling nervous. Especially since I am prone to mistakes.</p>
<p>So what I do - I automate <code>FILETAGS</code>. I have a function <code>vulpea-ensure-filetag</code> which automatically sets the <code>FILETAGS</code> buffer property for <code>org-roam</code> entries tagged as <code>People</code>. The following code reuses most of the functions from <a href="https://d12frosted.io/posts/2020-06-10-org-roam-tags.html">org-roam tags</a> post.</p>
<div class="sourceCode" id="cb3" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb3-1"><a href="#cb3-1"></a>(<span class="kw">defun</span><span class="fu"> vulpea-ensure-filetag </span>()</span>
<span id="cb3-2"><a href="#cb3-2"></a>  <span class="st">&quot;Add respective file tag if it's missing in the current note.&quot;</span></span>
<span id="cb3-3"><a href="#cb3-3"></a>  (interactive)</span>
<span id="cb3-4"><a href="#cb3-4"></a>  (<span class="kw">let</span> ((tags (org-roam--extract-tags-prop</span>
<span id="cb3-5"><a href="#cb3-5"></a>               (buffer-file-name</span>
<span id="cb3-6"><a href="#cb3-6"></a>                (buffer-base-buffer)))))</span>
<span id="cb3-7"><a href="#cb3-7"></a>    (<span class="kw">when</span> (<span class="kw">and</span> (seq-contains-p tags <span class="st">&quot;People&quot;</span>)</span>
<span id="cb3-8"><a href="#cb3-8"></a>               (<span class="kw">null</span> (org-roam--extract-global-props-keyword</span>
<span id="cb3-9"><a href="#cb3-9"></a>                      '(<span class="st">&quot;FILETAGS&quot;</span>))))</span>
<span id="cb3-10"><a href="#cb3-10"></a>      (org-roam--set-global-prop</span>
<span id="cb3-11"><a href="#cb3-11"></a>       <span class="st">&quot;FILETAGS&quot;</span></span>
<span id="cb3-12"><a href="#cb3-12"></a>       (vulpea--title-to-tag (vulpea-buffer-prop-get <span class="st">&quot;TITLE&quot;</span>))))))</span>
<span id="cb3-13"><a href="#cb3-13"></a></span>
<span id="cb3-14"><a href="#cb3-14"></a>(<span class="kw">defun</span><span class="fu"> vulpea--title-to-tag </span>(title)</span>
<span id="cb3-15"><a href="#cb3-15"></a>  <span class="st">&quot;Convert TITLE to tag.&quot;</span></span>
<span id="cb3-16"><a href="#cb3-16"></a>  (concat <span class="st">&quot;@&quot;</span> (s-replace <span class="st">&quot; &quot;</span> <span class="st">&quot;&quot;</span> title)))</span></code></pre></div>
<p>This function can be called interactively, but since I usually place the tag using <code>vulpea-tags-add</code>, I just add the <code>vulpea-ensure-filetag</code> to the end of that function.</p>
<div class="sourceCode" id="cb4" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb4-1"><a href="#cb4-1"></a>(<span class="kw">defun</span><span class="fu"> vulpea-tags-add </span>()</span>
<span id="cb4-2"><a href="#cb4-2"></a>  <span class="st">&quot;Add a tag to current note.&quot;</span></span>
<span id="cb4-3"><a href="#cb4-3"></a>  (interactive)</span>
<span id="cb4-4"><a href="#cb4-4"></a>  (<span class="kw">when</span> (org-roam-tag-add)</span>
<span id="cb4-5"><a href="#cb4-5"></a>    (vulpea-ensure-filetag)))</span></code></pre></div>
<p>Though for other purposes one can put this function to the file visit hook. But hooks are sensitive, so I am going to stop here.</p>
<p>In the <a href="https://d12frosted.io/posts/2020-07-07-task-management-with-roam-vol4.html">next article</a> we are going to talk about automatic insertion of person tag (e.g. <code>@FrodoBaggins</code>) when mentioning this person in other task.</p>
<h1 id="references">References</h1>
<ul>
<li><code>org-roam</code> documentation on <a href="https://github.com/org-roam/org-roam">GitHub</a>.</li>
<li><code>org-mode</code> documentation on the <a href="https://orgmode.org">official site</a>.</li>
<li>Org-roam tags on <a href="https://d12frosted.io/posts/2020-06-10-org-roam-tags.html">d12frosted.io</a>.</li>
<li>personal configurations on <a href="https://github.com/d12frosted/environment/blob/master/emacs/lisp/%2Borg-notes.el">GitHub</a>.</li>
</ul>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2020-06-25-task-management-with-roam-vol3.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Task management with org-roam Vol. 2: Categories</title>
    <link href="https://d12frosted.io/posts/2020-06-24-task-management-with-roam-vol2.html" />
    <id>https://d12frosted.io/posts/2020-06-24-task-management-with-roam-vol2.html</id>
    <published>2020-06-24T00:00:00Z</published>
    <updated>2020-06-24T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Task management with org-roam Vol. 2: Categories</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Automatic category extraction from org-roam" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Automatic category extraction from org-roam">
    <meta property="og:description" content="Automatic category extraction from org-roam">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Task management with org-roam Vol. 2: Categories</h1>
    <div class="date"><span>June 24, 2020</span></div>
    <div class="tags"><span>(<a href="../tags/emacs.html">emacs</a>, <a href="../tags/org-roam.html">org-roam</a>, <a href="../tags/org-mode.html">org-mode</a>)</span></div>
    <article>
      <section>
        <p>In the <a href="https://d12frosted.io/posts/2020-06-23-task-management-with-roam-vol1.html">previous article</a> we set a ground for moving tasks to <a href="https://github.com/org-roam/org-roam">org-roam</a>, and encountered an issue with visual garbage in the agenda buffer. Namely, <code>org-roam</code> file id as part of the category. In this article, we are going to explore the means to overcome this issue.</p>
<div class="post-image post-image-split">
<img src="../images/org-roam-task-management-vol2-1.png" /><img src="../images/org-roam-task-management-vol2-2.png" />
</div>

<p><strong>Change Log:</strong></p>
<ul>
<li><code>[2021-03-02 Tue]</code>: Update category extraction function to use <code>TITLE</code> of the note and enforce length limit. Kudos to <a href="https://github.com/real-or-random/">Tim Ruffing</a> for the idea.</li>
<li><code>[2021-03-02 Tue]</code>: Update naming convention to match <a href="https://github.com/d12frosted/environment/tree/master/emacs">personal configurations</a>.</li>
</ul>
<!--more-->

<p>On of the simplest solutions is to mimic solution for headlines by setting <code>CATEGORY</code> property on the file level (manually or by using <code>org-set-property</code>).</p>
<pre class="org"><code>:PROPERTIES:
:CATEGORY:               emacs-plus
:END:
#+TITLE: emacs-plus

...
</code></pre>
<pre class="org"><code>:PROPERTIES:
:CATEGORY:               blog
:END:
#+TITLE: Blog

...
</code></pre>
<div class="post-image">
<img src="../images/org-roam-task-management-vol2-2.png" />
</div>

<p>While this works, it is a manual labor. And in most cases we want to use <code>TITLE</code> as <code>CATEGORY</code>, at least for agenda buffer. Fortunately, we can help agenda to properly parse the category by modifying the value of <code>org-agenda-prefix-format</code>, which allows to specify how to render each line in the different agenda buffers (e.g. regular agenda, in the list of todo tasks etc). We are looking for the capability to evaluate arbitrary lisp expressions. The default value of this variable is</p>
<div class="sourceCode" id="cb3" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb3-1"><a href="#cb3-1"></a>((agenda . <span class="st">&quot; %i %-12:c%?-12t% s&quot;</span>)</span>
<span id="cb3-2"><a href="#cb3-2"></a> (todo . <span class="st">&quot; %i %-12:c&quot;</span>)</span>
<span id="cb3-3"><a href="#cb3-3"></a> (tags . <span class="st">&quot; %i %-12:c&quot;</span>)</span>
<span id="cb3-4"><a href="#cb3-4"></a> (<span class="kw">search</span> . <span class="st">&quot; %i %-12:c&quot;</span>))</span></code></pre></div>
<p>The interesting part is <code>%-12:c</code> which means:</p>
<ul>
<li>Give the category (because of <code>c</code>) a 12 chars wide field, padded with whitespace on the right (because of <code>-</code>).</li>
<li>Append a colon if there is a category (because of <code>:</code>).</li>
<li>Finally, append the category of the item, or as given by the <code>CATEGORY</code> property, or derived from the file name.</li>
</ul>
<p>Instead of <code>c</code> we can use any expression.</p>
<div class="sourceCode" id="cb4" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb4-1"><a href="#cb4-1"></a>(<span class="kw">setq</span> org-agenda-prefix-format</span>
<span id="cb4-2"><a href="#cb4-2"></a>      '((agenda . <span class="st">&quot; %i %-12(vulpea-agenda-category)%?-12t% s&quot;</span>)</span>
<span id="cb4-3"><a href="#cb4-3"></a>        (todo . <span class="st">&quot; %i %-12(vulpea-agenda-category) &quot;</span>)</span>
<span id="cb4-4"><a href="#cb4-4"></a>        (tags . <span class="st">&quot; %i %-12(vulpea-agenda-category) &quot;</span>)</span>
<span id="cb4-5"><a href="#cb4-5"></a>        (<span class="kw">search</span> . <span class="st">&quot; %i %-12(vaulpea-agenda-category) &quot;</span>)))</span>
<span id="cb4-6"><a href="#cb4-6"></a></span>
<span id="cb4-7"><a href="#cb4-7"></a>(<span class="kw">defun</span><span class="fu"> vulpea-agenda-category </span>()</span>
<span id="cb4-8"><a href="#cb4-8"></a>  <span class="st">&quot;Get category of item at point for agenda.</span></span>
<span id="cb4-9"><a href="#cb4-9"></a></span>
<span id="cb4-10"><a href="#cb4-10"></a><span class="st">Category is defined by one of the following items:</span></span>
<span id="cb4-11"><a href="#cb4-11"></a></span>
<span id="cb4-12"><a href="#cb4-12"></a><span class="st">- CATEGORY property</span></span>
<span id="cb4-13"><a href="#cb4-13"></a><span class="st">- TITLE keyword</span></span>
<span id="cb4-14"><a href="#cb4-14"></a><span class="st">- TITLE property</span></span>
<span id="cb4-15"><a href="#cb4-15"></a><span class="st">- filename without directory and extension</span></span>
<span id="cb4-16"><a href="#cb4-16"></a></span>
<span id="cb4-17"><a href="#cb4-17"></a><span class="st">Usage example:</span></span>
<span id="cb4-18"><a href="#cb4-18"></a></span>
<span id="cb4-19"><a href="#cb4-19"></a><span class="st">  (setq org-agenda-prefix-format</span></span>
<span id="cb4-20"><a href="#cb4-20"></a><span class="st">        '((agenda . </span><span class="sc">\&quot;</span><span class="st"> %(vulpea-agenda-category) %?-12t %12s</span><span class="sc">\&quot;</span><span class="st">)))</span></span>
<span id="cb4-21"><a href="#cb4-21"></a></span>
<span id="cb4-22"><a href="#cb4-22"></a><span class="st">Refer to `org-agenda-prefix-format' for more information.&quot;</span></span>
<span id="cb4-23"><a href="#cb4-23"></a>  (<span class="kw">let*</span> ((file-name (<span class="kw">when</span> buffer-file-name</span>
<span id="cb4-24"><a href="#cb4-24"></a>                      (file-name-sans-extension</span>
<span id="cb4-25"><a href="#cb4-25"></a>                       (file-name-nondirectory buffer-file-name))))</span>
<span id="cb4-26"><a href="#cb4-26"></a>         (title (car-safe (org-roam--extract-titles-title)))</span>
<span id="cb4-27"><a href="#cb4-27"></a>         (category (org-get-category)))</span>
<span id="cb4-28"><a href="#cb4-28"></a>    (<span class="kw">or</span> (<span class="kw">if</span> (<span class="kw">and</span></span>
<span id="cb4-29"><a href="#cb4-29"></a>             title</span>
<span id="cb4-30"><a href="#cb4-30"></a>             (<span class="kw">string-equal</span> category file-name))</span>
<span id="cb4-31"><a href="#cb4-31"></a>            title</span>
<span id="cb4-32"><a href="#cb4-32"></a>          category)</span>
<span id="cb4-33"><a href="#cb4-33"></a>        <span class="st">&quot;&quot;</span>)))</span></code></pre></div>
<div class="post-image">
<img src="../images/org-roam-task-management-vol2-3.png" />
</div>

<p>Now if we remove the manually set <code>CATEGORY</code> property from both files we will get the same result with nicely parsed categories. Please note that these two approaches can be mixed. For example, if you wish to override the category, just set this property explicitly and call it a day.</p>
<p>Additionally, it’s easy to extend this function to truncate overly long categories (in the screenshot above, <code>Some project with ridiculously long title</code> and <code>Frodo Baggins</code> are examples of long categories). We will use <a href="https://github.com/magnars/s.el">s.el</a> library to achieve this.</p>
<div class="sourceCode" id="cb5" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb5-1"><a href="#cb5-1"></a>(<span class="kw">setq</span> org-agenda-prefix-format</span>
<span id="cb5-2"><a href="#cb5-2"></a>      '((agenda . <span class="st">&quot; %i %(vulpea-agenda-category 12)%?-12t% s&quot;</span>)</span>
<span id="cb5-3"><a href="#cb5-3"></a>        (todo . <span class="st">&quot; %i %(vulpea-agenda-category 12) &quot;</span>)</span>
<span id="cb5-4"><a href="#cb5-4"></a>        (tags . <span class="st">&quot; %i %(vulpea-agenda-category 12) &quot;</span>)</span>
<span id="cb5-5"><a href="#cb5-5"></a>        (<span class="kw">search</span> . <span class="st">&quot; %i %(vaulpea-agenda-category 12) &quot;</span>)))</span>
<span id="cb5-6"><a href="#cb5-6"></a></span>
<span id="cb5-7"><a href="#cb5-7"></a>(<span class="kw">defun</span><span class="fu"> vulpea-agenda-category </span>(&amp;optional len)</span>
<span id="cb5-8"><a href="#cb5-8"></a>  <span class="st">&quot;Get category of item at point for agenda.</span></span>
<span id="cb5-9"><a href="#cb5-9"></a></span>
<span id="cb5-10"><a href="#cb5-10"></a><span class="st">Category is defined by one of the following items:</span></span>
<span id="cb5-11"><a href="#cb5-11"></a></span>
<span id="cb5-12"><a href="#cb5-12"></a><span class="st">- CATEGORY property</span></span>
<span id="cb5-13"><a href="#cb5-13"></a><span class="st">- TITLE keyword</span></span>
<span id="cb5-14"><a href="#cb5-14"></a><span class="st">- TITLE property</span></span>
<span id="cb5-15"><a href="#cb5-15"></a><span class="st">- filename without directory and extension</span></span>
<span id="cb5-16"><a href="#cb5-16"></a></span>
<span id="cb5-17"><a href="#cb5-17"></a><span class="st">When LEN is a number, resulting string is padded right with</span></span>
<span id="cb5-18"><a href="#cb5-18"></a><span class="st">spaces and then truncated with ... on the right if result is</span></span>
<span id="cb5-19"><a href="#cb5-19"></a><span class="st">longer than LEN.</span></span>
<span id="cb5-20"><a href="#cb5-20"></a></span>
<span id="cb5-21"><a href="#cb5-21"></a><span class="st">Usage example:</span></span>
<span id="cb5-22"><a href="#cb5-22"></a></span>
<span id="cb5-23"><a href="#cb5-23"></a><span class="st">  (setq org-agenda-prefix-format</span></span>
<span id="cb5-24"><a href="#cb5-24"></a><span class="st">        '((agenda . </span><span class="sc">\&quot;</span><span class="st"> %(vulpea-agenda-category) %?-12t %12s</span><span class="sc">\&quot;</span><span class="st">)))</span></span>
<span id="cb5-25"><a href="#cb5-25"></a></span>
<span id="cb5-26"><a href="#cb5-26"></a><span class="st">Refer to `org-agenda-prefix-format' for more information.&quot;</span></span>
<span id="cb5-27"><a href="#cb5-27"></a>  (<span class="kw">let*</span> ((file-name (<span class="kw">when</span> buffer-file-name</span>
<span id="cb5-28"><a href="#cb5-28"></a>                      (file-name-sans-extension</span>
<span id="cb5-29"><a href="#cb5-29"></a>                       (file-name-nondirectory buffer-file-name))))</span>
<span id="cb5-30"><a href="#cb5-30"></a>         (title (car-safe (org-roam--extract-titles-title)))</span>
<span id="cb5-31"><a href="#cb5-31"></a>         (category (org-get-category))</span>
<span id="cb5-32"><a href="#cb5-32"></a>         (result</span>
<span id="cb5-33"><a href="#cb5-33"></a>          (<span class="kw">or</span> (<span class="kw">if</span> (<span class="kw">and</span></span>
<span id="cb5-34"><a href="#cb5-34"></a>                   title</span>
<span id="cb5-35"><a href="#cb5-35"></a>                   (<span class="kw">string-equal</span> category file-name))</span>
<span id="cb5-36"><a href="#cb5-36"></a>                  title</span>
<span id="cb5-37"><a href="#cb5-37"></a>                category)</span>
<span id="cb5-38"><a href="#cb5-38"></a>              <span class="st">&quot;&quot;</span>)))</span>
<span id="cb5-39"><a href="#cb5-39"></a>    (<span class="kw">if</span> (<span class="kw">numberp</span> len)</span>
<span id="cb5-40"><a href="#cb5-40"></a>        (s-truncate len (s-pad-right len <span class="st">&quot; &quot;</span> result))</span>
<span id="cb5-41"><a href="#cb5-41"></a>      result)))</span></code></pre></div>
<div class="post-image">
<img src="../images/org-roam-task-management-vol2-4.png" />
</div>

<p>Now the agenda is clean.</p>
<p>In the <a href="https://d12frosted.io/posts/2020-06-25-task-management-with-roam-vol3.html">next article</a> we are going to talk about tagging tasks related to a person. Stay tuned and keep roaming!</p>
<h1 id="references">References</h1>
<ul>
<li><code>org-roam</code> documentation on <a href="https://github.com/org-roam/org-roam">GitHub</a>.</li>
<li><code>org-mode</code> documentation on the <a href="https://orgmode.org">official site</a>.</li>
<li>personal configurations on <a href="https://github.com/d12frosted/environment/blob/master/emacs/lisp/%2Borg-notes.el">GitHub</a>.</li>
</ul>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2020-06-24-task-management-with-roam-vol2.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Task management with org-roam Vol. 1: Path to Roam</title>
    <link href="https://d12frosted.io/posts/2020-06-23-task-management-with-roam-vol1.html" />
    <id>https://d12frosted.io/posts/2020-06-23-task-management-with-roam-vol1.html</id>
    <published>2020-06-23T00:00:00Z</published>
    <updated>2020-06-23T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Task management with org-roam Vol. 1: Path to Roam</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Moving tasks to org-roam" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Moving tasks to org-roam">
    <meta property="og:description" content="Moving tasks to org-roam">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Task management with org-roam Vol. 1: Path to Roam</h1>
    <div class="date"><span>June 23, 2020</span></div>
    <div class="tags"><span>(<a href="../tags/emacs.html">emacs</a>, <a href="../tags/org-roam.html">org-roam</a>, <a href="../tags/org-mode.html">org-mode</a>)</span></div>
    <article>
      <section>
        <p><a href="https://orgmode.org">org-mode</a> is a simple outliner for note taking and list management. It <a href="https://d12frosted.io/posts/2016-12-20-Being-an-org-mode-addict.html">doesn’t impose</a> any complex features on users. This is something users excel at by themselves. My suggestion is to continue this tradition and discover task management capabilities in <a href="https://github.com/org-roam/org-roam">org-roam</a>, an <a href="https://orgmode.org">org-mode</a> extension for non-hierarchical notes management.</p>
<div class="post-image">
<img src="../images/org-roam-task-management-vol1-3.png" />
</div>

<!--more-->

<p>Since org-mode gives a lot of freedom, everyone implements tasks management system differently. In the nutshell, most of the implementations I’ve seen consist of the following elements.</p>
<ul>
<li>Task - the simplest actionable item with some <code>TODO</code> state.</li>
<li>Project - a <code>TODO</code> item with outcome requiring multiple steps to achieve it.</li>
<li>Meta project - long lasting project without a final outcome. It consists of projects or tasks. Think of it as of an area or a category. For example, personal blog is a meta project, as it doesn’t have any terminating outcome. It does have projects with specific outcome (like setting up your blog or writing a post) or simple tasks (like renewing SSL certificate), but the blog itself doesn’t have an outcome.</li>
<li>Resource - a person, a tool, a location or whatever possibly related to a task. In plain org-mode usually implemented as a tag.</li>
</ul>
<p>Take a look at the following example of <code>org-mode</code> file with all these elements.</p>
<pre class="org"><code>#+TITLE: Work

* emacs-plus
:PROPERTIES:
:CATEGORY:               emacs-plus
:END:

Emacs Plus is [[https://www.gnu.org/software/emacs/emacs.html][→ GNU Emacs]]
formulae for macOS [[https://brew.sh][→ Homebrew]] package manager. It offers a
wide rage of extra functionality over regular
[[https://formulae.brew.sh/formula/emacs#default][→ Emacs]] package. Emacs Plus
intent is to give the most of 'plus' stuff by default, leaving only
controversial options as opt-in.

** TODO Restore icons selection for =emacs-plus@26=          :@FrodoBaggins:
Frodo Baggins volunteered to help with this.

** TODO [0/3] Automate bottle producing                            :PROJECT:
*** TODO Create a project on Bintray
*** TODO Investigate if =brew test-bot= can be used for =emacs-plus=
*** TODO Write a CI job that creates and uploads bottles

* Blog
:PROPERTIES:
:CATEGORY:               blog
:END:

https://d12frosted.io

** TODO Write a post about org-roam and agenda
** TODO [0/2] Get rid of embedded HTML for images in posts         :PROJECT:
*** TODO Write a custom handler of image links
This should transform link to images into proper HTML.

#+begin_example
  &lt;div class=&quot;post-image post-image&quot;&gt;
  &lt;img src=&quot;/images/some-image.png&quot; /&gt;
  &lt;/div&gt;
#+end_example

*** TODO Replace all image exports by regular org links
</code></pre>
<div class="post-image post-image-split">
<img src="../images/org-roam-task-management-vol1-1.png" /><img src="../images/org-roam-task-management-vol1-2.png" />
</div>

<p>As you can see, it has 2 meta projects: <code>emacs-plus</code> and personal blog, each containing a task and a project. Meta projects have their own category property (set by hand or using <code>org-set-property</code> which is bound to <code>C-c C-x p</code> by default), so in agenda view one can quickly distinguish (and filter) tasks from different meta projects (for example, using <code>org-agenda-filter-by-category</code> which is bound to <code>&lt;</code> by default).</p>
<div class="post-image">
<img src="../images/org-agenda-filter-by-category.gif" />
</div>

<p>Formally (and technically) projects might be defined as a <a href="http://doc.norang.ca/org-mode.html#Projects">task having sub-tasks</a>, but in this rare case I vote for simplicity - project is a task tagged as <code>PROJECT</code>.</p>
<p>When a task relates to a person or some location, I tag it respectively, just use <code>@</code> symbol as a prefix to distinguish from utilitarian tags. Agenda helps to quickly find all tasks with specific tag. Use <code>M-x org-agenda m</code> or <code>M-x
org-agenda M</code> (to list only TODO items).</p>
<div class="post-image">
<img src="../images/org-agenda-filter-by-tags.gif" />
</div>

<p>With <code>org-roam</code> it makes sense to create a separate file for each meta project. In the endless debate ‘many small’ vs ‘few big’ org files, <code>org-roam</code> favours the former approach (though even this is debatable).</p>
<pre class="org"><code>#+TITLE: emacs-plus

Emacs Plus is [[https://www.gnu.org/software/emacs/emacs.html][→ GNU Emacs]] formulae for macOS [[https://brew.sh][→ Homebrew]] package manager. It
offers a wide rage of extra functionality over regular [[https://formulae.brew.sh/formula/emacs#default][→ Emacs]] package. Emacs
Plus intent is to give the most of 'plus' stuff by default, leaving only
controversial options as opt-in.

* Tasks
** TODO Restore icons selection for =emacs-plus@26=          :@FrodoBaggins:
Frodo Baggins volunteered to help with this.

** TODO [0/3] Automate bottle producing                            :PROJECT:
*** TODO Create a project on Bintray
*** TODO Investigate if =brew test-bot= can be used for =emacs-plus=
*** TODO Write a CI job that creates and uploads bottles
</code></pre>
<pre class="org"><code>#+TITLE: Blog

https://d12frosted.io

* Tasks
** TODO Write a post about org-roam and agenda
** TODO [0/2] Get rid of embedded HTML for images in posts         :PROJECT:
*** TODO Write a custom handler of image links
This should transform link to images into proper HTML.

#+begin_example
  &lt;div class=&quot;post-image post-image&quot;&gt;
  &lt;img src=&quot;/images/some-image.png&quot; /&gt;
  &lt;/div&gt;
#+end_example

*** TODO Replace all image exports by regular org links
</code></pre>
<div class="post-image post-image-split">
<img src="../images/org-roam-task-management-vol1-3.png" /><img src="../images/org-roam-task-management-vol1-4.png" />
</div>

<div class="post-image">
<img src="../images/org-roam-task-management-vol1-5.png" />
</div>

<p>So far the transition was simple. However as you can see, in agenda buffer the inferred category contains garbage - note id.</p>
<p>In the <a href="https://d12frosted.io/posts/2020-06-24-task-management-with-roam-vol2.html">next article</a> we are going to explore the means to fix the agenda buffer from the unwanted visual garbage. Stay tuned!</p>
<h1 id="references">References</h1>
<ul>
<li><code>org-roam</code> documentation on <a href="https://github.com/org-roam/org-roam">GitHub</a>.</li>
<li><code>org-mode</code> documentation on the <a href="https://orgmode.org">official site</a>.</li>
<li><a href="http://doc.norang.ca/org-mode.html#Projects">Org Mode - Organize Your Life In Plain Text!</a> by Bernt Hansen.</li>
</ul>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2020-06-23-task-management-with-roam-vol1.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Org-roam tags</title>
    <link href="https://d12frosted.io/posts/2020-06-10-org-roam-tags.html" />
    <id>https://d12frosted.io/posts/2020-06-10-org-roam-tags.html</id>
    <published>2020-06-10T00:00:00Z</published>
    <updated>2020-06-10T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Org-roam tags</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Utility functions to manage org-roam tags" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Utility functions to manage org-roam tags">
    <meta property="og:description" content="Utility functions to manage org-roam tags">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Org-roam tags</h1>
    <div class="date"><span>June 10, 2020</span></div>
    <div class="tags"><span>(<a href="../tags/emacs.html">emacs</a>, <a href="../tags/org-roam.html">org-roam</a>)</span></div>
    <article>
      <section>
        <p><a href="https://github.com/org-roam/org-roam">Org-roam</a> is a note-taking tool built on top of Emacs and Org. Essentially, it’s a replica of <a href="https://roamresearch.com">Roam Research</a>. These tools provide an easy way to create and manage non-hierarchical notes. If you wish to learn more, just take a look at the <a href="https://org-roam.github.io/org-roam/manual/">Org-roam manual</a> or watch <a href="https://www.youtube.com/watch?v=Lg61ocfxk3c">Making Connections in your Notes</a> video by Matt Williams. Believe me, Org-roam and Roam Research are game-changers. Or even better, don’t believe me and validate my claim by yourself.</p>
<p>Since I am already <a href="https://d12frosted.io/posts/2016-12-20-Being-an-org-mode-addict.html">addicted</a>, it was only natural to prefer <code>org-roam</code> over some web application. Apart from being developed on top of mature Org ecosystem, Emacs brings many merits and extensibility is one of them. Once <code>org-roam</code> introduced tags system in <a href="https://github.com/org-roam/org-roam/blob/master/CHANGELOG.md#111-18-05-2020">v1.1.1</a> I felt the lack of functions to manage them. Adding and removing them by hand is not nice. So in this article I am sharing a snippet that I’ve forged to ease the unbearable lightness of being.</p>
<p><strong>Change Log:</strong></p>
<ul>
<li><code>[2020-10-12 Mon]</code>: Functionality described in this post (and similar functionality to manage aliases) is <a href="https://github.com/org-roam/org-roam/pull/1183">merged to the upstream</a>. Now simply use one of the following functions:
<ul>
<li><code>org-roam-tag-add</code></li>
<li><code>org-roam-tag-delete</code></li>
<li><code>org-roam-alias-add</code></li>
<li><code>org-roam-alias-delete</code></li>
</ul></li>
</ul>
<!--more-->

<div class="post-image post-image">
<img src="../images/org-roam-tags-demo.gif" />
</div>

<p>When it comes to tags removal, I just want to have a list of tags set in the current buffer and chose one of them to remove. Important thing here is that it should not allow me to remove tag set by directories (see <code>org-roam-tag-sources</code>).</p>
<p>On the other hand, when I add a tag, I want to see the list of all tags set either by buffer property or by directory. I can chose one of them (otherwise I tend to mistype) or add a completely new one.</p>
<p>So let’s implement these two functions. But before that, we need to have a function to get the list of buffer wide tags. For this we can write some simple helper (that uses regexps) or reuse internal API from <code>org-roam</code> (that does all the dirty work for us).</p>
<div class="sourceCode" id="cb1" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb1-1"><a href="#cb1-1"></a>(<span class="kw">defun</span><span class="fu"> +org-notes-tags-read </span>()</span>
<span id="cb1-2"><a href="#cb1-2"></a>  <span class="st">&quot;Return list of tags as set in the buffer.&quot;</span></span>
<span id="cb1-3"><a href="#cb1-3"></a>  (org-roam--extract-tags-prop (buffer-file-name (buffer-base-buffer))))</span></code></pre></div>
<p>Now it’s easy to implement the function to delete one of the buffer tags.</p>
<div class="sourceCode" id="cb2" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb2-1"><a href="#cb2-1"></a>(<span class="kw">defun</span><span class="fu"> +org-notes-tags-delete </span>()</span>
<span id="cb2-2"><a href="#cb2-2"></a>  <span class="st">&quot;Delete a tag from current note.&quot;</span></span>
<span id="cb2-3"><a href="#cb2-3"></a>  (interactive)</span>
<span id="cb2-4"><a href="#cb2-4"></a>  (<span class="kw">unless</span> (+org-notes-buffer-p)</span>
<span id="cb2-5"><a href="#cb2-5"></a>    (user-error <span class="st">&quot;Current buffer is not a note&quot;</span>))</span>
<span id="cb2-6"><a href="#cb2-6"></a>  (<span class="kw">let*</span> ((tags (+org-notes-tags-read))</span>
<span id="cb2-7"><a href="#cb2-7"></a>         (tag (completing-read <span class="st">&quot;Tag: &quot;</span> tags <span class="kw">nil</span> 'require-match)))</span>
<span id="cb2-8"><a href="#cb2-8"></a>    (+org-buffer-prop-set</span>
<span id="cb2-9"><a href="#cb2-9"></a>     <span class="st">&quot;ROAM_TAGS&quot;</span></span>
<span id="cb2-10"><a href="#cb2-10"></a>     (combine-and-quote-strings (<span class="kw">delete</span> tag tags)))</span>
<span id="cb2-11"><a href="#cb2-11"></a>    (org-roam-db--update-tags)))</span></code></pre></div>
<p>Since it works only in the context of <code>org-roam</code> it’s good to have a meaningful error when this function used in the invalid context. Next we read the buffer tags and select one of them using <code>completing-read</code>. The <code>'require-match</code> is just a dummy non-nil value used instead of <code>t</code> as it improves readability.</p>
<p>Next we remove the selected tag from tags and set the result to the buffer property <code>ROAM_TAGS</code>, each tag quoted. Simple as that. I will provide implementation of <code>+org-notes-buffer-p</code> and <code>+org-buffer-prop-set</code> later on.</p>
<p>And the most important function is for adding tags.</p>
<div class="sourceCode" id="cb3" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb3-1"><a href="#cb3-1"></a>(<span class="kw">defun</span><span class="fu"> +org-notes-tags-add </span>()</span>
<span id="cb3-2"><a href="#cb3-2"></a>  <span class="st">&quot;Add a tag to current note.&quot;</span></span>
<span id="cb3-3"><a href="#cb3-3"></a>  (interactive)</span>
<span id="cb3-4"><a href="#cb3-4"></a>  (<span class="kw">unless</span> (+org-notes-buffer-p)</span>
<span id="cb3-5"><a href="#cb3-5"></a>    (user-error <span class="st">&quot;Current buffer is not a note&quot;</span>))</span>
<span id="cb3-6"><a href="#cb3-6"></a>  (<span class="kw">let*</span> ((tags (seq-uniq</span>
<span id="cb3-7"><a href="#cb3-7"></a>                (+seq-flatten</span>
<span id="cb3-8"><a href="#cb3-8"></a>                 (+seq-flatten</span>
<span id="cb3-9"><a href="#cb3-9"></a>                  (org-roam-db-query [:select tags :from tags])))))</span>
<span id="cb3-10"><a href="#cb3-10"></a>         (tag (completing-read <span class="st">&quot;Tag: &quot;</span> tags)))</span>
<span id="cb3-11"><a href="#cb3-11"></a>    (<span class="kw">when</span> (string-empty-p tag)</span>
<span id="cb3-12"><a href="#cb3-12"></a>      (user-error <span class="st">&quot;Tag can't be empty&quot;</span>))</span>
<span id="cb3-13"><a href="#cb3-13"></a>    (+org-buffer-prop-set</span>
<span id="cb3-14"><a href="#cb3-14"></a>     <span class="st">&quot;ROAM_TAGS&quot;</span></span>
<span id="cb3-15"><a href="#cb3-15"></a>     (combine-and-quote-strings (seq-uniq (<span class="kw">cons</span> tag (+org-notes-tags-read)))))</span>
<span id="cb3-16"><a href="#cb3-16"></a>    (org-roam-db--update-tags)))</span></code></pre></div>
<p>It also errors out when called outside of <code>org-roam</code> buffer. Then we query all tags from the <code>org-roam-db</code>. Since this is the list of lists of lists, we have two double flatten the result and then leave only unique entries. After that everything is straightforward.</p>
<p>Now the missing functions.</p>
<div class="sourceCode" id="cb4" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb4-1"><a href="#cb4-1"></a>(<span class="kw">defun</span><span class="fu"> +org-notes-buffer-p </span>()</span>
<span id="cb4-2"><a href="#cb4-2"></a>  <span class="st">&quot;Return non-nil if the currently visited buffer is a note.&quot;</span></span>
<span id="cb4-3"><a href="#cb4-3"></a>  (<span class="kw">and</span> buffer-file-name</span>
<span id="cb4-4"><a href="#cb4-4"></a>       (<span class="kw">string-equal</span> (file-name-as-directory org-roam-directory)</span>
<span id="cb4-5"><a href="#cb4-5"></a>                     (file-name-directory buffer-file-name))))</span>
<span id="cb4-6"><a href="#cb4-6"></a></span>
<span id="cb4-7"><a href="#cb4-7"></a>(<span class="kw">defun</span><span class="fu"> +seq-flatten </span>(list-of-lists)</span>
<span id="cb4-8"><a href="#cb4-8"></a>  <span class="st">&quot;Flatten LIST-OF-LISTS.&quot;</span></span>
<span id="cb4-9"><a href="#cb4-9"></a>  (<span class="kw">apply</span> #'append list-of-lists))</span>
<span id="cb4-10"><a href="#cb4-10"></a></span>
<span id="cb4-11"><a href="#cb4-11"></a>(<span class="kw">defun</span><span class="fu"> +org-buffer-prop-set </span>(name value)</span>
<span id="cb4-12"><a href="#cb4-12"></a>  <span class="st">&quot;Set a buffer property called NAME to VALUE.&quot;</span></span>
<span id="cb4-13"><a href="#cb4-13"></a>  (save-excursion</span>
<span id="cb4-14"><a href="#cb4-14"></a>    (widen)</span>
<span id="cb4-15"><a href="#cb4-15"></a>    (goto-char (point-min))</span>
<span id="cb4-16"><a href="#cb4-16"></a>    (<span class="kw">if</span> (re-search-forward (concat <span class="st">&quot;^</span><span class="ch">#\\</span><span class="st">+&quot;</span> name <span class="st">&quot;: </span><span class="sc">\\</span><span class="st">(.*</span><span class="sc">\\</span><span class="st">)&quot;</span>) (point-max) <span class="kw">t</span>)</span>
<span id="cb4-17"><a href="#cb4-17"></a>        (replace-match (concat <span class="st">&quot;#+&quot;</span> name <span class="st">&quot;: &quot;</span> value))</span>
<span id="cb4-18"><a href="#cb4-18"></a>      <span class="co">;; find the first line that doesn't begin with ':' or '#'</span></span>
<span id="cb4-19"><a href="#cb4-19"></a>      (<span class="kw">let</span> ((found))</span>
<span id="cb4-20"><a href="#cb4-20"></a>        (while (<span class="kw">not</span> (<span class="kw">or</span> found (eobp)))</span>
<span id="cb4-21"><a href="#cb4-21"></a>          (beginning-of-line)</span>
<span id="cb4-22"><a href="#cb4-22"></a>          (<span class="kw">if</span> (<span class="kw">or</span> (looking-at <span class="st">&quot;^#&quot;</span>)</span>
<span id="cb4-23"><a href="#cb4-23"></a>                  (looking-at <span class="st">&quot;^:&quot;</span>))</span>
<span id="cb4-24"><a href="#cb4-24"></a>              (line-move <span class="dv">1</span> <span class="kw">t</span>)</span>
<span id="cb4-25"><a href="#cb4-25"></a>            (<span class="kw">setq</span> found <span class="kw">t</span>)))</span>
<span id="cb4-26"><a href="#cb4-26"></a>        (insert <span class="st">&quot;#+&quot;</span> name <span class="st">&quot;: &quot;</span> value <span class="st">&quot;</span><span class="sc">\n</span><span class="st">&quot;</span>)))))</span></code></pre></div>
<p>That’s it. You can find all solution as a gist on <a href="https://gist.github.com/d12frosted/4a55f3d072a813159c1d7b31c21bac9a">GitHub</a>. Have fun!</p>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2020-06-10-org-roam-tags.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Emacs: moving to beginning of line</title>
    <link href="https://d12frosted.io/posts/2020-06-04-beginning-of-line.html" />
    <id>https://d12frosted.io/posts/2020-06-04-beginning-of-line.html</id>
    <published>2020-06-04T00:00:00Z</published>
    <updated>2020-06-04T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Emacs: moving to beginning of line</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Convenient functions to move cursor" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Convenient functions to move cursor">
    <meta property="og:description" content="Convenient functions to move cursor">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Emacs: moving to beginning of line</h1>
    <div class="date"><span>June  4, 2020</span></div>
    <div class="tags"><span>(<a href="../tags/emacs.html">emacs</a>)</span></div>
    <article>
      <section>
        <p>In stocks Emacs there is a function <code>move-to-beginning-of-line</code> which is bound to <code>C-a</code> by default. It does exactly what its name implies - moves the cursor to the beginning of line. In case of indented code this is usually not what I want, but instead I would love to move the first visible character of line.</p>
<div class="post-image post-image-half">
<img src="../images/beginning-of-line-haskell.gif" />
</div>

<p>There are packages that does this and even more (for example, <a href="https://github.com/alezost/mwim.el">mwim</a>), but if for some reason you don’t want to add one more package to your dependencies, or you care only about moving to the beginning of line, then you might grab the code that I share in the details. There is a version that also knows how to work with org mode headers and lists.</p>
<div class="post-image post-image-half">
<img src="../images/beginning-of-line-org.gif" />
</div>

<!--more-->

<p>The implementation is pretty straight-forward. We just disable visual movement and jump to first visible character using <code>back-to-indentation</code> and if we didn’t change our location, then we move the beginning of line.</p>
<div class="sourceCode" id="cb1" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb1-1"><a href="#cb1-1"></a>(<span class="kw">defun</span><span class="fu"> +beginning-of-line </span>(arg)</span>
<span id="cb1-2"><a href="#cb1-2"></a>  <span class="st">&quot;Move point back to indentation of beginning of line.</span></span>
<span id="cb1-3"><a href="#cb1-3"></a></span>
<span id="cb1-4"><a href="#cb1-4"></a><span class="st">Move point to the first non-whitespace character on this line. If</span></span>
<span id="cb1-5"><a href="#cb1-5"></a><span class="st">point is already there, move to the beginning of the line.</span></span>
<span id="cb1-6"><a href="#cb1-6"></a><span class="st">Effectively toggle between the first non-whitespace character and</span></span>
<span id="cb1-7"><a href="#cb1-7"></a><span class="st">the beginning of the line.</span></span>
<span id="cb1-8"><a href="#cb1-8"></a></span>
<span id="cb1-9"><a href="#cb1-9"></a><span class="st">If ARG is not nil, move forward ARG lines first. If point reaches</span></span>
<span id="cb1-10"><a href="#cb1-10"></a><span class="st">the beginning or end of the buffer, stop there.&quot;</span></span>
<span id="cb1-11"><a href="#cb1-11"></a>  (interactive <span class="st">&quot;P&quot;</span>)</span>
<span id="cb1-12"><a href="#cb1-12"></a>  (<span class="kw">when</span> (<span class="kw">numberp</span> arg)</span>
<span id="cb1-13"><a href="#cb1-13"></a>    (<span class="kw">let</span> ((line-move-visual <span class="kw">nil</span>))</span>
<span id="cb1-14"><a href="#cb1-14"></a>      (forward-line arg)))</span>
<span id="cb1-15"><a href="#cb1-15"></a></span>
<span id="cb1-16"><a href="#cb1-16"></a>  (<span class="kw">let</span> ((orig-point (point)))</span>
<span id="cb1-17"><a href="#cb1-17"></a>    (back-to-indentation)</span>
<span id="cb1-18"><a href="#cb1-18"></a>    (<span class="kw">when</span> (<span class="op">=</span> orig-point (point))</span>
<span id="cb1-19"><a href="#cb1-19"></a>      (move-beginning-of-line <span class="dv">1</span>))))</span>
<span id="cb1-20"><a href="#cb1-20"></a></span>
<span id="cb1-21"><a href="#cb1-21"></a>(global-set-key [remap move-beginning-of-line] '+beginning-of-line)</span></code></pre></div>
<p>Now when it comes to similar thing in <code>org-mode</code>, it’s more tricky. In some sense, it’s adaptation o <code>org-beginning-of-line</code>, which doesn’t support desired order of stop points, e.g. I want to stop at first meaningful character, then go left to the first visible character and only then to the beginning of line.</p>
<div class="sourceCode" id="cb2" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb2-1"><a href="#cb2-1"></a>(<span class="kw">defun</span><span class="fu"> +org-beginning-of-line </span>(arg)</span>
<span id="cb2-2"><a href="#cb2-2"></a>  <span class="st">&quot;Move point back to indentation of beginning of line.</span></span>
<span id="cb2-3"><a href="#cb2-3"></a></span>
<span id="cb2-4"><a href="#cb2-4"></a><span class="st">Move point to the first non-whitespace character on this line. If</span></span>
<span id="cb2-5"><a href="#cb2-5"></a><span class="st">point is already there, move to the beginning of the line.</span></span>
<span id="cb2-6"><a href="#cb2-6"></a><span class="st">Effectively toggle between the first non-whitespace character and</span></span>
<span id="cb2-7"><a href="#cb2-7"></a><span class="st">the beginning of the line.</span></span>
<span id="cb2-8"><a href="#cb2-8"></a></span>
<span id="cb2-9"><a href="#cb2-9"></a><span class="st">When the point is on the heading, move to the beginning of title,</span></span>
<span id="cb2-10"><a href="#cb2-10"></a><span class="st">but after any TODO keyword or priority cookie. Repeated usage</span></span>
<span id="cb2-11"><a href="#cb2-11"></a><span class="st">moves to the beginning of the line.</span></span>
<span id="cb2-12"><a href="#cb2-12"></a></span>
<span id="cb2-13"><a href="#cb2-13"></a><span class="st">When the point is on the bullet, first move to the beginning of</span></span>
<span id="cb2-14"><a href="#cb2-14"></a><span class="st">the item, but after bullet and check-box. Repeated usage</span></span>
<span id="cb2-15"><a href="#cb2-15"></a><span class="st">moves to the first non-whitespace character and then to the</span></span>
<span id="cb2-16"><a href="#cb2-16"></a><span class="st">beginning of the line.</span></span>
<span id="cb2-17"><a href="#cb2-17"></a></span>
<span id="cb2-18"><a href="#cb2-18"></a><span class="st">If ARG is not nil, move forward ARG lines first. If point reaches</span></span>
<span id="cb2-19"><a href="#cb2-19"></a><span class="st">the beginning or end of the buffer, stop there.&quot;</span></span>
<span id="cb2-20"><a href="#cb2-20"></a>  (interactive <span class="st">&quot;P&quot;</span>)</span>
<span id="cb2-21"><a href="#cb2-21"></a>  (<span class="kw">let</span> ((origin (point)))</span>
<span id="cb2-22"><a href="#cb2-22"></a>    <span class="co">;; move to the beginning of line, so regexp lookups properly capture context</span></span>
<span id="cb2-23"><a href="#cb2-23"></a>    (<span class="kw">when</span> (<span class="kw">numberp</span> arg)</span>
<span id="cb2-24"><a href="#cb2-24"></a>      (<span class="kw">let</span> ((line-move-visual <span class="kw">nil</span>))</span>
<span id="cb2-25"><a href="#cb2-25"></a>        (forward-line arg)))</span>
<span id="cb2-26"><a href="#cb2-26"></a>    (beginning-of-line)</span>
<span id="cb2-27"><a href="#cb2-27"></a>    (<span class="kw">cond</span></span>
<span id="cb2-28"><a href="#cb2-28"></a>     ((<span class="kw">let</span> ((case-fold-search <span class="kw">nil</span>)) (looking-at org-complex-heading-regexp))</span>
<span id="cb2-29"><a href="#cb2-29"></a>      <span class="co">;; at the headline, special position is before the title, but after any</span></span>
<span id="cb2-30"><a href="#cb2-30"></a>      <span class="co">;; TODO keyword or priority cookie.</span></span>
<span id="cb2-31"><a href="#cb2-31"></a>      (<span class="kw">let</span> ((refpos (<span class="kw">min</span> (<span class="op">1+</span> (<span class="kw">or</span> (match-end <span class="dv">3</span>) (match-end <span class="dv">2</span>) (match-end <span class="dv">1</span>)))</span>
<span id="cb2-32"><a href="#cb2-32"></a>                         (line-end-position)))</span>
<span id="cb2-33"><a href="#cb2-33"></a>            (bol (point)))</span>
<span id="cb2-34"><a href="#cb2-34"></a>        (<span class="kw">when</span> (<span class="kw">or</span> (<span class="op">&gt;</span> origin refpos) (<span class="op">=</span> origin bol))</span>
<span id="cb2-35"><a href="#cb2-35"></a>          (goto-char refpos))))</span>
<span id="cb2-36"><a href="#cb2-36"></a></span>
<span id="cb2-37"><a href="#cb2-37"></a>     ((<span class="kw">and</span> (looking-at org-list-full-item-re)</span>
<span id="cb2-38"><a href="#cb2-38"></a>           (memq (org-element-type (save-match-data (org-element-at-point)))</span>
<span id="cb2-39"><a href="#cb2-39"></a>                 '(item plain-list)))</span>
<span id="cb2-40"><a href="#cb2-40"></a>      <span class="co">;; Set special position at first white space character after</span></span>
<span id="cb2-41"><a href="#cb2-41"></a>      <span class="co">;; bullet, and check-box, if any.</span></span>
<span id="cb2-42"><a href="#cb2-42"></a>      (<span class="kw">let</span> ((after-bullet</span>
<span id="cb2-43"><a href="#cb2-43"></a>             (<span class="kw">let</span> ((box (match-end <span class="dv">3</span>)))</span>
<span id="cb2-44"><a href="#cb2-44"></a>               (<span class="kw">cond</span> ((<span class="kw">not</span> box) (match-end <span class="dv">1</span>))</span>
<span id="cb2-45"><a href="#cb2-45"></a>                     ((<span class="kw">eq</span> (char-after box) ?\s) (<span class="op">1+</span> box))</span>
<span id="cb2-46"><a href="#cb2-46"></a>                     (<span class="kw">t</span> box)))))</span>
<span id="cb2-47"><a href="#cb2-47"></a>        <span class="co">;; since we want proper rotation, we go back to the original point so</span></span>
<span id="cb2-48"><a href="#cb2-48"></a>        <span class="co">;; `bolp' works as expected</span></span>
<span id="cb2-49"><a href="#cb2-49"></a>        (goto-char origin)</span>
<span id="cb2-50"><a href="#cb2-50"></a>        (<span class="kw">cond</span></span>
<span id="cb2-51"><a href="#cb2-51"></a>         ((<span class="kw">or</span> (<span class="op">&gt;</span> origin after-bullet) (bolp))</span>
<span id="cb2-52"><a href="#cb2-52"></a>          (goto-char after-bullet))</span>
<span id="cb2-53"><a href="#cb2-53"></a>         ((<span class="op">=</span> origin after-bullet)</span>
<span id="cb2-54"><a href="#cb2-54"></a>          (back-to-indentation))</span>
<span id="cb2-55"><a href="#cb2-55"></a>         ((<span class="op">=</span> origin (point))</span>
<span id="cb2-56"><a href="#cb2-56"></a>          (move-beginning-of-line <span class="dv">1</span>)))))</span>
<span id="cb2-57"><a href="#cb2-57"></a></span>
<span id="cb2-58"><a href="#cb2-58"></a>     (<span class="kw">t</span></span>
<span id="cb2-59"><a href="#cb2-59"></a>      (back-to-indentation)</span>
<span id="cb2-60"><a href="#cb2-60"></a>      (<span class="kw">when</span> (<span class="op">=</span> origin (point))</span>
<span id="cb2-61"><a href="#cb2-61"></a>        (move-beginning-of-line <span class="dv">1</span>))))))</span>
<span id="cb2-62"><a href="#cb2-62"></a></span>
<span id="cb2-63"><a href="#cb2-63"></a>(define-key org-mode-map (kbd <span class="st">&quot;C-a&quot;</span>) '+org-beginning-of-line)</span></code></pre></div>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2020-06-04-beginning-of-line.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Readings Vol. I: precondition encoding</title>
    <link href="https://d12frosted.io/posts/2020-02-01-readings-vol-1.html" />
    <id>https://d12frosted.io/posts/2020-02-01-readings-vol-1.html</id>
    <published>2020-02-01T00:00:00Z</published>
    <updated>2020-02-01T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Readings Vol. I: precondition encoding</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Recommended articles and books" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Recommended articles and books">
    <meta property="og:description" content="Recommended articles and books">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Readings Vol. I: precondition encoding</h1>
    <div class="date"><span>February  1, 2020</span></div>
    <div class="tags"><span>(<a href="../tags/haskell.html">haskell</a>, <a href="../tags/readings.html">readings</a>)</span></div>
    <article>
      <section>
        <p>When we write libraries for others (including ourselves) to use we often require some preconditions to be met. Sometimes we just make them implicit (in the form of folklore, passed from developer to developer during post mortem ritual). In other cases we encode the possibility of failure due to unsatisfied preconditions.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1"></a><span class="ot">safeHead ::</span> [a] <span class="ot">-&gt;</span> <span class="dt">Maybe</span> a</span>
<span id="cb1-2"><a href="#cb1-2"></a>safeHead [] <span class="ot">=</span> <span class="dt">None</span></span>
<span id="cb1-3"><a href="#cb1-3"></a>safehead (a<span class="op">:</span>_) <span class="ot">=</span> <span class="dt">Just</span> a</span></code></pre></div>
<p>Is this is the only thing we can do? Definitely no! Is it the best thing we can do? Depends on the situation.</p>
<p>This article is part of the <a href="../tags/readings.html">readings</a> series where I take one topic and share links to related articles and papers. This time our focus is precondition encoding.</p>
<!--more-->

<p>In <a href="https://lexi-lambda.github.io/blog/2019/11/05/parse-don-t-validate/">Parse, don’t validate</a> article<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> Alexis King describes the meaning of type-driven design and explains the “parse, don’t validate” slogan using simple code examples. The most prominent one is comparison of the following two functions. Alexis then elaborates more on the differences between them.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1"></a><span class="ot">validateNonEmpty ::</span> [a] <span class="ot">-&gt;</span> <span class="dt">IO</span> ()</span>
<span id="cb2-2"><a href="#cb2-2"></a>validateNonEmpty (_<span class="op">:</span>_) <span class="ot">=</span> <span class="fu">pure</span> ()</span>
<span id="cb2-3"><a href="#cb2-3"></a>validateNonEmpty [] <span class="ot">=</span> throwIO <span class="op">$</span> <span class="fu">userError</span> <span class="st">&quot;list cannot be empty&quot;</span></span>
<span id="cb2-4"><a href="#cb2-4"></a></span>
<span id="cb2-5"><a href="#cb2-5"></a><span class="ot">parseNonEmpty ::</span> [a] <span class="ot">-&gt;</span> <span class="dt">IO</span> (<span class="dt">NonEmpty</span> a)</span>
<span id="cb2-6"><a href="#cb2-6"></a>parseNonEmpty (x<span class="op">:</span>xs) <span class="ot">=</span> <span class="fu">pure</span> (x<span class="op">:|</span>xs)</span>
<span id="cb2-7"><a href="#cb2-7"></a>parseNonEmpty [] <span class="ot">=</span> throwIO <span class="op">$</span> <span class="fu">userError</span> <span class="st">&quot;list cannot be empty&quot;</span></span></code></pre></div>
<p>I really like that in the parse approach the learned information is not discarded and can be used later, while validate approach completely discards what it learned.</p>
<p>Alexis also mentions one important thing. Using <code>Maybe</code> as resulting type of safe functions means that it’s easier to implement safe functions, but it pushes responsibility of the failure to the call site significantly increasing complexity of safe function user. This issue is talked through and Alexis provides a solution. You just need to be stricter about your input, that’s where all the static typing helps you. <a href="https://www.parsonsmatt.org/about/">Matt Parsons</a> has a wonderful <a href="https://www.parsonsmatt.org/2017/10/11/type_safety_back_and_forth.html">blog post</a> on this topic as well.</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1"></a><span class="ot">safeHead ::</span> <span class="dt">NonEmpty</span> a <span class="ot">-&gt;</span> a</span>
<span id="cb3-2"><a href="#cb3-2"></a>safeHead (x <span class="op">:|</span> xs) <span class="ot">=</span> x</span></code></pre></div>
<p>With this approach, users of <code>safeHead</code> must handle failure ahead of time. But this also means that the calling code doesn’t need to use things like <code>fromJust</code> when it is genuinely known that the list is non empty.</p>
<p>There is an orthogonal view on this exact problem by <a href="https://storm-country.com/">Matt Noonan</a> which he explains in the <a href="https://kataskeue.com/gdp.pdf">Ghosts of Departed Proofs (Functional Pearl)</a> paper. Matt describes an approach where preconditions (like non-empty list or key existence in the map) are encoded in the Haskell type system as phantoms. Users must prove that preconditions are met using exported set of lemmas.</p>
<p>One of my favourite examples from this paper is the function that computes the dot product of the vector and its reverse. It can be implemented in terms of a regular dot product function, which has one important precondition - both vectors must share the same length. So we encode it on the type level.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1"></a><span class="co">-- regular dot product</span></span>
<span id="cb4-2"><a href="#cb4-2"></a><span class="ot">dot ::</span> ([<span class="dt">Double</span>] <span class="op">~~</span> vec1 <span class="op">:::</span> <span class="dt">Length</span> vec1 <span class="op">==</span> n)</span>
<span id="cb4-3"><a href="#cb4-3"></a>    <span class="ot">-&gt;</span> ([<span class="dt">Double</span>] <span class="op">~~</span> vec2 <span class="op">:::</span> <span class="dt">Length</span> vec2 <span class="op">==</span> n)</span>
<span id="cb4-4"><a href="#cb4-4"></a>    <span class="ot">-&gt;</span> <span class="dt">Double</span></span>
<span id="cb4-5"><a href="#cb4-5"></a>dot v1 v2 <span class="ot">=</span> <span class="fu">sum</span> (<span class="fu">zipWith</span> (<span class="op">*</span>) v1 v2)</span></code></pre></div>
<p>You can read it as ‘give me some list which I call <code>vec1</code> such that its length is equal to <code>n</code> and then give me some other list which I call <code>vec2</code> such that its length is also equal to <code>n</code> and I will give you some number back’. As you can see, it’s much more verbose compared to <code>dot :: [Double] -&gt; [Double] -&gt;
Double</code>, but it caries an important requirement on the type level, so no value is accidentally discarded by <code>zipWith</code>. Now if we want to write a function that calculates the dot product of vector and it’s reverse we need to provide a proof that the length of reversed list is equal to the original lists’ length.</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1"></a><span class="ot">dot_rev ::</span> [<span class="dt">Double</span>] <span class="ot">-&gt;</span> <span class="dt">Double</span></span>
<span id="cb5-2"><a href="#cb5-2"></a>dot_rev xs <span class="ot">=</span> name xs <span class="op">$</span></span>
<span id="cb5-3"><a href="#cb5-3"></a>  \vec <span class="ot">-&gt;</span> dot (vec <span class="op">...</span>refl) (<span class="fu">reverse</span> vec <span class="op">...</span>rev_length)</span></code></pre></div>
<p>First thing that we notice is that this function doesn’t have any preconditions, it works with any vector. And then there are <code>name</code>, <code>...refl</code> and <code>...rev_length</code> gluing the <code>dot</code> function and it’s arguments: original vector and its reverse. I am not going to explain everything (this is what paper does after all), my goal is to interest you enough so that you read the paper.</p>
<p>In short, <code>name</code> is the way to give some fixed name to it’s argument and then evaluate a function that takes a named argument. It uses Rank-2 scoping (the trick from ST<a href="#fn2" class="footnote-ref" id="fnref2" role="doc-noteref"><sup>2</sup></a>). Now since we have a named vector we need to prove that <code>vec</code> and <code>reverse vec</code> share the same length. So we need to provide a proof that the length of the first vector is equal to some <code>n</code> and then prove that the length of the second vector is equal to the same <code>n</code>. First proof is provided by reflection. <code>...</code> is just an operator which gives a way to attach a proof. The second vectors length is proven by lemma (or in this case, axiom) called <code>rev_length</code>, and it’s implementation is simple.</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1"></a><span class="ot">rev_length ::</span> <span class="dt">Proof</span> (<span class="dt">Length</span> (<span class="dt">Rev</span> xs) <span class="op">==</span> <span class="dt">Length</span> xs)</span>
<span id="cb6-2"><a href="#cb6-2"></a>rev_length <span class="ot">=</span> axiom</span></code></pre></div>
<p>It just says that there is an axiom that says that the length of the list is equal to the length of its reverse. And we use it to make the compiler happy. The meaning of <code>Length</code> and <code>Rev</code> are explained in the… paper. So please take a look!</p>
<p>It’s also interesting that all this machinery is implemented using Phantom Types and coercions, so compiler discards a lot of stuff from the runtime (like naming and proof providing), which means that there is no run-time penalty on having compile-time guarantees. We already <a href="2020-01-20-predicate-composition.html">touched this topic previously</a>.</p>
<p>Now, it’s up to you how far to go into encoding preconditions. I think that in many cases it’s enough to use proper data types. In some very specific scenarios it’s easier (or more convenient) to use ghosts of departed proofs approach. But always keep in mind, that it’s possible to move constraints into input type instead of wrapping result into <code>Maybe</code> (or alike). As people say, make illegal states unrepresentable.</p>
<p>Stay safe!</p>
<h1 id="references">References</h1>
<ol>
<li>Alexis King. Parse, don’t validate. <a href="https://lexi-lambda.github.io/blog/2019/11/05/parse-don-t-validate/">https://lexi-lambda.github.io/blog/2019/11/05/parse-don-t-validate/</a>, 2019.</li>
<li>Alexis King. No, dynamic type systems are not inherently more open. <a href="https://lexi-lambda.github.io/blog/2020/01/19/no-dynamic-type-systems-are-not-inherently-more-open/">https://lexi-lambda.github.io/blog/2020/01/19/no-dynamic-type-systems-are-not-inherently-more-open/</a>, 2020.</li>
<li>Haskell Wiki. Monad/ST. <a href="https://wiki.haskell.org/Monad/ST">https://wiki.haskell.org/Monad/ST</a>, 2012. Accessed: 2020-01-28.</li>
<li>Sandy Maguire. Thinking with Types. <a href="https://leanpub.com/thinking-with-types">https://leanpub.com/thinking-with-types</a>, 2018.</li>
<li>Matt Parsons. Type Safety Back and Forth. <a href="https://www.parsonsmatt.org/2017/10/11/type_safety_back_and_forth.html">https://www.parsonsmatt.org/2017/10/11/type_safety_back_and_forth.html</a>, 2017.</li>
<li>M. Noonan. Ghosts of departed proofs. <a href="http://www.github.com/matt-noonan/gdp-paper/">http://www.github.com/matt-noonan/gdp-paper/</a>, 2018. Accessed: 2020-01-28.</li>
<li>Boris Buliga. Predicate composition. <a href="https://d12frosted.io/posts/2020-01-20-predicate-composition.html">https://d12frosted.io/posts/2020-01-20-predicate-composition.html</a>, 2020.</li>
</ol>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>If you liked this post, please make sure to read the next part ‘<a href="https://lexi-lambda.github.io/blog/2020/01/19/no-dynamic-type-systems-are-not-inherently-more-open/">No, dynamic type systems are not inherently more open</a>’ which is an open answer to some of the original article’ comments.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2" role="doc-endnote"><p>ST monad enables pure computations with local mutable variable that is not exposed (leaked) to the outside. It uses Rank-2 trick to enforce the scope of the variable on the compile time. You can read more about ST on <a href="https://wiki.haskell.org/Monad/ST">Haskell Wiki</a>, <a href="https://stackoverflow.com/questions/12468622/how-does-the-st-monad-work">Stackoverflow</a>, but the best explanation can be found in <a href="https://leanpub.com/thinking-with-types">Thinking with Types</a> book by <a href="https://reasonablypolymorphic.com/">Sandy Maguire</a>, Chapter 7.2 Scoping Information with Existentials.<a href="#fnref2" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2020-02-01-readings-vol-1.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Predicate composition</title>
    <link href="https://d12frosted.io/posts/2020-01-20-predicate-composition.html" />
    <id>https://d12frosted.io/posts/2020-01-20-predicate-composition.html</id>
    <published>2020-01-20T00:00:00Z</published>
    <updated>2020-01-20T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Predicate composition</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Combinators for predicate composition" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Combinators for predicate composition">
    <meta property="og:description" content="Combinators for predicate composition">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Predicate composition</h1>
    <div class="date"><span>January 20, 2020</span></div>
    <div class="tags"><span>(<a href="../tags/haskell.html">haskell</a>, <a href="../tags/semigroup.html">semigroup</a>, <a href="../tags/performance.html">performance</a>)</span></div>
    <article>
      <section>
        <p>They say functional programming has many essences and the composition is one of them. Thanks to the wonderful dot operator, we know how to compose functions like <code>a -&gt; b</code> and <code>b -&gt; c</code> to get a function <code>a -&gt; c</code>. But in some cases functions are not that simple and it becomes tricky to compose them nicely.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1"></a><span class="ot">valid ::</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb1-2"><a href="#cb1-2"></a>valid <span class="ot">=</span> \a <span class="ot">-&gt;</span> check1 a <span class="op">&amp;&amp;</span> (check2 a <span class="op">||</span> check3 a)</span>
<span id="cb1-3"><a href="#cb1-3"></a>  <span class="kw">where</span> check1 <span class="ot">= undefined ::</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb1-4"><a href="#cb1-4"></a>        check2 <span class="ot">= undefined ::</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb1-5"><a href="#cb1-5"></a>        check3 <span class="ot">= undefined ::</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span></code></pre></div>
<p>It would be lovely to express it in a more declarative way by abstracting away function application and result combination.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1"></a><span class="ot">valid ::</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb2-2"><a href="#cb2-2"></a>valid <span class="ot">=</span> check1 <span class="op">.&amp;&amp;</span> (check2 <span class="op">.||</span> check3)</span>
<span id="cb2-3"><a href="#cb2-3"></a></span>
<span id="cb2-4"><a href="#cb2-4"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb2-5"><a href="#cb2-5"></a><span class="ot">(.||) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span></code></pre></div>
<p>Apart from implementing combinators for predicate composition, we want to avoid any runtime penalty from using abstractions. In this article we are going to implement the following functions and investigate how far we can go with abstractions until performance degrades. Or maybe it won’t degrade. Who knows?</p>
<!--more-->

<h1 id="inline-implementation">Inline implementation</h1>
<p>Before we jump into the void, lets draw a baseline by keeping implementation simple.</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1"></a><span class="kw">module</span> <span class="dt">Main</span> (main) <span class="kw">where</span></span>
<span id="cb3-2"><a href="#cb3-2"></a></span>
<span id="cb3-3"><a href="#cb3-3"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb3-4"><a href="#cb3-4"></a>main <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb3-5"><a href="#cb3-5"></a>  input <span class="ot">&lt;-</span> <span class="fu">read</span> <span class="op">&lt;$&gt;</span> <span class="fu">getLine</span></span>
<span id="cb3-6"><a href="#cb3-6"></a>  <span class="kw">let</span> result <span class="ot">=</span> input <span class="op">&lt;</span> <span class="dv">10</span> <span class="op">&amp;&amp;</span> (input <span class="op">&gt;</span> <span class="dv">0</span> <span class="op">||</span> <span class="fu">even</span> input)</span>
<span id="cb3-7"><a href="#cb3-7"></a>  <span class="fu">print</span> result</span></code></pre></div>
<p>Haskell language is abstract and high-level thus in some cases in order to really understand what the program does we need to look at the intermediate language called <a href="https://www.aosabook.org/en/ghc.html">Core</a> (or System FC) produced by Glasgow Haskell Compiler (GHC) when it compiles<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> our program. This reduced code is the end result of GHC’s optmisations-by-transformation process, which iteratively rewrites the original code into more optimised versions in a smaller language.</p>
<p>In order to dump the intermediate code in Core language we need to ask GHC to do it. We use <code>-O</code> (or <code>-O2</code>) to enable optimisations and <code>-ddump-simpl</code> to dump the simplified output, which can be combined with <code>-ddump-to-file</code> to write result into a file instead of <code>stdout</code>. More options are described in the <a href="https://downloads.haskell.org/ghc/latest/docs/html/users_guide/debugging.html#debugging-the-compiler">GHC manual</a>.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb4-1"><a href="#cb4-1"></a>$ <span class="ex">ghc</span> -O -ddump-simpl Main.hs</span></code></pre></div>
<p>Or if you are using <a href="https://docs.haskellstack.org/en/stable/README/">stack</a>:</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb5-1"><a href="#cb5-1"></a>$ <span class="ex">stack</span> ghc -- -O -ddump-simpl Main.hs</span></code></pre></div>
<p>This <a href="https://github.com/d12frosted/d12frosted.io/blob/master/assets/snippets/predicate-composition/inline.dump-simpl">prints a lot of stuff</a>, so let me focus on the most important part.</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1"></a><span class="op">...</span></span>
<span id="cb6-2"><a href="#cb6-2"></a></span>
<span id="cb6-3"><a href="#cb6-3"></a><span class="op">Main.$</span><span class="ot">seven1 ::</span> <span class="dt">Integer</span></span>
<span id="cb6-4"><a href="#cb6-4"></a><span class="op">Main.$</span>seven1 <span class="ot">=</span> <span class="dv">0</span></span>
<span id="cb6-5"><a href="#cb6-5"></a></span>
<span id="cb6-6"><a href="#cb6-6"></a><span class="op">...</span></span>
<span id="cb6-7"><a href="#cb6-7"></a></span>
<span id="cb6-8"><a href="#cb6-8"></a><span class="op">Main.$</span><span class="ot">seven2 ::</span> <span class="dt">Integer</span></span>
<span id="cb6-9"><a href="#cb6-9"></a><span class="op">Main.$</span>seven2 <span class="ot">=</span> <span class="dv">2</span></span>
<span id="cb6-10"><a href="#cb6-10"></a></span>
<span id="cb6-11"><a href="#cb6-11"></a><span class="op">...</span></span>
<span id="cb6-12"><a href="#cb6-12"></a></span>
<span id="cb6-13"><a href="#cb6-13"></a>Main.main3<span class="ot"> ::</span> <span class="dt">Integer</span></span>
<span id="cb6-14"><a href="#cb6-14"></a>Main.main3 <span class="ot">=</span> <span class="dv">10</span></span>
<span id="cb6-15"><a href="#cb6-15"></a></span>
<span id="cb6-16"><a href="#cb6-16"></a><span class="op">...</span></span>
<span id="cb6-17"><a href="#cb6-17"></a></span>
<span id="cb6-18"><a href="#cb6-18"></a>  <span class="co">-- check if input &lt; 10</span></span>
<span id="cb6-19"><a href="#cb6-19"></a>  <span class="kw">case</span> integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.ltInteger<span class="op">#</span></span>
<span id="cb6-20"><a href="#cb6-20"></a>         x1_a6kH    <span class="co">-- input</span></span>
<span id="cb6-21"><a href="#cb6-21"></a>         Main.main3 <span class="co">-- 10</span></span>
<span id="cb6-22"><a href="#cb6-22"></a>  <span class="kw">of</span> {</span>
<span id="cb6-23"><a href="#cb6-23"></a>    <span class="co">-- false</span></span>
<span id="cb6-24"><a href="#cb6-24"></a>    __DEFAULT <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool4; <span class="co">-- false</span></span>
<span id="cb6-25"><a href="#cb6-25"></a></span>
<span id="cb6-26"><a href="#cb6-26"></a>    <span class="co">-- true</span></span>
<span id="cb6-27"><a href="#cb6-27"></a>    <span class="dv">1</span><span class="op">#</span> <span class="ot">-&gt;</span></span>
<span id="cb6-28"><a href="#cb6-28"></a>      <span class="co">-- check if input &gt; 0</span></span>
<span id="cb6-29"><a href="#cb6-29"></a>      <span class="kw">case</span> integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.gtInteger<span class="op">#</span></span>
<span id="cb6-30"><a href="#cb6-30"></a>             x1_a6kH      <span class="co">-- input</span></span>
<span id="cb6-31"><a href="#cb6-31"></a>             <span class="op">Main.$</span>seven1 <span class="co">-- 0</span></span>
<span id="cb6-32"><a href="#cb6-32"></a>      <span class="kw">of</span> {</span>
<span id="cb6-33"><a href="#cb6-33"></a>        __DEFAULT <span class="ot">-&gt;</span></span>
<span id="cb6-34"><a href="#cb6-34"></a>          <span class="co">-- check if input is even (e.g. rem input 2 == 0)</span></span>
<span id="cb6-35"><a href="#cb6-35"></a>          <span class="kw">case</span> integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.eqInteger<span class="op">#</span></span>
<span id="cb6-36"><a href="#cb6-36"></a>                 (integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.remInteger</span>
<span id="cb6-37"><a href="#cb6-37"></a>                    x1_a6kH      <span class="co">-- input</span></span>
<span id="cb6-38"><a href="#cb6-38"></a>                    <span class="op">Main.$</span>seven2 <span class="co">-- 2</span></span>
<span id="cb6-39"><a href="#cb6-39"></a>                 )</span>
<span id="cb6-40"><a href="#cb6-40"></a>                 <span class="op">Main.$</span>seven1    <span class="co">-- 0</span></span>
<span id="cb6-41"><a href="#cb6-41"></a>          <span class="kw">of</span> {</span>
<span id="cb6-42"><a href="#cb6-42"></a>            __DEFAULT <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool4; <span class="co">-- false</span></span>
<span id="cb6-43"><a href="#cb6-43"></a>            <span class="dv">1</span><span class="op">#</span> <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool2         <span class="co">-- true</span></span>
<span id="cb6-44"><a href="#cb6-44"></a>          };</span>
<span id="cb6-45"><a href="#cb6-45"></a>        <span class="dv">1</span><span class="op">#</span> <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool2             <span class="co">-- true</span></span>
<span id="cb6-46"><a href="#cb6-46"></a>      }</span>
<span id="cb6-47"><a href="#cb6-47"></a>  };</span>
<span id="cb6-48"><a href="#cb6-48"></a></span>
<span id="cb6-49"><a href="#cb6-49"></a><span class="op">...</span></span></code></pre></div>
<p>So this how it looks in Core, verbose but really straightforward.</p>
<h1 id="reason-to-read-the-line">Reason to read the line</h1>
<p>Strictly speaking there is no need for reading integer from <code>stdin</code> in our example. After all, we care only about the predicates. But GHC is pretty aggressive in terms of in-lining and simplifications when optimisations are enabled. With <code>-O2</code> there will be even more cross-module optimisation compared to <code>-O</code>.</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb7-1"><a href="#cb7-1"></a><span class="kw">module</span> <span class="dt">Main</span> (main) <span class="kw">where</span></span>
<span id="cb7-2"><a href="#cb7-2"></a></span>
<span id="cb7-3"><a href="#cb7-3"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb7-4"><a href="#cb7-4"></a>main <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb7-5"><a href="#cb7-5"></a>  <span class="kw">let</span> input  <span class="ot">=</span> <span class="dv">5</span></span>
<span id="cb7-6"><a href="#cb7-6"></a>  <span class="kw">let</span> result <span class="ot">=</span> input <span class="op">&lt;</span> <span class="dv">10</span> <span class="op">&amp;&amp;</span> (input <span class="op">&gt;</span> <span class="dv">0</span> <span class="op">||</span> <span class="fu">even</span> input)</span>
<span id="cb7-7"><a href="#cb7-7"></a>  <span class="fu">print</span> result</span></code></pre></div>
<p>Compiling this module with <code>-O</code> produces <a href="https://github.com/d12frosted/d12frosted.io/blob/master/assets/snippets/predicate-composition/no-getline.dump-simpl">the following Core</a> (83 lines).</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb8-1"><a href="#cb8-1"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb8-2"><a href="#cb8-2"></a>[<span class="dt">GblId</span>,</span>
<span id="cb8-3"><a href="#cb8-3"></a> <span class="dt">Arity</span><span class="ot">=</span><span class="dv">1</span>,</span>
<span id="cb8-4"><a href="#cb8-4"></a> <span class="dt">Unf</span><span class="ot">=</span><span class="dt">Unf</span>{<span class="dt">Src</span><span class="op">=&lt;</span>vanilla<span class="op">&gt;</span>, <span class="dt">TopLvl</span><span class="ot">=</span><span class="dt">True</span>, <span class="dt">Value</span><span class="ot">=</span><span class="dt">True</span>, <span class="dt">ConLike</span><span class="ot">=</span><span class="dt">True</span>,</span>
<span id="cb8-5"><a href="#cb8-5"></a>         <span class="dt">WorkFree</span><span class="ot">=</span><span class="dt">True</span>, <span class="dt">Expandable</span><span class="ot">=</span><span class="dt">True</span>, <span class="dt">Guidance</span><span class="ot">=</span><span class="dt">IF_ARGS</span> [] <span class="dv">40</span> <span class="dv">60</span>}]</span>
<span id="cb8-6"><a href="#cb8-6"></a>main</span>
<span id="cb8-7"><a href="#cb8-7"></a>  <span class="ot">=</span> GHC.IO.Handle.Text.hPutStr'</span>
<span id="cb8-8"><a href="#cb8-8"></a>      GHC.IO.Handle.FD.stdout <span class="op">GHC.Show.$</span>fShowBool2 <span class="dt">GHC.Types.True</span></span></code></pre></div>
<p>As you can see, it figured out that there is no need to evaluate it in runtime. But in order to compare different implementations of composition operators, we don’t want compiler to inline the result.</p>
<p>If you are curious about reductions steps, you can pass <code>-v</code> option to <code>ghc</code> to be more verbose. When you build with <code>-v</code>, compilation of the version with <code>getLine</code> is less verbose than without.</p>
<h1 id="trivial-implementation">Trivial implementation</h1>
<p>Now that we have a solid source of nightmares, let’s return to cozy nook. Our first step is to create operators in the most trivial manner.</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb9-1"><a href="#cb9-1"></a><span class="kw">module</span> <span class="dt">Main</span> (main) <span class="kw">where</span></span>
<span id="cb9-2"><a href="#cb9-2"></a></span>
<span id="cb9-3"><a href="#cb9-3"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb9-4"><a href="#cb9-4"></a>main <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb9-5"><a href="#cb9-5"></a>  input <span class="ot">&lt;-</span> <span class="fu">read</span> <span class="op">&lt;$&gt;</span> <span class="fu">getLine</span></span>
<span id="cb9-6"><a href="#cb9-6"></a>  <span class="kw">let</span> result <span class="ot">=</span> (<span class="op">&lt;</span> <span class="dv">10</span>) <span class="op">.&amp;&amp;</span> ((<span class="op">&gt;</span> <span class="dv">0</span>) <span class="op">.||</span> <span class="fu">even</span>) <span class="op">$</span> input</span>
<span id="cb9-7"><a href="#cb9-7"></a>  <span class="fu">print</span> result</span>
<span id="cb9-8"><a href="#cb9-8"></a></span>
<span id="cb9-9"><a href="#cb9-9"></a><span class="kw">infixr</span> <span class="dv">3</span> <span class="op">.&amp;&amp;</span></span>
<span id="cb9-10"><a href="#cb9-10"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb9-11"><a href="#cb9-11"></a>p1 <span class="op">.&amp;&amp;</span> p2 <span class="ot">=</span> \a <span class="ot">-&gt;</span> p1 a <span class="op">&amp;&amp;</span> p2 a</span>
<span id="cb9-12"><a href="#cb9-12"></a></span>
<span id="cb9-13"><a href="#cb9-13"></a><span class="kw">infixr</span> <span class="dv">2</span> <span class="op">.||</span></span>
<span id="cb9-14"><a href="#cb9-14"></a><span class="ot">(.||) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb9-15"><a href="#cb9-15"></a>p1 <span class="op">.||</span> p2 <span class="ot">=</span> \a <span class="ot">-&gt;</span> p1 a <span class="op">||</span> p2 a</span></code></pre></div>
<p>If we compile it, the relevant part in the <em>Core language</em> is the same.</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb10-1"><a href="#cb10-1"></a><span class="op">...</span></span>
<span id="cb10-2"><a href="#cb10-2"></a></span>
<span id="cb10-3"><a href="#cb10-3"></a>  <span class="kw">case</span> integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.ltInteger<span class="op">#</span></span>
<span id="cb10-4"><a href="#cb10-4"></a>         x1_a6m7 Main.main3</span>
<span id="cb10-5"><a href="#cb10-5"></a>  <span class="kw">of</span> {</span>
<span id="cb10-6"><a href="#cb10-6"></a>    __DEFAULT <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool4;</span>
<span id="cb10-7"><a href="#cb10-7"></a>    <span class="dv">1</span><span class="op">#</span> <span class="ot">-&gt;</span></span>
<span id="cb10-8"><a href="#cb10-8"></a>      <span class="kw">case</span> integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.gtInteger<span class="op">#</span></span>
<span id="cb10-9"><a href="#cb10-9"></a>             x1_a6m7 <span class="op">Main.$</span>seven1</span>
<span id="cb10-10"><a href="#cb10-10"></a>      <span class="kw">of</span> {</span>
<span id="cb10-11"><a href="#cb10-11"></a>        __DEFAULT <span class="ot">-&gt;</span></span>
<span id="cb10-12"><a href="#cb10-12"></a>          <span class="kw">case</span> integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.eqInteger<span class="op">#</span></span>
<span id="cb10-13"><a href="#cb10-13"></a>                 (integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.remInteger</span>
<span id="cb10-14"><a href="#cb10-14"></a>                    x1_a6m7 <span class="op">Main.$</span>seven2)</span>
<span id="cb10-15"><a href="#cb10-15"></a>                 <span class="op">Main.$</span>seven1</span>
<span id="cb10-16"><a href="#cb10-16"></a>          <span class="kw">of</span> {</span>
<span id="cb10-17"><a href="#cb10-17"></a>            __DEFAULT <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool4;</span>
<span id="cb10-18"><a href="#cb10-18"></a>            <span class="dv">1</span><span class="op">#</span> <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool2</span>
<span id="cb10-19"><a href="#cb10-19"></a>          };</span>
<span id="cb10-20"><a href="#cb10-20"></a>        <span class="dv">1</span><span class="op">#</span> <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool2</span>
<span id="cb10-21"><a href="#cb10-21"></a>      }</span>
<span id="cb10-22"><a href="#cb10-22"></a>  };</span>
<span id="cb10-23"><a href="#cb10-23"></a></span>
<span id="cb10-24"><a href="#cb10-24"></a><span class="op">...</span></span></code></pre></div>
<p>While our code looks better, there are no runtime penalties. In short, with <code>-O</code> option GHC always tries to inline small functions (based on <a href="https://downloads.haskell.org/ghc/latest/docs/html/users_guide/using-optimisation.html#ghc-flag--funfolding-creation-threshold=%E2%9F%A8n%E2%9F%A9">unfolding-creation-threshold</a> and heuristics) thus avoiding the call overhead and enabling other optimisations (like replacing whole expression with its result). And when unfolding doesn’t happen for some of the reasons and you really think that it should happen (make such decision based on CPU and memory profiling), then put <a href="https://downloads.haskell.org/ghc/latest/docs/html/users_guide/glasgow_exts.html#inline-pragma">INLINE pragma</a>.</p>
<div class="sourceCode" id="cb11"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb11-1"><a href="#cb11-1"></a><span class="kw">infixr</span> <span class="dv">3</span> <span class="op">.&amp;&amp;</span></span>
<span id="cb11-2"><a href="#cb11-2"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb11-3"><a href="#cb11-3"></a>p1 <span class="op">.&amp;&amp;</span> p2 <span class="ot">=</span> \a <span class="ot">-&gt;</span> p1 a <span class="op">&amp;&amp;</span> p2 a</span>
<span id="cb11-4"><a href="#cb11-4"></a><span class="ot">{-# INLINE (.&amp;&amp;) #-}</span></span></code></pre></div>
<p>Please note that in-lining usually leads to bigger executable.</p>
<h1 id="using-newtype-wrappers">Using <code>newtype</code> wrappers</h1>
<p>If we look at the definition of <code>.&amp;&amp;</code> and <code>.||</code> we see that they are pretty much the same. The only difference is the use of <code>&amp;&amp;</code> instead of <code>||</code>.</p>
<div class="sourceCode" id="cb12"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb12-1"><a href="#cb12-1"></a><span class="kw">infixr</span> <span class="dv">3</span> <span class="op">.&amp;&amp;</span></span>
<span id="cb12-2"><a href="#cb12-2"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb12-3"><a href="#cb12-3"></a>p1 <span class="op">.&amp;&amp;</span> p2 <span class="ot">=</span> \a <span class="ot">-&gt;</span> p1 a <span class="op">&amp;&amp;</span> p2 a</span>
<span id="cb12-4"><a href="#cb12-4"></a></span>
<span id="cb12-5"><a href="#cb12-5"></a><span class="kw">infixr</span> <span class="dv">2</span> <span class="op">.||</span></span>
<span id="cb12-6"><a href="#cb12-6"></a><span class="ot">(.||) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb12-7"><a href="#cb12-7"></a>p1 <span class="op">.||</span> p2 <span class="ot">=</span> \a <span class="ot">-&gt;</span> p1 a <span class="op">||</span> p2 a</span></code></pre></div>
<p>Maybe there is some magic function that takes a function for combining two booleans, two predicates, a value and returns a boolean? So we can express our combinators with it.</p>
<div class="sourceCode" id="cb13"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb13-1"><a href="#cb13-1"></a><span class="ot">magic ::</span> (<span class="dt">Bool</span> <span class="ot">-&gt;</span> <span class="dt">Bool</span> <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb13-2"><a href="#cb13-2"></a>magic plus p1 p2 <span class="ot">=</span> \a <span class="ot">-&gt;</span> p1 a <span class="ot">`plus`</span> p2 a</span></code></pre></div>
<p>Or even more generic one:</p>
<div class="sourceCode" id="cb14"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb14-1"><a href="#cb14-1"></a><span class="ot">gmagic ::</span> (b <span class="ot">-&gt;</span> b <span class="ot">-&gt;</span> b) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> b) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> b) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> b</span>
<span id="cb14-2"><a href="#cb14-2"></a>gmagic plus p1 p2 <span class="ot">=</span> \a <span class="ot">-&gt;</span> p1 a <span class="ot">`plus`</span> p2 a</span></code></pre></div>
<p>This all reminds me of <code>Semigroup</code>.</p>
<div class="sourceCode" id="cb15"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb15-1"><a href="#cb15-1"></a><span class="kw">class</span> <span class="dt">Semigroup</span> a <span class="kw">where</span></span>
<span id="cb15-2"><a href="#cb15-2"></a><span class="ot">  (&lt;&gt;) ::</span> a <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> a</span>
<span id="cb15-3"><a href="#cb15-3"></a></span>
<span id="cb15-4"><a href="#cb15-4"></a><span class="ot">gmagic ::</span> (<span class="dt">Semigroup</span> b) <span class="ot">=&gt;</span> (a <span class="ot">-&gt;</span> b) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> b) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> b</span>
<span id="cb15-5"><a href="#cb15-5"></a>gmagic f g <span class="ot">=</span> \a <span class="ot">-&gt;</span> f a <span class="op">&lt;&gt;</span> g a</span></code></pre></div>
<p>Thanks to <code>Semigroup</code> the <code>plus</code> function is not passed explicitly and <code>gmagic</code> become lighter. Now, functions which return type is an instance of <code>Semigroup</code> also form <code>Semigroup</code> and it’s implementation looks familiar.</p>
<div class="sourceCode" id="cb16"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb16-1"><a href="#cb16-1"></a><span class="kw">instance</span> <span class="dt">Semigroup</span> b <span class="ot">=&gt;</span> <span class="dt">Semigroup</span> (a <span class="ot">-&gt;</span> b) <span class="kw">where</span></span>
<span id="cb16-2"><a href="#cb16-2"></a>  f <span class="op">&lt;&gt;</span> g <span class="ot">=</span> \a <span class="ot">-&gt;</span> f a <span class="op">&lt;&gt;</span> g a</span></code></pre></div>
<p>So it turns out that our <code>gmagic</code> function is a binary operator from <code>Semigroup</code>. How convenient, isn’t it? If we add more parenthesis to the signature you’ll notice that it actually takes two functions and produces new one (exactly what we are doing with predicates).</p>
<div class="sourceCode" id="cb17"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb17-1"><a href="#cb17-1"></a><span class="ot">gmagic ::</span> (<span class="dt">Semigroup</span> b) <span class="ot">=&gt;</span> (a <span class="ot">-&gt;</span> b) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> b) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> b)</span>
<span id="cb17-2"><a href="#cb17-2"></a>gmagic f g <span class="ot">=</span> \a <span class="ot">-&gt;</span> f a <span class="op">&lt;&gt;</span> g a</span></code></pre></div>
<p>In Haskell every single data type can have not more than one instance of a given type class. But for some data types there are more than one valid (lawful) instances of a given type class. For example, we know that the set of natural numbers forms different semigroups with different operations: <span class="math inline">\( ( \mathbb{N}, +
) \)</span> or <span class="math inline">\( ( \mathbb{N}, \cdot ) \)</span>. The same story with booleans - <span class="math inline">\( (
\mathbb{B}, \wedge ) \)</span> and <span class="math inline">\( ( \mathbb{B}, \vee ) \)</span> are both valid semigroups.</p>
<p>Restriction for amount of instances means that we need to wrap our data types when we need to create multiple instances. A wrapper per each instance. That leads to an awful runtime cost - wrapping and unwrapping are not free. That’s why we use <code>newtype</code> to create wrappers. In compile time the <code>newtype</code> wrapper is not equal to the type that is being wrapped, so we can use different instances. But since the types are isomorphic, all the wrapping and unwrapping can be removed by compiler, so we don’t have any runtime costs anymore.</p>
<p>When it comes to booleans with conjunction (<code>&amp;&amp;</code>) or disjunction (<code>||</code>), we don’t need to define our own wrappers since <code>Data.Monoid</code> already provides them - <code>All</code> and <code>Any</code>.</p>
<div class="sourceCode" id="cb18"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb18-1"><a href="#cb18-1"></a><span class="op">&gt;</span> getAll (<span class="dt">All</span> <span class="dt">True</span> <span class="op">&lt;&gt;</span> <span class="dt">All</span> <span class="dt">False</span>)</span>
<span id="cb18-2"><a href="#cb18-2"></a><span class="dt">False</span></span>
<span id="cb18-3"><a href="#cb18-3"></a></span>
<span id="cb18-4"><a href="#cb18-4"></a><span class="op">&gt;</span> getAny (<span class="dt">Any</span> <span class="dt">True</span> <span class="op">&lt;&gt;</span> <span class="dt">Any</span> <span class="dt">False</span>)</span>
<span id="cb18-5"><a href="#cb18-5"></a><span class="dt">True</span></span></code></pre></div>
<p>We can fetch it all together and get new definition of <code>.&amp;&amp;</code> and <code>.||</code>.</p>
<div class="sourceCode" id="cb19"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb19-1"><a href="#cb19-1"></a><span class="kw">infixr</span> <span class="dv">3</span> <span class="op">.&amp;&amp;</span></span>
<span id="cb19-2"><a href="#cb19-2"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb19-3"><a href="#cb19-3"></a>p1 <span class="op">.&amp;&amp;</span> p2 <span class="ot">=</span> getAll <span class="op">.</span> (<span class="dt">All</span> <span class="op">.</span> p1 <span class="op">&lt;&gt;</span> <span class="dt">All</span> <span class="op">.</span> p2)</span>
<span id="cb19-4"><a href="#cb19-4"></a></span>
<span id="cb19-5"><a href="#cb19-5"></a><span class="kw">infixr</span> <span class="dv">2</span> <span class="op">.||</span></span>
<span id="cb19-6"><a href="#cb19-6"></a><span class="ot">(.||) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb19-7"><a href="#cb19-7"></a>p1 <span class="op">.||</span> p2 <span class="ot">=</span> getAny <span class="op">.</span> (<span class="dt">Any</span> <span class="op">.</span> p1 <span class="op">&lt;&gt;</span> <span class="dt">Any</span> <span class="op">.</span> p2)</span></code></pre></div>
<p>I’ve heard multiple times that <code>newtype</code> is erased during compilation and by inspecting <a href="https://github.com/d12frosted/d12frosted.io/blob/master/assets/snippets/predicate-composition/operator-newtype.dump-simpl">the dumped Core</a> we can confirm that this version is not different from the previous one.</p>
<p>However we didn’t improve the code. I’d say that we degraded. While we abstracted away function application, we have strengthened the link between the definition shape and the binary operation, which now appears three times on two different levels. Not good, definitely not good.</p>
<h1 id="coercion">Coercion</h1>
<p>What comes to the rescue is coercion. Starting with GHC 7.8 there is a new type class allowing conversion between any two types that are representationally equal.</p>
<div class="sourceCode" id="cb20"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb20-1"><a href="#cb20-1"></a><span class="co">-- Data.Coerce</span></span>
<span id="cb20-2"><a href="#cb20-2"></a><span class="kw">class</span> <span class="dt">Coercible</span> a b <span class="kw">where</span></span>
<span id="cb20-3"><a href="#cb20-3"></a><span class="ot">  coerce ::</span> a <span class="ot">-&gt;</span> b</span></code></pre></div>
<p>But what does it mean to be representationally equal? And are there any other types<a href="#fn2" class="footnote-ref" id="fnref2" role="doc-noteref"><sup>2</sup></a> of type equality? It turns out that there are two of them and they were <a href="https://gitlab.haskell.org/ghc/ghc/wikis/roles">introduced</a> as a solution for a long existing hole in a type system.</p>
<p>Nominal equality means that types are <em>really</em> equal. If two types have the same name (expanding synonyms) they are nominally equal. If they don’t have the same name, well, then they are not nominally equal.</p>
<p>But what about <code>newtype</code> wrappers like <code>All</code> and <code>Any</code>? We know that they are isomorphic to <code>Bool</code> (and mutually as well). Are they equal? Here comes the second kind of type equality – representational. They all share the same representation. While <code>All</code> and <code>Bool</code> are representationally equal, they are not equal nominally!</p>
<p>So all that means that we can use <code>coerce</code> to convert from <code>All</code> to <code>Bool</code> and back. Let’s try it.</p>
<div class="sourceCode" id="cb21"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb21-1"><a href="#cb21-1"></a><span class="op">&gt;</span> <span class="op">:</span>m <span class="op">+</span><span class="dt">Data.Coerce</span></span>
<span id="cb21-2"><a href="#cb21-2"></a><span class="op">&gt;</span> <span class="op">:</span>m <span class="op">+</span><span class="dt">Data.Monoid</span></span>
<span id="cb21-3"><a href="#cb21-3"></a></span>
<span id="cb21-4"><a href="#cb21-4"></a><span class="op">&gt;</span> <span class="op">:</span>t coerce</span>
<span id="cb21-5"><a href="#cb21-5"></a><span class="ot">coerce ::</span> <span class="dt">Coercible</span> a b <span class="ot">=&gt;</span> a <span class="ot">-&gt;</span> b</span>
<span id="cb21-6"><a href="#cb21-6"></a></span>
<span id="cb21-7"><a href="#cb21-7"></a><span class="op">&gt;</span> <span class="op">:</span>t getAll <span class="op">.</span> coerce</span>
<span id="cb21-8"><a href="#cb21-8"></a>getAll <span class="op">.</span><span class="ot"> coerce ::</span> <span class="dt">Coercible</span> a <span class="dt">Bool</span> <span class="ot">=&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span></code></pre></div>
<p>Wow, this is kind of tricky. The <code>getAll . coerce</code> expression literally says – ‘give me something representationally equal to <code>Bool</code> and I will get to back the <code>Bool</code>’. It will do all the conversion for us.</p>
<p>When we look at the previous implementation of <code>.&amp;&amp;</code> we might notice that we actually <del>convert</del> coerce <code>Bool</code> to <code>All</code> and then get back the <code>Bool</code> value.</p>
<div class="sourceCode" id="cb22"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb22-1"><a href="#cb22-1"></a><span class="kw">infixr</span> <span class="dv">3</span> <span class="op">.&amp;&amp;</span></span>
<span id="cb22-2"><a href="#cb22-2"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb22-3"><a href="#cb22-3"></a>p1 <span class="op">.&amp;&amp;</span> p2 <span class="ot">=</span> getAll <span class="op">.</span> (<span class="dt">All</span> <span class="op">.</span> p1 <span class="op">&lt;&gt;</span> <span class="dt">All</span> <span class="op">.</span> p2)</span></code></pre></div>
<p>Maybe we can replace <code>All</code> with <code>coerce</code>?</p>
<div class="sourceCode" id="cb23"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb23-1"><a href="#cb23-1"></a><span class="kw">infixr</span> <span class="dv">3</span> <span class="op">.&amp;&amp;</span></span>
<span id="cb23-2"><a href="#cb23-2"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb23-3"><a href="#cb23-3"></a>p1 <span class="op">.&amp;&amp;</span> p2 <span class="ot">=</span> getAll <span class="op">.</span> (coerce <span class="op">.</span> p1 <span class="op">&lt;&gt;</span> coerce <span class="op">.</span> p2)</span></code></pre></div>
<p>And it works. We can repeat the trick with <code>.||</code>, but at this point we can move this patter to a helper operator <code>&lt;~&gt;</code>.</p>
<div class="sourceCode" id="cb24"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb24-1"><a href="#cb24-1"></a>f <span class="op">&lt;~&gt;</span> g <span class="ot">=</span> coerce <span class="op">.</span> f  <span class="op">&lt;&gt;</span> coerce <span class="op">.</span> g</span>
<span id="cb24-2"><a href="#cb24-2"></a><span class="co">-- or in other words</span></span>
<span id="cb24-3"><a href="#cb24-3"></a>f <span class="op">&lt;~&gt;</span> g <span class="ot">=</span> \a <span class="ot">-&gt;</span> coerce (f a) <span class="op">&lt;&gt;</span> coerce (g a)</span>
<span id="cb24-4"><a href="#cb24-4"></a></span>
<span id="cb24-5"><a href="#cb24-5"></a><span class="kw">infixr</span> <span class="dv">3</span> <span class="op">.&amp;&amp;</span></span>
<span id="cb24-6"><a href="#cb24-6"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb24-7"><a href="#cb24-7"></a>p1 <span class="op">.&amp;&amp;</span> p2 <span class="ot">=</span> getAll <span class="op">.</span> (p1 <span class="op">&lt;~&gt;</span> p2)</span></code></pre></div>
<p>I specially omitted the type signature of <code>&lt;~&gt;</code>. It’s not our job to infer the types, but let’s steal some bread from GHC’s table.</p>
<p>We know that the type of <code>f</code> should be <code>a -&gt; b</code>. Previously we put a constraint on <code>b</code> to form <code>Semigroup</code>. But now we <code>coerce</code> it some type and only then use <code>&lt;&gt;</code>. The result of <code>coerce (f a)</code> must form <code>Semigroup</code>. Which means that if <code>f</code> has type <code>a -&gt; b</code> then we need be able to covert <code>b</code> to some type <code>c</code> which is semigroup.</p>
<div class="sourceCode" id="cb25"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb25-1"><a href="#cb25-1"></a><span class="ot">(&lt;~&gt;) ::</span> (<span class="dt">Coercible</span> b c, <span class="dt">Monoid</span> c) <span class="ot">=&gt;</span> (a <span class="ot">-&gt;</span> b) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> b) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> c</span>
<span id="cb25-2"><a href="#cb25-2"></a>f <span class="op">&lt;~&gt;</span> g <span class="ot">=</span> coerce <span class="op">.</span> f <span class="op">&lt;&gt;</span> coerce <span class="op">.</span> g</span></code></pre></div>
<p>And you know what? It works! But if you think about the <code>g</code>, then you realise that <code>f a</code> and <code>g a</code> are independent, the only requirement is to be able to coerce them to the same type <code>c</code> that forms <code>Semigroup</code>.</p>
<div class="sourceCode" id="cb26"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb26-1"><a href="#cb26-1"></a><span class="kw">module</span> <span class="dt">Main</span> (main) <span class="kw">where</span></span>
<span id="cb26-2"><a href="#cb26-2"></a></span>
<span id="cb26-3"><a href="#cb26-3"></a><span class="kw">import</span>           <span class="dt">Data.Coerce</span></span>
<span id="cb26-4"><a href="#cb26-4"></a><span class="kw">import</span>           <span class="dt">Data.Monoid</span></span>
<span id="cb26-5"><a href="#cb26-5"></a></span>
<span id="cb26-6"><a href="#cb26-6"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb26-7"><a href="#cb26-7"></a>main <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb26-8"><a href="#cb26-8"></a>  input <span class="ot">&lt;-</span> <span class="fu">read</span> <span class="op">&lt;$&gt;</span> <span class="fu">getLine</span></span>
<span id="cb26-9"><a href="#cb26-9"></a>  <span class="kw">let</span> result <span class="ot">=</span> (<span class="op">&lt;</span> <span class="dv">10</span>) <span class="op">.&amp;&amp;</span> ((<span class="op">&gt;</span> <span class="dv">0</span>) <span class="op">.||</span> <span class="fu">even</span>) <span class="op">$</span> input</span>
<span id="cb26-10"><a href="#cb26-10"></a>  <span class="fu">print</span> result</span>
<span id="cb26-11"><a href="#cb26-11"></a></span>
<span id="cb26-12"><a href="#cb26-12"></a><span class="ot">(&lt;~&gt;) ::</span> ( <span class="dt">Coercible</span> b1 c</span>
<span id="cb26-13"><a href="#cb26-13"></a>         , <span class="dt">Coercible</span> b2 c</span>
<span id="cb26-14"><a href="#cb26-14"></a>         , <span class="dt">Monoid</span> c</span>
<span id="cb26-15"><a href="#cb26-15"></a>         )</span>
<span id="cb26-16"><a href="#cb26-16"></a>      <span class="ot">=&gt;</span> (a <span class="ot">-&gt;</span> b1) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> b2) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> c</span>
<span id="cb26-17"><a href="#cb26-17"></a>f <span class="op">&lt;~&gt;</span> g <span class="ot">=</span> coerce <span class="op">.</span> f <span class="op">&lt;&gt;</span> coerce <span class="op">.</span> g</span>
<span id="cb26-18"><a href="#cb26-18"></a></span>
<span id="cb26-19"><a href="#cb26-19"></a><span class="kw">infixr</span> <span class="dv">3</span> <span class="op">.&amp;&amp;</span></span>
<span id="cb26-20"><a href="#cb26-20"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb26-21"><a href="#cb26-21"></a>p1 <span class="op">.&amp;&amp;</span> p2 <span class="ot">=</span> getAll <span class="op">.</span> (p1 <span class="op">&lt;~&gt;</span> p2)</span>
<span id="cb26-22"><a href="#cb26-22"></a></span>
<span id="cb26-23"><a href="#cb26-23"></a><span class="kw">infixr</span> <span class="dv">2</span> <span class="op">.||</span></span>
<span id="cb26-24"><a href="#cb26-24"></a><span class="ot">(.||) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb26-25"><a href="#cb26-25"></a>p1 <span class="op">.||</span> p2 <span class="ot">=</span> getAny <span class="op">.</span> (p1 <span class="op">&lt;~&gt;</span> p2)</span></code></pre></div>
<p>This works, this composes. You can also use it with other semigroups like <code>Sum</code> and <code>Product</code>. But it might look a little bit weird.</p>
<div class="sourceCode" id="cb27"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb27-1"><a href="#cb27-1"></a><span class="op">&gt;</span> getSum <span class="op">.</span> ((<span class="op">*</span><span class="dv">2</span>) <span class="op">&lt;~&gt;</span> (<span class="op">+</span><span class="dv">100</span>)) <span class="op">$</span> <span class="dv">15</span></span>
<span id="cb27-2"><a href="#cb27-2"></a><span class="dv">145</span></span></code></pre></div>
<p>So instead, let’s look at <a href="https://github.com/d12frosted/d12frosted.io/blob/master/assets/snippets/predicate-composition/operator-coerce.dump-simpl">the Core dump</a>.</p>
<div class="sourceCode" id="cb28"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb28-1"><a href="#cb28-1"></a><span class="op">...</span></span>
<span id="cb28-2"><a href="#cb28-2"></a></span>
<span id="cb28-3"><a href="#cb28-3"></a><span class="kw">case</span> ds2_a6m8 <span class="kw">of</span> {</span>
<span id="cb28-4"><a href="#cb28-4"></a>  [] <span class="ot">-&gt;</span></span>
<span id="cb28-5"><a href="#cb28-5"></a>    <span class="kw">case</span> integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.ltInteger<span class="op">#</span></span>
<span id="cb28-6"><a href="#cb28-6"></a>           x1_a6m7 Main.main3</span>
<span id="cb28-7"><a href="#cb28-7"></a>    <span class="kw">of</span> {</span>
<span id="cb28-8"><a href="#cb28-8"></a>      __DEFAULT <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool4;</span>
<span id="cb28-9"><a href="#cb28-9"></a>      <span class="dv">1</span><span class="op">#</span> <span class="ot">-&gt;</span></span>
<span id="cb28-10"><a href="#cb28-10"></a>        <span class="kw">case</span> integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.gtInteger<span class="op">#</span></span>
<span id="cb28-11"><a href="#cb28-11"></a>               x1_a6m7 <span class="op">Main.$</span>seven1</span>
<span id="cb28-12"><a href="#cb28-12"></a>        <span class="kw">of</span> {</span>
<span id="cb28-13"><a href="#cb28-13"></a>          __DEFAULT <span class="ot">-&gt;</span></span>
<span id="cb28-14"><a href="#cb28-14"></a>            <span class="kw">case</span> integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.eqInteger<span class="op">#</span></span>
<span id="cb28-15"><a href="#cb28-15"></a>                   (integer<span class="op">-</span>gmp<span class="op">-</span><span class="fl">1.0</span><span class="op">.</span><span class="fl">2.0</span><span class="op">:</span>GHC.Integer.Type.remInteger</span>
<span id="cb28-16"><a href="#cb28-16"></a>                      x1_a6m7 <span class="op">Main.$</span>seven2)</span>
<span id="cb28-17"><a href="#cb28-17"></a>                   <span class="op">Main.$</span>seven1</span>
<span id="cb28-18"><a href="#cb28-18"></a>            <span class="kw">of</span> {</span>
<span id="cb28-19"><a href="#cb28-19"></a>              __DEFAULT <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool4;</span>
<span id="cb28-20"><a href="#cb28-20"></a>              <span class="dv">1</span><span class="op">#</span> <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool2</span>
<span id="cb28-21"><a href="#cb28-21"></a>            };</span>
<span id="cb28-22"><a href="#cb28-22"></a>          <span class="dv">1</span><span class="op">#</span> <span class="ot">-&gt;</span> <span class="op">GHC.Show.$</span>fShowBool2</span>
<span id="cb28-23"><a href="#cb28-23"></a>        }</span>
<span id="cb28-24"><a href="#cb28-24"></a>    };</span>
<span id="cb28-25"><a href="#cb28-25"></a></span>
<span id="cb28-26"><a href="#cb28-26"></a><span class="op">...</span></span></code></pre></div>
<p>The important bits are the same.</p>
<h1 id="criterion">Criterion</h1>
<p>I bet that at this point it’s obvious, but they perform similarly – the naive implementation and the most abstract one with coercion and <code>newtype</code> wrappers. We know this because we inspected the dumped Core, but we can also refer to criterion to inspect the runtime performance.</p>
<pre class="example"><code>benchmarking single/naive
time                 3.027 ns   (3.011 ns .. 3.043 ns)
                     1.000 R²   (1.000 R² .. 1.000 R²)
mean                 3.017 ns   (3.009 ns .. 3.029 ns)
std dev              31.73 ps   (22.17 ps .. 48.50 ps)
variance introduced by outliers: 12% (moderately inflated)

benchmarking single/coerce
time                 3.017 ns   (3.009 ns .. 3.025 ns)
                     1.000 R²   (1.000 R² .. 1.000 R²)
mean                 3.026 ns   (3.015 ns .. 3.055 ns)
std dev              56.91 ps   (26.62 ps .. 114.5 ps)
variance introduced by outliers: 30% (moderately inflated)
</code></pre>
<div class="criterion-chart"><canvas id="predicate-composition/single-5399847985197354366" height="120"></canvas><script>
      new Chart(document.getElementById("predicate-composition/single-5399847985197354366"), {
        type: "horizontalBar",
        data: {
          labels: ["naive","coerce"],
          datasets: [{"data":[3.0463795600278686e-9,3.053795658440273e-9],"backgroundColor":"#36a2eb","fill":false,"label":"single","borderColor":"#36a2eb"}]
        },
        options: {
          maintainAspectRatio: false,
          title: {
            display: false,
            text: ""
          },
          legend: {
            display: false
          },
          tooltips: {
            callbacks: {
              label: function(item, data) {
                var vtls = {"single/coerce":"3.053796 ns","single/naive":"3.046380 ns"};
                var name = data.datasets[item.datasetIndex].label + "/" + item.label;
                var label = data.datasets[item.datasetIndex].label || '';
                if (label) label += ": ";
                label += vtls[name];
                return label;
              }
            }
          },
          scales: {"xAxes":[{"type":"linear","ticks":{"beginAtZero":true}}],"yAxes":[]}
        }
      });
      </script></div>
<h1 id="final-words">Final words</h1>
<p>I love that in Haskell one can use <em>some</em> of the abstractions without hurting the runtime. After all, as developers we want to simplify our <em>development</em> life with minimal negative influence on the application.</p>
<p>Today we implemented two simple operators for predicate composition using semigroups and coercion. And we saw that they don’t introduce runtime penalty. Techniques that made it possible are usable in other scenarios.</p>
<div class="sourceCode" id="cb30"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb30-1"><a href="#cb30-1"></a><span class="kw">module</span> <span class="dt">Data.Monoid.Extra</span></span>
<span id="cb30-2"><a href="#cb30-2"></a>  ( (<span class="op">.&amp;&amp;</span>)</span>
<span id="cb30-3"><a href="#cb30-3"></a>  , (<span class="op">.||</span>)</span>
<span id="cb30-4"><a href="#cb30-4"></a>  ) <span class="kw">where</span></span>
<span id="cb30-5"><a href="#cb30-5"></a></span>
<span id="cb30-6"><a href="#cb30-6"></a><span class="kw">import</span>           <span class="dt">Data.Coerce</span></span>
<span id="cb30-7"><a href="#cb30-7"></a><span class="kw">import</span>           <span class="dt">Data.Monoid</span></span>
<span id="cb30-8"><a href="#cb30-8"></a></span>
<span id="cb30-9"><a href="#cb30-9"></a><span class="kw">infixr</span> <span class="dv">3</span> <span class="op">.&amp;&amp;</span></span>
<span id="cb30-10"><a href="#cb30-10"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb30-11"><a href="#cb30-11"></a>p1 <span class="op">.&amp;&amp;</span> p2 <span class="ot">=</span> getAll <span class="op">.</span> (p1 <span class="op">&lt;~&gt;</span> p2)</span>
<span id="cb30-12"><a href="#cb30-12"></a></span>
<span id="cb30-13"><a href="#cb30-13"></a><span class="kw">infixr</span> <span class="dv">2</span> <span class="op">.||</span></span>
<span id="cb30-14"><a href="#cb30-14"></a><span class="ot">(.||) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb30-15"><a href="#cb30-15"></a>p1 <span class="op">.||</span> p2 <span class="ot">=</span> getAny <span class="op">.</span> (p1 <span class="op">&lt;~&gt;</span> p2)</span>
<span id="cb30-16"><a href="#cb30-16"></a></span>
<span id="cb30-17"><a href="#cb30-17"></a><span class="ot">(&lt;~&gt;) ::</span> ( <span class="dt">Coercible</span> b1 c</span>
<span id="cb30-18"><a href="#cb30-18"></a>         , <span class="dt">Coercible</span> b2 c</span>
<span id="cb30-19"><a href="#cb30-19"></a>         , <span class="dt">Monoid</span> c</span>
<span id="cb30-20"><a href="#cb30-20"></a>         )</span>
<span id="cb30-21"><a href="#cb30-21"></a>      <span class="ot">=&gt;</span> (a <span class="ot">-&gt;</span> b1) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> b2) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> c</span>
<span id="cb30-22"><a href="#cb30-22"></a>f <span class="op">&lt;~&gt;</span> g <span class="ot">=</span> coerce <span class="op">.</span> f <span class="op">&lt;&gt;</span> coerce <span class="op">.</span> g</span></code></pre></div>
<h1 id="evolution">Evolution</h1>
<p>I love the <a href="https://willamette.edu/~fruehr/haskell/evolution.html">The Evolution of a Haskell Programmer</a> by Fritz Ruehr. And to keep the traction of this evolution path, we should step back and reflect on atrocious results. We all love functions, don’t we? And functions are known functors and applicatives. So instead of going this lengthy path, we could just do something dead simple.</p>
<div class="sourceCode" id="cb31"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb31-1"><a href="#cb31-1"></a><span class="kw">infixr</span> <span class="dv">3</span> <span class="op">.&amp;&amp;</span></span>
<span id="cb31-2"><a href="#cb31-2"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb31-3"><a href="#cb31-3"></a>p1 <span class="op">.&amp;&amp;</span> p2 <span class="ot">=</span> (<span class="op">&amp;&amp;</span>) <span class="op">&lt;$&gt;</span> p1 <span class="op">&lt;*&gt;</span> p2</span>
<span id="cb31-4"><a href="#cb31-4"></a></span>
<span id="cb31-5"><a href="#cb31-5"></a><span class="kw">infixr</span> <span class="dv">2</span> <span class="op">.||</span></span>
<span id="cb31-6"><a href="#cb31-6"></a><span class="ot">(.||) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb31-7"><a href="#cb31-7"></a>p1 <span class="op">.||</span> p2 <span class="ot">=</span> (<span class="op">||</span>) <span class="op">&lt;$&gt;</span> p1 <span class="op">&lt;*&gt;</span> p2</span></code></pre></div>
<p>Or using <code>liftA2</code>:</p>
<div class="sourceCode" id="cb32"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb32-1"><a href="#cb32-1"></a><span class="kw">infixr</span> <span class="dv">3</span> <span class="op">.&amp;&amp;</span></span>
<span id="cb32-2"><a href="#cb32-2"></a><span class="ot">(.&amp;&amp;) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb32-3"><a href="#cb32-3"></a>(<span class="op">.&amp;&amp;</span>) p2 <span class="ot">=</span> liftA2 (<span class="op">&amp;&amp;</span>)</span>
<span id="cb32-4"><a href="#cb32-4"></a></span>
<span id="cb32-5"><a href="#cb32-5"></a><span class="kw">infixr</span> <span class="dv">2</span> <span class="op">.||</span></span>
<span id="cb32-6"><a href="#cb32-6"></a><span class="ot">(.||) ::</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> (a <span class="ot">-&gt;</span> <span class="dt">Bool</span>) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb32-7"><a href="#cb32-7"></a>(<span class="op">.||</span>) <span class="ot">=</span> liftA2 (<span class="op">||</span>)</span></code></pre></div>
<p>Stay safe!</p>
<h1 id="references">References</h1>
<ul>
<li><a href="https://www.aosabook.org/en/ghc.html">The Glasgow Haskell Compiler</a> by <a href="https://www.aosabook.org/en/intro2.html#marlow-simon">Simon Marlow</a> and <a href="https://www.aosabook.org/en/intro2.html#peyton-jones-simon">Simon Peyton-Jones</a>.</li>
<li><a href="http://book.realworldhaskell.org/read/profiling-and-optimization.html">Real World Haskell</a> Chapter 25. Profiling and optimization by Bryan O’Sullivan, Don Stewart, and John Goerzen.</li>
<li><a href="https://downloads.haskell.org/ghc/latest/docs/html/users_guide/debugging.html#id2">Glasgow Haskell Compiler User’s Guide</a> Debugging the compiler.</li>
<li><a href="https://gitlab.haskell.org/ghc/ghc/wikis/roles">Roles</a> on GHC Wiki.</li>
</ul>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>Aaah, the tautology…<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2" role="doc-endnote"><p>Kinds?<a href="#fnref2" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2020-01-20-predicate-composition.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>emacs-plus: current state</title>
    <link href="https://d12frosted.io/posts/2020-01-08-emacs-plus-current-state.html" />
    <id>https://d12frosted.io/posts/2020-01-08-emacs-plus-current-state.html</id>
    <published>2020-01-08T00:00:00Z</published>
    <updated>2020-01-08T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>emacs-plus: current state</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Overview of current state of emacs-plus project" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Overview of current state of emacs-plus project">
    <meta property="og:description" content="Overview of current state of emacs-plus project">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">emacs-plus: current state</h1>
    <div class="date"><span>January  8, 2020</span></div>
    <div class="tags"><span>(<a href="../tags/emacs.html">emacs</a>, <a href="../tags/emacs-plus.html">emacs-plus</a>)</span></div>
    <article>
      <section>
        <p>Not everyone knows, but emacs-plus is almost an April Fool’s joke that was late for 1 day. Its <a href="https://github.com/d12frosted/homebrew-emacs-plus/commit/6c85ae34d2649512a3088207b5f2a81c23cd8630">initial release</a> was on 2nd April 2016 and the only feature it had compared to the official Emacs formula back in the day was Spacemacs icon (installed by default). Homebrew maintainers didn’t want to increase the complexity of the Emacs formula, so I’ve put that burden on myself, created a tap with one additional icon and after several years info command for <code>emacs-plus</code> prints a wall of options.</p>
<p>I am very happy to see so many users of this feature-inclusive (and sometimes experimental) formula. I am grateful to all of you. I would’ve abandoned this project a year ago if it would not you! And I think it’s fair to (in)formally share the current state of the project and shed some light on my plans.</p>
<!--more-->

<h2 id="the-problem">The problem</h2>
<p>During the last year (or maybe even more), <code>emacs-plus</code> overgrown with issues. Here is my top list.</p>
<ol>
<li>Experimental <code>xwidgets</code> build was breaking time to time and only thanks to users (mostly <a href="https://github.com/agzam">Ag Ibragimov</a>) I would acknowledge it.</li>
<li>While I state that <code>HEAD</code> builds are not supported, I still want this option to work. First of all, for Emacs users to easily get the latest features. And secondly, for Emacs maintainers to get feedback from the users.</li>
<li>Patches are located outside of the project and their destiny is unclear.</li>
<li>It’s hard to contribute a new patch or a patch fix inside the <code>emacs-plus</code> repository, because you have to mess with the URLs and branch names.</li>
<li>Travis builds are very slow. The default build takes 7 minutes, from sources - 13 minutes, build HEAD was impossible. I’d like to test much more build options and their combination.</li>
<li>Too many options are given to the users, which kind of contradicts my initial idea.</li>
</ol>
<p>When it comes to build failures, all of them can be divided into two categories - patch application failure or upstream issue.</p>
<h2 id="visibility-and-stability">Visibility and stability</h2>
<p>In order to address the named issues, I’ve performed the following actions.</p>
<p>I moved from Travis to GitHub Workflows, which gave such a decent boost in compiling time. It also enabled me to add the <code>HEAD</code> and <code>xwidgets</code> options to the build matrix. I would love to add <code>x11</code> as well, but I didn’t figure out how to setup XQuartz on build machines. I haven’t really measured the build time, but here are some examples that I fetched from the build history. Also I’ve heard that Travis improved their machines so more calculating power is given to the users now.</p>
<table>
<thead>
<tr class="header">
<th>Options</th>
<th>Travis</th>
<th>GitHub</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td></td>
<td>6m 30s</td>
<td>3m 14s</td>
</tr>
<tr class="even">
<td><code>--with-no-titlebar</code></td>
<td>11m 16s</td>
<td>5m 18s</td>
</tr>
<tr class="odd">
<td><code>--HEAD</code></td>
<td>-</td>
<td>11m 18s</td>
</tr>
<tr class="even">
<td><code>--HEAD --with-xwidgets</code></td>
<td>-</td>
<td>11m 15s</td>
</tr>
</tbody>
</table>
<p>This gives me better visibility on the build breakages (both when patch application fails or it’s because of the upstream issue).</p>
<p>All of the used patches were moved to the <code>emacs-plus</code> repository, so they are always with formula. And I added support for running builds on CI with patches from PR.</p>
<p>And in order to better maintain the patches I’ve created <a href="https://github.com/d12frosted/emacs-plus-basis">emacs-plus-basis</a> repository, which is basically a fork of Emacs repository with patches applied on top. Everyday CI fetches the latest updates from the upstream and applies the patches on top. And if it fails, I get a notification. So it gives me visibility of patch application failures. And it gives me environment to fix named patches.</p>
<p>While all these steps should improve stability of the <code>emacs-plus</code>, I am aware that build breakages are impossible to avoid (upstream changes very fast). What I am achieving here is better visibility and tools to address breakages more quickly.</p>
<h2 id="build-options-zoo">Build options zoo</h2>
<p>What was not covered yet is the amount of build options. The initial idea behind <code>emacs-plus</code> is that users should not pass many custom options to <code>install</code> command. If you remember, back then everyone recommended to install Emacs from brew using the following command:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb1-1"><a href="#cb1-1"></a>$ <span class="ex">brew</span> install emacs --with-cocoa --with-gnutls --with-rsvg --with-imagemagick</span></code></pre></div>
<p>All of these options where recommended to most of the users. So I decided to turn them on by default.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb2-1"><a href="#cb2-1"></a>$ <span class="ex">brew</span> install emacs-plus</span></code></pre></div>
<p>In my ideal world, the only options users should pass is the icon choice and experimental or controversial stuff.</p>
<p>I still don’t have a solution, but I am willing to decrease amount of available options. For example, I think that no one actually installs without cocoa and without x11. So the choice must be between these two options. GnuTLS? Well, why would anyone want to install Emacs without it? <code>librsvg</code> and <code>libxml2</code>? Does it hurt if installed with it? I want <code>emacs-plus</code> be as simple as possible, while still providing things that are not available from the official Emacs formula.</p>
<p>Unfortunately, I don’t have anonymous statistics about options usage. So I still don’t have any decision in this field. Will keep you posted :)</p>
<h2 id="cask">Cask</h2>
<p>This is something people are waiting for. I understand why, but I don’t have time to invest into this project. At least now. If someone wants to step in - I would be super glad and grateful!</p>
<h2 id="final-words">Final words</h2>
<p>That being said, all contributions are welcome! Let’s keep in touch ;)</p>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2020-01-08-emacs-plus-current-state.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>env-extra v1.0.0.0</title>
    <link href="https://d12frosted.io/posts/2020-01-06-env-extra-v1.0.0.0.html" />
    <id>https://d12frosted.io/posts/2020-01-06-env-extra-v1.0.0.0.html</id>
    <published>2020-01-06T00:00:00Z</published>
    <updated>2020-01-06T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>env-extra v1.0.0.0</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Announcement of new version of env-extra" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Announcement of new version of env-extra">
    <meta property="og:description" content="Announcement of new version of env-extra">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">env-extra v1.0.0.0</h1>
    <div class="date"><span>January  6, 2020</span></div>
    <div class="tags"><span>(<a href="../tags/haskell.html">haskell</a>, <a href="../tags/release.html">release</a>, <a href="../tags/env-extra.html">env-extra</a>)</span></div>
    <article>
      <section>
        <p>It’s hard (though possible) to imagine a script that doesn’t access environment variables. In Haskell ecosystem there is a good built-in module <a href="https://hackage.haskell.org/package/base-4.12.0.0/docs/System-Environment.html">System.Environment</a> that does the job, but it has several drawbacks. First of all, it uses <code>String</code> data type as input and as an output. And secondly, it lives in <code>IO</code>. Both of these drawbacks are not that critical, but calling all these <code>Text.pack</code>, <code>Text.unpack</code> and <code>liftIO</code> in every program drives me nuts. So several years ago (somewhere in 2016) I decided to write a <a href="https://github.com/d12frosted/env-extra">simple library</a> that wraps it for me.</p>
<p>I’ve been using this library for a long time and today I uploaded it to <a href="https://hackage.haskell.org/package/env-extra">Hackage</a>. While it’s really small, I still think that some of you might find it useful, because it has nice features that original <code>System.Environment</code> doesn’t have. Let’s take a look!</p>
<!--more-->

<p>So in the nutshell, there are three functions to access environment variables:</p>
<ul>
<li><code>envMaybe</code> of type <code>( MonadIO m, IsString a ) =&gt; Text -&gt; m (Maybe a)</code> - a wrapper for <code>lookupEnv</code>. Aside from working with <code>Text</code> input it also lives in <code>MonadIO</code> and returns an <code>IsString</code>, which the most interesting part here. I will talk about it shortly.</li>
<li><code>getEnv</code> of type <code>( MonadThrow m, MonadIO m, IsString a ) =&gt; Text -&gt; m a</code> - the unsafe version of <code>envMaybe</code>.</li>
<li><code>envRead</code> of type <code>( MonadIO m ) =&gt; Reader a -&gt; Text -&gt; m (Maybe a)</code> - a helper function that also reads (or parses) a value from environment variable by specifying the <a href="https://hackage.haskell.org/package/text-1.2.4.0/docs/Data-Text-Read.html">Reader</a>.</li>
</ul>
<p>In most cases my choice is between <code>envMaybe</code> and <code>getEnv</code> depending on my desire to handle missing variable.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1"></a><span class="op">&gt;</span> envMaybe <span class="st">&quot;NAME&quot;</span></span>
<span id="cb1-2"><a href="#cb1-2"></a><span class="dt">Nothing</span></span>
<span id="cb1-3"><a href="#cb1-3"></a></span>
<span id="cb1-4"><a href="#cb1-4"></a><span class="op">&gt;</span> setEnv <span class="st">&quot;NAME&quot;</span> <span class="st">&quot;Boris&quot;</span></span>
<span id="cb1-5"><a href="#cb1-5"></a></span>
<span id="cb1-6"><a href="#cb1-6"></a><span class="op">&gt;</span> envMaybe <span class="st">&quot;NAME&quot;</span></span>
<span id="cb1-7"><a href="#cb1-7"></a><span class="dt">Just</span> <span class="st">&quot;Boris&quot;</span></span></code></pre></div>
<p>What’s interesting, <code>envMaybe</code> is polymorphic in it’s return type, which means that it plays nicely in composition chains and you don’t need to explicitly convert between <code>Text</code>, <code>String</code>, <code>ByteString</code> data types.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1"></a><span class="op">&gt;</span> <span class="op">:</span>t <span class="fu">putStrLn</span></span>
<span id="cb2-2"><a href="#cb2-2"></a><span class="fu">putStrLn</span><span class="ot"> ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> ()</span>
<span id="cb2-3"><a href="#cb2-3"></a></span>
<span id="cb2-4"><a href="#cb2-4"></a><span class="op">&gt;</span> getEnv <span class="st">&quot;NAME&quot;</span> <span class="op">&gt;&gt;=</span> <span class="fu">putStrLn</span></span>
<span id="cb2-5"><a href="#cb2-5"></a><span class="dt">Boris</span></span>
<span id="cb2-6"><a href="#cb2-6"></a></span>
<span id="cb2-7"><a href="#cb2-7"></a><span class="op">&gt;</span> setEnv <span class="st">&quot;SOME_VAR&quot;</span> <span class="st">&quot;NAME&quot;</span></span>
<span id="cb2-8"><a href="#cb2-8"></a></span>
<span id="cb2-9"><a href="#cb2-9"></a><span class="op">&gt;</span> getEnv <span class="st">&quot;SOME_VAR&quot;</span></span>
<span id="cb2-10"><a href="#cb2-10"></a><span class="st">&quot;NAME&quot;</span></span>
<span id="cb2-11"><a href="#cb2-11"></a></span>
<span id="cb2-12"><a href="#cb2-12"></a><span class="op">&gt;</span> getEnv <span class="st">&quot;SOME_VAR&quot;</span> <span class="op">&gt;&gt;=</span> getEnv</span>
<span id="cb2-13"><a href="#cb2-13"></a><span class="st">&quot;Boris&quot;</span></span></code></pre></div>
<p>The other function that I use (though not that often) is <code>envRead</code>.</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1"></a><span class="op">&gt;</span> setEnv <span class="st">&quot;AGE&quot;</span> <span class="st">&quot;10&quot;</span></span>
<span id="cb3-2"><a href="#cb3-2"></a></span>
<span id="cb3-3"><a href="#cb3-3"></a><span class="op">&gt;</span> envRead decimal <span class="st">&quot;AGE&quot;</span></span>
<span id="cb3-4"><a href="#cb3-4"></a><span class="dt">Just</span> <span class="dv">10</span></span>
<span id="cb3-5"><a href="#cb3-5"></a></span>
<span id="cb3-6"><a href="#cb3-6"></a><span class="op">&gt;</span> envRead hexadecimal <span class="st">&quot;AGE&quot;</span></span>
<span id="cb3-7"><a href="#cb3-7"></a><span class="dt">Just</span> <span class="dv">16</span></span>
<span id="cb3-8"><a href="#cb3-8"></a></span>
<span id="cb3-9"><a href="#cb3-9"></a><span class="op">&gt;</span> envRead hexadecimal <span class="st">&quot;HOME&quot;</span></span>
<span id="cb3-10"><a href="#cb3-10"></a><span class="dt">Nothing</span></span></code></pre></div>
<p>In some rare cases you might want to use the <code>Read</code> instance for parsing. Though it’s not advisable.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1"></a><span class="op">&gt;</span> <span class="kw">data</span> <span class="dt">Status</span> <span class="ot">=</span> <span class="dt">SomeStatus1</span> <span class="op">|</span> <span class="dt">SomeStatus2</span> <span class="kw">deriving</span> (<span class="dt">Show</span>, <span class="dt">Read</span>, <span class="dt">Eq</span>)</span>
<span id="cb4-2"><a href="#cb4-2"></a></span>
<span id="cb4-3"><a href="#cb4-3"></a><span class="op">&gt;</span> getEnv <span class="st">&quot;STATUS&quot;</span></span>
<span id="cb4-4"><a href="#cb4-4"></a><span class="st">&quot;SomeStatus2&quot;</span></span>
<span id="cb4-5"><a href="#cb4-5"></a></span>
<span id="cb4-6"><a href="#cb4-6"></a><span class="op">&gt;</span> envRead <span class="fu">read</span> <span class="st">&quot;STATUS&quot;</span><span class="ot"> ::</span> <span class="dt">IO</span> (<span class="dt">Maybe</span> <span class="dt">Status</span>)</span>
<span id="cb4-7"><a href="#cb4-7"></a><span class="dt">Just</span> <span class="dt">SomeStatus2</span></span></code></pre></div>
<p>I hope you’ll find this library helpful. Please do send me your comments, idea and thoughts! And any contribution is welcome!</p>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2020-01-06-env-extra-v1.0.0.0.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>flyspell-correct v0.6</title>
    <link href="https://d12frosted.io/posts/2019-12-29-flyspell-correct-v0.6.html" />
    <id>https://d12frosted.io/posts/2019-12-29-flyspell-correct-v0.6.html</id>
    <published>2019-12-29T00:00:00Z</published>
    <updated>2019-12-29T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>flyspell-correct v0.6</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Announcement of new version of flyspell-correct" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Announcement of new version of flyspell-correct">
    <meta property="og:description" content="Announcement of new version of flyspell-correct">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">flyspell-correct v0.6</h1>
    <div class="date"><span>December 29, 2019</span></div>
    <div class="tags"><span>(<a href="../tags/emacs.html">emacs</a>, <a href="../tags/release.html">release</a>, <a href="../tags/flyspell-correct.html">flyspell-correct</a>)</span></div>
    <article>
      <section>
        <p><code>flyspell-correct</code> is a package for distraction-free words correction with flyspell via selected interface. It’s available on <a href="https://github.com/d12frosted/flyspell-correct">GitHub</a> and <a href="http://melpa.org/#/flyspell-correct">MELPA</a>.</p>
<p>Recently, there was a major release of <code>flyspell-correct</code>. Version 0.6 introduces new interface (<code>avy-menu</code>), fixes point movements, warnings, autoloads and switch to the lexical binding. Read further for the complete change list.</p>
<!--more-->

<ul>
<li>Do not move point by correction functions.</li>
<li>New interface - <code>avy-menu</code>. Thanks to <span class="citation" data-cites="clemera">@clemera</span>.</li>
<li>Mark custom interfaces as autoload.</li>
<li>Add <code>use-package</code> examples.</li>
<li>Fix errors in transient mode. Thanks to <span class="citation" data-cites="Ergus">@Ergus</span></li>
<li>Fix many warnings.</li>
<li>Start using <code>lexical-binding</code>.</li>
</ul>
<p><a href="./2016-05-09-flyspell-correct-intro.html">Intro post</a> also was updated to better explain key points of <code>flyspell-correct</code>.</p>
<p>Hopefully, you’ll like the new version. If something doesn’t work for you, please let me know!</p>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2019-12-29-flyspell-correct-v0.6.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Emacs: reusing window for helpful buffers</title>
    <link href="https://d12frosted.io/posts/2019-06-26-emacs-helpful.html" />
    <id>https://d12frosted.io/posts/2019-06-26-emacs-helpful.html</id>
    <published>2019-06-26T00:00:00Z</published>
    <updated>2019-06-26T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Emacs: reusing window for helpful buffers</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Tweaking helpful to avoid all windows taken by helpful" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Tweaking helpful to avoid all windows taken by helpful">
    <meta property="og:description" content="Tweaking helpful to avoid all windows taken by helpful">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Emacs: reusing window for helpful buffers</h1>
    <div class="date"><span>June 26, 2019</span></div>
    <div class="tags"><span>(<a href="../tags/emacs.html">emacs</a>, <a href="../tags/environment.html">environment</a>)</span></div>
    <article>
      <section>
        <p>Ironically, I find the <a href="https://github.com/Wilfred/helpful">helpful</a> package quite helpful. It boosts Emacs <strong>help</strong> buffer with much more contextual information. If you haven’t tried it out yet, I advice you to do so.</p>
<p>However, by default, it doesn’t play nicely with windows.</p>
<p>Usually when I write some Elisp and I want to read the documentation of some function or variable, I hit <code>C-h f</code> or <code>C-h v</code> respectively and the help buffer is shown in the separate window. Which is convenient in my opinion, because I can see the code and the help.</p>
<p>Sometimes help contains links to other entries that I need to navigate. And when I hit <code>&lt;RET&gt;</code> window containing code shows another help buffer. Which might be good for some people, but I hate this behaviour, because usually I want to see the code that I am editing.</p>
<p>This is also annoying if you set the value of <code>helpful-max-buffers</code> to <code>1</code>. Help window and the window with code are swapped on every navigation.</p>
<p>The good thing, it’s configurable (as almost everything in Emacs land).</p>
<div class="sourceCode" id="cb1" data-org-language="emacs-lisp"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb1-1"><a href="#cb1-1"></a>(<span class="kw">setq</span> helpful-switch-buffer-function #'+helpful-switch-to-buffer)</span>
<span id="cb1-2"><a href="#cb1-2"></a></span>
<span id="cb1-3"><a href="#cb1-3"></a>(<span class="kw">defun</span><span class="fu"> +helpful-switch-to-buffer </span>(buffer-or-name)</span>
<span id="cb1-4"><a href="#cb1-4"></a>  <span class="st">&quot;Switch to helpful BUFFER-OR-NAME.</span></span>
<span id="cb1-5"><a href="#cb1-5"></a></span>
<span id="cb1-6"><a href="#cb1-6"></a><span class="st">The logic is simple, if we are currently in the helpful buffer,</span></span>
<span id="cb1-7"><a href="#cb1-7"></a><span class="st">reuse it's window, otherwise create new one.&quot;</span></span>
<span id="cb1-8"><a href="#cb1-8"></a>  (<span class="kw">if</span> (<span class="kw">eq</span> major-mode 'helpful-mode)</span>
<span id="cb1-9"><a href="#cb1-9"></a>      (switch-to-buffer buffer-or-name)</span>
<span id="cb1-10"><a href="#cb1-10"></a>    (pop-to-buffer buffer-or-name)))</span></code></pre></div>
<!--more-->

      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2019-06-26-emacs-helpful.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Asking for input in Bash</title>
    <link href="https://d12frosted.io/posts/2019-04-12-asking-for-input-in-bash.html" />
    <id>https://d12frosted.io/posts/2019-04-12-asking-for-input-in-bash.html</id>
    <published>2019-04-12T00:00:00Z</published>
    <updated>2019-04-12T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Asking for input in Bash</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="A little black magic to ease the burden of asking user for some input" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="A little black magic to ease the burden of asking user for some input">
    <meta property="og:description" content="A little black magic to ease the burden of asking user for some input">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Asking for input in Bash</h1>
    <div class="date"><span>April 12, 2019</span></div>
    <div class="tags"><span>(<a href="../tags/shell.html">shell</a>, <a href="../tags/mirror-elpa.html">mirror-elpa</a>)</span></div>
    <article>
      <section>
        <p>Yet another tale about bash script refactoring. This time the configuration script of <a href="https://github.com/d12frosted/mirror-elpa">mirror-elpa</a> had undergone some changes and I like the result so much that I decided to share it.</p>
<p>Imagine that you want your users to give you some input, for example, repository owner and name. And you want the value so desperately, that if the user doesn’t provide one, you ask the user again. On the other hand, you also want to support default values. So for example, if the user doesn’t provide any value, but there is a default value, you could use that.</p>
<p>And you can’t use anything except for bash.</p>
<!--more-->

<p>Text descriptions are nice, but let’s see in action, what we want to achieve.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb1-1"><a href="#cb1-1"></a>λ <span class="ex">./config</span></span>
<span id="cb1-2"><a href="#cb1-2"></a><span class="ex">Repository</span> owner: ↵</span>
<span id="cb1-3"><a href="#cb1-3"></a><span class="ex">Repository</span> owner: d12frosted↵</span>
<span id="cb1-4"><a href="#cb1-4"></a><span class="ex">Repository</span> name: (mirror-elpa) ↵</span>
<span id="cb1-5"><a href="#cb1-5"></a></span>
<span id="cb1-6"><a href="#cb1-6"></a><span class="ex">owner</span> = d12frosted</span>
<span id="cb1-7"><a href="#cb1-7"></a><span class="ex">repo</span> = mirror-elpa</span>
<span id="cb1-8"><a href="#cb1-8"></a></span>
<span id="cb1-9"><a href="#cb1-9"></a>λ <span class="ex">./config</span></span>
<span id="cb1-10"><a href="#cb1-10"></a><span class="ex">Repository</span> owner: d12frosted↵</span>
<span id="cb1-11"><a href="#cb1-11"></a><span class="ex">Repository</span> name: (mirror-elpa) <span class="ex">some-other-repo</span>↵</span>
<span id="cb1-12"><a href="#cb1-12"></a></span>
<span id="cb1-13"><a href="#cb1-13"></a><span class="ex">owner</span> = d12frosted</span>
<span id="cb1-14"><a href="#cb1-14"></a><span class="ex">repo</span> = some-other-repo</span></code></pre></div>
<p>As you can see, there is no default value of the repository owner, so if the user doesn’t provide a value, we ask again. While the repository name has a default value so when the user doesn’t provide any other value, the default one is used. And the user has an option to override the default value.</p>
<p>It’s really easy to implement this in bash.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb2-1"><a href="#cb2-1"></a><span class="co"># defaults</span></span>
<span id="cb2-2"><a href="#cb2-2"></a><span class="va">owner=</span></span>
<span id="cb2-3"><a href="#cb2-3"></a><span class="va">repo=</span>mirror-elpa</span>
<span id="cb2-4"><a href="#cb2-4"></a></span>
<span id="cb2-5"><a href="#cb2-5"></a><span class="co"># get the owner</span></span>
<span id="cb2-6"><a href="#cb2-6"></a><span class="kw">while</span> <span class="bu">:</span><span class="kw">;</span> <span class="kw">do</span></span>
<span id="cb2-7"><a href="#cb2-7"></a>  <span class="bu">printf</span> <span class="st">&quot;Repository owner: &quot;</span></span>
<span id="cb2-8"><a href="#cb2-8"></a>  [ <span class="ex">-n</span> <span class="st">&quot;</span><span class="va">$owner</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="bu">printf</span> <span class="st">&quot;(%s) &quot;</span> <span class="st">&quot;</span><span class="va">$owner</span><span class="st">&quot;</span></span>
<span id="cb2-9"><a href="#cb2-9"></a>  <span class="bu">read</span> -r <span class="va">i_owner</span></span>
<span id="cb2-10"><a href="#cb2-10"></a>  [ <span class="ex">-z</span> <span class="st">&quot;</span><span class="va">$i_owner</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="va">i_owner=</span><span class="st">&quot;</span><span class="va">$owner</span><span class="st">&quot;</span></span>
<span id="cb2-11"><a href="#cb2-11"></a>  [ <span class="ex">-n</span> <span class="st">&quot;</span><span class="va">$i_owner</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="bu">break</span></span>
<span id="cb2-12"><a href="#cb2-12"></a><span class="kw">done</span></span>
<span id="cb2-13"><a href="#cb2-13"></a><span class="va">owner=</span><span class="st">&quot;</span><span class="va">$i_owner</span><span class="st">&quot;</span></span>
<span id="cb2-14"><a href="#cb2-14"></a></span>
<span id="cb2-15"><a href="#cb2-15"></a><span class="co"># get the repo</span></span>
<span id="cb2-16"><a href="#cb2-16"></a><span class="kw">while</span> <span class="bu">:</span><span class="kw">;</span> <span class="kw">do</span></span>
<span id="cb2-17"><a href="#cb2-17"></a>  <span class="bu">printf</span> <span class="st">&quot;Repository name: &quot;</span></span>
<span id="cb2-18"><a href="#cb2-18"></a>  [ <span class="ex">-n</span> <span class="st">&quot;</span><span class="va">$repo</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="bu">printf</span> <span class="st">&quot;(%s) &quot;</span> <span class="st">&quot;</span><span class="va">$repo</span><span class="st">&quot;</span></span>
<span id="cb2-19"><a href="#cb2-19"></a>  <span class="bu">read</span> -r <span class="va">i_repo</span></span>
<span id="cb2-20"><a href="#cb2-20"></a>  [ <span class="ex">-z</span> <span class="st">&quot;</span><span class="va">$i_repo</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="va">i_repo=</span><span class="st">&quot;</span><span class="va">$repo</span><span class="st">&quot;</span></span>
<span id="cb2-21"><a href="#cb2-21"></a>  [ <span class="ex">-n</span> <span class="st">&quot;</span><span class="va">$i_repo</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="bu">break</span></span>
<span id="cb2-22"><a href="#cb2-22"></a><span class="kw">done</span></span>
<span id="cb2-23"><a href="#cb2-23"></a><span class="va">repo=</span><span class="st">&quot;</span><span class="va">$i_repo</span><span class="st">&quot;</span></span>
<span id="cb2-24"><a href="#cb2-24"></a></span>
<span id="cb2-25"><a href="#cb2-25"></a><span class="co"># print result</span></span>
<span id="cb2-26"><a href="#cb2-26"></a><span class="bu">echo</span></span>
<span id="cb2-27"><a href="#cb2-27"></a><span class="bu">echo</span> <span class="st">&quot;owner = </span><span class="va">$owner</span><span class="st">&quot;</span></span>
<span id="cb2-28"><a href="#cb2-28"></a><span class="bu">echo</span> <span class="st">&quot;repo = </span><span class="va">$repo</span><span class="st">&quot;</span></span></code></pre></div>
<p>But common, we’ve heard about the DRY thing, right? We can use variable indirection and <code>declare</code> to do all the dirty job for us.</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb3-1"><a href="#cb3-1"></a><span class="co"># defaults</span></span>
<span id="cb3-2"><a href="#cb3-2"></a><span class="va">owner=</span></span>
<span id="cb3-3"><a href="#cb3-3"></a><span class="va">repo=</span>mirror-elpa</span>
<span id="cb3-4"><a href="#cb3-4"></a></span>
<span id="cb3-5"><a href="#cb3-5"></a><span class="fu">ask()</span> <span class="kw">{</span></span>
<span id="cb3-6"><a href="#cb3-6"></a>  <span class="bu">local</span> <span class="va">def</span></span>
<span id="cb3-7"><a href="#cb3-7"></a>  <span class="bu">local</span> <span class="va">res</span></span>
<span id="cb3-8"><a href="#cb3-8"></a>  <span class="va">def=</span><span class="st">&quot;${!1}&quot;</span></span>
<span id="cb3-9"><a href="#cb3-9"></a>  <span class="va">res=</span></span>
<span id="cb3-10"><a href="#cb3-10"></a>  <span class="kw">while</span> <span class="bu">:</span><span class="kw">;</span> <span class="kw">do</span></span>
<span id="cb3-11"><a href="#cb3-11"></a>    <span class="bu">printf</span> <span class="st">&quot;%s: &quot;</span> <span class="st">&quot;</span><span class="va">$2</span><span class="st">&quot;</span></span>
<span id="cb3-12"><a href="#cb3-12"></a>    [ <span class="ex">-n</span> <span class="st">&quot;</span><span class="va">$def</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="bu">printf</span> <span class="st">&quot;(%s) &quot;</span> <span class="st">&quot;</span><span class="va">$def</span><span class="st">&quot;</span></span>
<span id="cb3-13"><a href="#cb3-13"></a>    <span class="bu">read</span> -r <span class="va">res</span></span>
<span id="cb3-14"><a href="#cb3-14"></a>    [ <span class="ex">-z</span> <span class="st">&quot;</span><span class="va">$res</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="va">res=</span><span class="st">&quot;</span><span class="va">$def</span><span class="st">&quot;</span></span>
<span id="cb3-15"><a href="#cb3-15"></a>    [ <span class="ex">-n</span> <span class="st">&quot;</span><span class="va">$res</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="bu">break</span></span>
<span id="cb3-16"><a href="#cb3-16"></a>  <span class="kw">done</span></span>
<span id="cb3-17"><a href="#cb3-17"></a>  <span class="bu">declare</span> -rg <span class="st">&quot;</span><span class="va">$1</span><span class="st">=</span><span class="va">$res</span><span class="st">&quot;</span></span>
<span id="cb3-18"><a href="#cb3-18"></a><span class="kw">}</span></span>
<span id="cb3-19"><a href="#cb3-19"></a></span>
<span id="cb3-20"><a href="#cb3-20"></a><span class="ex">ask</span> <span class="st">&quot;owner&quot;</span> <span class="st">&quot;Repository owner&quot;</span></span>
<span id="cb3-21"><a href="#cb3-21"></a><span class="ex">ask</span> <span class="st">&quot;repo&quot;</span> <span class="st">&quot;Repository name&quot;</span></span>
<span id="cb3-22"><a href="#cb3-22"></a></span>
<span id="cb3-23"><a href="#cb3-23"></a><span class="bu">echo</span></span>
<span id="cb3-24"><a href="#cb3-24"></a><span class="bu">echo</span> <span class="st">&quot;owner = </span><span class="va">$owner</span><span class="st">&quot;</span></span>
<span id="cb3-25"><a href="#cb3-25"></a><span class="bu">echo</span> <span class="st">&quot;repo = </span><span class="va">$repo</span><span class="st">&quot;</span></span></code></pre></div>
<p>Indirection allows us to get the value of the variable by name. From the bash man:</p>
<blockquote>
<p>If the first character of parameter is an exclamation point (<code>!</code>), a level of variable indirection is introduced. Bash uses the value of the variable formed from the rest of parameter as the name of the variable; this variable is then expanded and that value is used in the rest of the substitution, rather than the value of parameter itself. This is known as indirect expansion.</p>
</blockquote>
<p>We could avoid using indirection here, by using <code>eval</code>, but <code>eval</code> can lead to problems as it allows execution of arbitrary code. So why risking, when we can use something suited for this job?</p>
<p>The same goes for <code>declare</code>. While we could use <code>eval</code>, there is a built-in just for this particular problem. We use <code>-r</code> since we are not planning to modify this value in the future and <code>-g</code> flag to make the variable accessible outside the function body via regular variable expansion. Please note that <code>-g</code> option is not available prior to 4.2 version of <code>bash</code>. If for some reason you plan to support older versions (for example, on macOS the preinstalled <code>bash</code> has version 3.2), then you can use <code>readonly</code>.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb4-1"><a href="#cb4-1"></a><span class="bu">readonly</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">=</span><span class="va">$res</span><span class="st">&quot;</span></span></code></pre></div>
<p>If you are interested in using <code>eval</code> instead of indirection and <code>decalre</code>, it would look like this.</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb5-1"><a href="#cb5-1"></a><span class="co">#!/usr/bin/env bash</span></span>
<span id="cb5-2"><a href="#cb5-2"></a></span>
<span id="cb5-3"><a href="#cb5-3"></a><span class="co"># defaults</span></span>
<span id="cb5-4"><a href="#cb5-4"></a><span class="va">owner=</span></span>
<span id="cb5-5"><a href="#cb5-5"></a><span class="va">repo=</span>mirror-elpa</span>
<span id="cb5-6"><a href="#cb5-6"></a></span>
<span id="cb5-7"><a href="#cb5-7"></a><span class="fu">ask()</span> <span class="kw">{</span></span>
<span id="cb5-8"><a href="#cb5-8"></a>  <span class="bu">local</span> <span class="va">def</span></span>
<span id="cb5-9"><a href="#cb5-9"></a>  <span class="bu">local</span> <span class="va">res</span></span>
<span id="cb5-10"><a href="#cb5-10"></a>  <span class="va">def=$(</span><span class="bu">eval</span> echo <span class="st">&quot;</span><span class="dt">\$</span><span class="va">$1</span><span class="st">&quot;</span><span class="va">)</span></span>
<span id="cb5-11"><a href="#cb5-11"></a>  <span class="va">res=</span></span>
<span id="cb5-12"><a href="#cb5-12"></a>  <span class="kw">while</span> <span class="bu">:</span><span class="kw">;</span> <span class="kw">do</span></span>
<span id="cb5-13"><a href="#cb5-13"></a>    <span class="bu">printf</span> <span class="st">&quot;%s: &quot;</span> <span class="st">&quot;</span><span class="va">$2</span><span class="st">&quot;</span></span>
<span id="cb5-14"><a href="#cb5-14"></a>    [ <span class="ex">-n</span> <span class="st">&quot;</span><span class="va">$def</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="bu">printf</span> <span class="st">&quot;(%s) &quot;</span> <span class="st">&quot;</span><span class="va">$def</span><span class="st">&quot;</span></span>
<span id="cb5-15"><a href="#cb5-15"></a>    <span class="bu">read</span> -r <span class="va">res</span></span>
<span id="cb5-16"><a href="#cb5-16"></a>    [ <span class="ex">-z</span> <span class="st">&quot;</span><span class="va">$res</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="va">res=</span><span class="st">&quot;</span><span class="va">$def</span><span class="st">&quot;</span></span>
<span id="cb5-17"><a href="#cb5-17"></a>    [ <span class="ex">-n</span> <span class="st">&quot;</span><span class="va">$res</span><span class="st">&quot;</span> ] <span class="kw">&amp;&amp;</span> <span class="bu">break</span></span>
<span id="cb5-18"><a href="#cb5-18"></a>  <span class="kw">done</span></span>
<span id="cb5-19"><a href="#cb5-19"></a>  <span class="bu">eval</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">=</span><span class="va">$res</span><span class="st">&quot;</span></span>
<span id="cb5-20"><a href="#cb5-20"></a><span class="kw">}</span></span>
<span id="cb5-21"><a href="#cb5-21"></a></span>
<span id="cb5-22"><a href="#cb5-22"></a><span class="ex">ask</span> <span class="st">&quot;owner&quot;</span> <span class="st">&quot;Repository owner&quot;</span></span>
<span id="cb5-23"><a href="#cb5-23"></a><span class="ex">ask</span> <span class="st">&quot;repo&quot;</span> <span class="st">&quot;Repository name&quot;</span></span>
<span id="cb5-24"><a href="#cb5-24"></a></span>
<span id="cb5-25"><a href="#cb5-25"></a><span class="bu">echo</span></span>
<span id="cb5-26"><a href="#cb5-26"></a><span class="bu">echo</span> <span class="st">&quot;owner = </span><span class="va">$owner</span><span class="st">&quot;</span></span>
<span id="cb5-27"><a href="#cb5-27"></a><span class="bu">echo</span> <span class="st">&quot;repo = </span><span class="va">$repo</span><span class="st">&quot;</span></span></code></pre></div>
<p>But personally, I would not use <code>eval</code> until I really have to.</p>
<p>You can find another example of this technique in <a href="./2018-11-04-revisiting-eru.html">Revisiting Eru</a> post.</p>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2019-04-12-asking-for-input-in-bash.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>
<entry>
    <title>Revisiting Eru</title>
    <link href="https://d12frosted.io/posts/2018-11-04-revisiting-eru.html" />
    <id>https://d12frosted.io/posts/2018-11-04-revisiting-eru.html</id>
    <published>2018-11-04T00:00:00Z</published>
    <updated>2018-11-04T00:00:00Z</updated>
    <summary type="html"><![CDATA[<!doctype html>
<html lang="en">
  <head>
    <title>Revisiting Eru</title>
    <link rel="stylesheet" href="../css/post-bundle.css" />
    <meta charset="utf-8">
<meta http-equiv="x-ua-compatible" content="ie=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="keywords" content="blog,programming,Haskell,Emacs,fish,bash">
<meta property="og:title" content="Boris Buliga">
<meta property="og:locale" content="es_US">
<meta property="og:url" content="https://d12frosted.io">

<link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
<link rel="manifest" href="../site.webmanifest">

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
<script>
  MathJax = {
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script type="text/javascript" id="MathJax-script" async src="../library/mathjax/es5/tex-svg.js">
</script>
<script src="../library/chart.js/dist/Chart.min.js"></script>
<script data-goatcounter="https://d12frosted.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>

    <meta name="description" content="Overview of my computer configuration maintenance system" />
    <meta name="twitter:card" content="summary">
    <meta name="twitter:description" content="Overview of my computer configuration maintenance system">
    <meta property="og:description" content="Overview of my computer configuration maintenance system">
  </head>
  <body>
    <header>
      <nav>
  <ul>
    <li><a class="fa fa-home" href="../" title="Home">&nbsp Home</a></li>
    <li><a class="fa fa-suitcase" href="../projects.html" title="Projects">&nbsp Projects</a></li>
    <li><a class="fa fa-rss" href="../atom.xml" title="RSS">&nbsp RSS</a></li>
    <li><a class="fa fa-github" href="https://github.com/d12frosted" title="GitHub profile">&nbsp GitHub</a></li>
  </ul>
</nav>

    </header>

    <h1 class="post-title">Revisiting Eru</h1>
    <div class="date"><span>November  4, 2018</span></div>
    <div class="tags"><span>(<a href="../tags/shell.html">shell</a>, <a href="../tags/environment.html">environment</a>)</span></div>
    <article>
      <section>
        <p>As you might know, Eru is the supreme deity of Arda. The first things that Eru created where the Ainur. He then bade the Ainur to sing to him. Each Ainu had a particular theme given by Eru. Sure enough, Eru makes the ‘World and All That Is’.</p>
<p>So when I get a new clean system there is nothing yet. And so I call upon the wisdom and power of <code>Eru.sh</code> - the one who creates Ainur and the ‘World and All That Is’.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb1-1"><a href="#cb1-1"></a>$ <span class="ex">curl</span> https://raw.githubusercontent.com/d12frosted/environment/master/bootstrap/eru.sh <span class="kw">|</span> <span class="fu">bash</span></span></code></pre></div>
<p>I just have to wait patiently, while everything is being downloaded and installed, while all configuration cogs are being placed on the right spot.</p>
<div class="post-image">
<img src="../images/eru-example-1.png" />
</div>

<!--more-->

<p>The good thing about <code>Eru.sh</code> is that after the ‘World and All That Is’ creation you can still call upon his help and you can even ask him to help with specific theme or themes only.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb2-1"><a href="#cb2-1"></a>$ <span class="ex">./bootstrap/eru.sh</span>                   <span class="co"># to help with all themes</span></span>
<span id="cb2-2"><a href="#cb2-2"></a>$ <span class="ex">./bootstrap/eru.sh</span> repositories      <span class="co"># to help with repositories</span></span>
<span id="cb2-3"><a href="#cb2-3"></a>$ <span class="ex">./bootstrap/eru.sh</span> repositories brew <span class="co"># to help with repositories and brew</span></span></code></pre></div>
<p><code>Eru.sh</code> reveals all available themes. For example,</p>
<pre class="text"><code>=&gt; Guardian Theme :: Checking operating system
=&gt; Supporting Theme :: Defining helpers
=&gt; Supporting Theme :: Defining variables
=&gt; Guardian Theme :: Ensure all directories exists
-&gt; SSH Theme :: Checking SSH keys
-&gt; Repositories Theme :: Sync environment repository
-&gt; Repositories Theme :: Sync repositories from Repofile
-&gt; Linking Theme :: Link all files as defined in Linkfile
</code></pre>
<p>Every theme that begins with <code>=&gt;</code> is a mandatory theme - the one you can’t skip. Every theme prefixed with <code>-&gt;</code> is an optional one. If you don’t specify any themes, all optional themes are sung. If you do specify at least one theme - only specified are sang.</p>
<div class="post-image">
<img src="../images/eru-example-2.png" />
</div>

<p>You can find the latest version of <code>Eru.sh</code> on <a href="https://github.com/d12frosted/environment/blob/master/bootstrap/eru.sh">GitHub</a>.</p>
<h1 id="interesting-stuff">Interesting stuff</h1>
<p>Mythology is great, but let’s check what’s cool about <code>Eru.sh</code>.</p>
<ol>
<li><p>It installs and updates a lot of applications and utilities that I use on a daily basis.</p></li>
<li><p>It installs my configurations for these applications. The most important ones are <code>Emacs</code>, <code>fish</code>, <code>skhd</code>, <code>chunkwm</code>. Plus it tries to set up different parts of macOS for my liking.</p></li>
<li><p>It works in an incremental fashion. In general, it installs only what is missing. So in case when my environment is up to date, <code>Eru.sh</code> is almost instant.</p></li>
<li><p>It allows you to setup only specific parts. For example, you can only install dependencies or sync mandatory repositories. Or do both.</p></li>
</ol>
<p>This means, that on a fresh system I can easily get my environment just by running one command and waiting for a long time. But in a working environment, I can easily get the latest version of it. This comes in handy since I use more than one computer.</p>
<h2 id="bash-magic">Bash magic</h2>
<p><code>Eru.sh</code> is written in bash because it must work on systems without Haskell preinstalled. Features of bash4 are not used, because of the same reasons, it is not available on a clean macOS.</p>
<p><code>Eru.sh</code> is mostly a boring bash script. But there are several points that I find interesting myself.</p>
<h3 id="mapping-files">Mapping files</h3>
<p>There are themes that perform the same action over a set of arguments. In order to avoid noise in <code>Eru.sh</code> these arguments are stored in external files (rule files). For example, <code>repositories</code> theme synchronises repositories by rules specified in the <code>Repofile</code>, which looks like the following.</p>
<pre class="text"><code>$HOME/.spacemacs syl20bnr/spacemacs develop
$DEVELOPER/fancy-yank d12frosted/fancy-yank
$DEVELOPER/orgability d12frosted/orgability
$DEVELOPER/org-drawer-list d12frosted/org-drawer-list
$DEVELOPER/flyspell-correct d12frosted/flyspell-correct
</code></pre>
<p>Every line specifies a rule - target location of the repository, remote repository URL (fir GitHub one case specify only <code>owner/repo</code> instead of a full HTTPS/SSH URL) and branch to use.</p>
<p>There is also a function that gets these value as arguments and does all the job. In the case of <code>repositories</code> theme, the function is called <code>sync_repo</code>.</p>
<p>One can easily write a script reading a given file line by line and passing them to a function. For example,</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb5-1"><a href="#cb5-1"></a><span class="kw">while</span> <span class="va">IFS=</span><span class="st">''</span> <span class="bu">read</span> -r <span class="va">line</span> <span class="kw">|| [[</span> <span class="ot">-n</span> <span class="st">&quot;</span><span class="va">$line</span><span class="st">&quot;</span><span class="kw"> ]]</span>; <span class="kw">do</span></span>
<span id="cb5-2"><a href="#cb5-2"></a>  <span class="ex">sync_repo</span> <span class="va">$line</span></span>
<span id="cb5-3"><a href="#cb5-3"></a><span class="kw">done</span> <span class="op">&lt;</span> <span class="st">&quot;Repofile&quot;</span></span></code></pre></div>
<p>Please note that <code>$line</code> should not be put in parenthesis otherwise whole line will be passed as a first argument, but we want to split it into multiple arguments by a space.</p>
<p>While this works, it doesn’t scale, requires repetition and decreases the level of code readability. Let’s abstract it a little bit by extracting the logic of reading the lines and passing them to a function.</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb6-1"><a href="#cb6-1"></a><span class="kw">function</span><span class="fu"> map_lines()</span> <span class="kw">{</span></span>
<span id="cb6-2"><a href="#cb6-2"></a>  <span class="kw">while</span> <span class="va">IFS=</span><span class="st">''</span> <span class="bu">read</span> -r <span class="va">line</span> <span class="kw">|| [[</span> <span class="ot">-n</span> <span class="st">&quot;</span><span class="va">$line</span><span class="st">&quot;</span><span class="kw"> ]]</span>; <span class="kw">do</span></span>
<span id="cb6-3"><a href="#cb6-3"></a>    <span class="va">$1</span> <span class="va">$line</span></span>
<span id="cb6-4"><a href="#cb6-4"></a>  <span class="kw">done</span> <span class="op">&lt;</span> <span class="st">&quot;</span><span class="va">$2</span><span class="st">&quot;</span></span>
<span id="cb6-5"><a href="#cb6-5"></a><span class="kw">}</span></span>
<span id="cb6-6"><a href="#cb6-6"></a></span>
<span id="cb6-7"><a href="#cb6-7"></a><span class="ex">map_lines</span> sync_repo <span class="st">&quot;Repofile&quot;</span></span></code></pre></div>
<p>Now it looks like a Functor for a file. No repetition, it does scale well and we know exactly what is the purpose of <code>map_lines sync_repo "RepoFile"</code>.</p>
<h3 id="dealing-with-multiple-optional-themes">Dealing with multiple optional themes</h3>
<p>So in <code>Eru.sh</code>, we have multiple optional themes. The easiest way to deal with them is to create a separate variable for every theme and then use it to check if we should run an action for a given theme.</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb7-1"><a href="#cb7-1"></a><span class="va">ALL=</span><span class="st">&quot;true&quot;</span></span>
<span id="cb7-2"><a href="#cb7-2"></a><span class="va">REPOSITORIES=</span><span class="st">&quot;false&quot;</span></span>
<span id="cb7-3"><a href="#cb7-3"></a><span class="va">LINKING=</span><span class="st">&quot;false&quot;</span></span>
<span id="cb7-4"><a href="#cb7-4"></a><span class="va">BREW=</span><span class="st">&quot;false&quot;</span></span>
<span id="cb7-5"><a href="#cb7-5"></a></span>
<span id="cb7-6"><a href="#cb7-6"></a><span class="va">POSITIONAL=()</span></span>
<span id="cb7-7"><a href="#cb7-7"></a></span>
<span id="cb7-8"><a href="#cb7-8"></a><span class="kw">while [[</span> <span class="va">$#</span> <span class="ot">-gt</span> 0<span class="kw"> ]]</span></span>
<span id="cb7-9"><a href="#cb7-9"></a><span class="kw">do</span></span>
<span id="cb7-10"><a href="#cb7-10"></a>  <span class="va">key=</span><span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span></span>
<span id="cb7-11"><a href="#cb7-11"></a>  <span class="kw">case</span> <span class="va">$key</span><span class="kw"> in</span></span>
<span id="cb7-12"><a href="#cb7-12"></a>    repo<span class="kw">)</span></span>
<span id="cb7-13"><a href="#cb7-13"></a>      <span class="va">ALL=</span><span class="st">&quot;false&quot;</span></span>
<span id="cb7-14"><a href="#cb7-14"></a>      <span class="va">REPOSITORIES=</span><span class="st">&quot;true&quot;</span></span>
<span id="cb7-15"><a href="#cb7-15"></a>      <span class="bu">shift</span> <span class="co"># past argument</span></span>
<span id="cb7-16"><a href="#cb7-16"></a>      <span class="kw">;;</span></span>
<span id="cb7-17"><a href="#cb7-17"></a>    link<span class="kw">)</span></span>
<span id="cb7-18"><a href="#cb7-18"></a>      <span class="va">ALL=</span><span class="st">&quot;false&quot;</span></span>
<span id="cb7-19"><a href="#cb7-19"></a>      <span class="va">LINKING=</span><span class="st">&quot;true&quot;</span></span>
<span id="cb7-20"><a href="#cb7-20"></a>      <span class="bu">shift</span> <span class="co"># past argument</span></span>
<span id="cb7-21"><a href="#cb7-21"></a>      <span class="kw">;;</span></span>
<span id="cb7-22"><a href="#cb7-22"></a>    brew<span class="kw">)</span></span>
<span id="cb7-23"><a href="#cb7-23"></a>      <span class="va">ALL=</span><span class="st">&quot;false&quot;</span></span>
<span id="cb7-24"><a href="#cb7-24"></a>      <span class="va">BREW=</span><span class="st">&quot;true&quot;</span></span>
<span id="cb7-25"><a href="#cb7-25"></a>      <span class="bu">shift</span> <span class="co"># past argument</span></span>
<span id="cb7-26"><a href="#cb7-26"></a>      <span class="kw">;;</span></span>
<span id="cb7-27"><a href="#cb7-27"></a>    *<span class="kw">)</span>    <span class="co"># unknown option</span></span>
<span id="cb7-28"><a href="#cb7-28"></a>      <span class="va">POSITIONAL+=(</span><span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span><span class="va">)</span> <span class="co"># save it in an array for later</span></span>
<span id="cb7-29"><a href="#cb7-29"></a>      <span class="bu">shift</span> <span class="co"># past argument</span></span>
<span id="cb7-30"><a href="#cb7-30"></a>      <span class="kw">;;</span></span>
<span id="cb7-31"><a href="#cb7-31"></a>  <span class="kw">esac</span></span>
<span id="cb7-32"><a href="#cb7-32"></a><span class="kw">done</span></span>
<span id="cb7-33"><a href="#cb7-33"></a><span class="kw">set</span> <span class="ex">--</span> <span class="st">&quot;</span><span class="va">${POSITIONAL[@]}</span><span class="st">&quot;</span> <span class="co"># restore positional parameters</span></span>
<span id="cb7-34"><a href="#cb7-34"></a></span>
<span id="cb7-35"><a href="#cb7-35"></a><span class="kw">if [[</span> <span class="st">&quot;</span><span class="va">$ALL</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span> || <span class="st">&quot;</span><span class="va">$REPOSITORIES</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span><span class="kw"> ]]</span>; <span class="kw">then</span></span>
<span id="cb7-36"><a href="#cb7-36"></a>  <span class="ex">optional_theme</span> <span class="st">&quot;Repositories&quot;</span> <span class="co"># logging</span></span>
<span id="cb7-37"><a href="#cb7-37"></a>  <span class="ex">map_lines</span> sync_repo <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap/Repofile&quot;</span></span>
<span id="cb7-38"><a href="#cb7-38"></a><span class="kw">else</span></span>
<span id="cb7-39"><a href="#cb7-39"></a>  <span class="ex">inactive_theme</span> <span class="st">&quot;Repositories&quot;</span> <span class="co"># logging</span></span>
<span id="cb7-40"><a href="#cb7-40"></a><span class="kw">fi</span></span>
<span id="cb7-41"><a href="#cb7-41"></a></span>
<span id="cb7-42"><a href="#cb7-42"></a><span class="kw">if [[</span> <span class="st">&quot;</span><span class="va">$ALL</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span> || <span class="st">&quot;</span><span class="va">$LINKING</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span><span class="kw"> ]]</span>; <span class="kw">then</span></span>
<span id="cb7-43"><a href="#cb7-43"></a>  <span class="ex">optional_theme</span> <span class="st">&quot;Linking&quot;</span> <span class="co"># logging</span></span>
<span id="cb7-44"><a href="#cb7-44"></a>  <span class="ex">map_lines</span> safe_link  <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap/Linkfile&quot;</span></span>
<span id="cb7-45"><a href="#cb7-45"></a><span class="kw">else</span></span>
<span id="cb7-46"><a href="#cb7-46"></a>  <span class="ex">inactive_theme</span> <span class="st">&quot;Linking&quot;</span> <span class="co"># logging</span></span>
<span id="cb7-47"><a href="#cb7-47"></a><span class="kw">fi</span></span>
<span id="cb7-48"><a href="#cb7-48"></a></span>
<span id="cb7-49"><a href="#cb7-49"></a><span class="kw">if [[</span> <span class="st">&quot;</span><span class="va">$ALL</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span> || <span class="st">&quot;</span><span class="va">$BREW</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span><span class="kw"> ]]</span>; <span class="kw">then</span></span>
<span id="cb7-50"><a href="#cb7-50"></a>  <span class="ex">optional_theme</span> <span class="st">&quot;Brew&quot;</span> <span class="co"># logging</span></span>
<span id="cb7-51"><a href="#cb7-51"></a>  <span class="bu">cd</span> <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap&quot;</span> <span class="kw">&amp;&amp;</span> <span class="ex">brew</span> bundle</span>
<span id="cb7-52"><a href="#cb7-52"></a><span class="kw">else</span></span>
<span id="cb7-53"><a href="#cb7-53"></a>  <span class="ex">inactive_theme</span> <span class="st">&quot;Brew&quot;</span> <span class="co"># logging</span></span>
<span id="cb7-54"><a href="#cb7-54"></a><span class="kw">fi</span></span></code></pre></div>
<p>But this gets nasty every time a new theme is added (though it happens rarely). And most importantly, it looks so redundant, so repetitive that I almost fall asleep while writing it.</p>
<p>We can improve it a little bit. Let’s focus on the part where we check for a variable value.</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb8-1"><a href="#cb8-1"></a><span class="kw">function</span><span class="fu"> theme_guard()</span> <span class="kw">{</span></span>
<span id="cb8-2"><a href="#cb8-2"></a>  <span class="kw">if [[</span> <span class="st">&quot;</span><span class="va">$ALL</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span> || <span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span><span class="kw"> ]]</span>; <span class="kw">then</span></span>
<span id="cb8-3"><a href="#cb8-3"></a>    <span class="ex">optional_theme</span> <span class="st">&quot;</span><span class="va">$2</span><span class="st">&quot;</span> <span class="co"># logging</span></span>
<span id="cb8-4"><a href="#cb8-4"></a>    <span class="bu">return</span> 0</span>
<span id="cb8-5"><a href="#cb8-5"></a>  <span class="kw">else</span></span>
<span id="cb8-6"><a href="#cb8-6"></a>    <span class="ex">inactive_theme</span> <span class="st">&quot;</span><span class="va">$2</span><span class="st">&quot;</span> <span class="co"># logging</span></span>
<span id="cb8-7"><a href="#cb8-7"></a>    <span class="bu">return</span> 1</span>
<span id="cb8-8"><a href="#cb8-8"></a>  <span class="kw">fi</span></span>
<span id="cb8-9"><a href="#cb8-9"></a><span class="kw">}</span></span>
<span id="cb8-10"><a href="#cb8-10"></a></span>
<span id="cb8-11"><a href="#cb8-11"></a><span class="ex">theme_guard</span> <span class="st">&quot;</span><span class="va">$REPOSITORIES</span><span class="st">&quot;</span> <span class="st">&quot;Repositores&quot;</span> <span class="kw">&amp;&amp;</span> <span class="kw">{</span></span>
<span id="cb8-12"><a href="#cb8-12"></a>  <span class="ex">map_lines</span> sync_repo <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap/Repofile&quot;</span></span>
<span id="cb8-13"><a href="#cb8-13"></a><span class="kw">}</span></span>
<span id="cb8-14"><a href="#cb8-14"></a></span>
<span id="cb8-15"><a href="#cb8-15"></a><span class="ex">theme_guard</span> <span class="st">&quot;</span><span class="va">$LINKING</span><span class="st">&quot;</span> <span class="st">&quot;Linking&quot;</span> <span class="kw">&amp;&amp;</span> <span class="kw">{</span></span>
<span id="cb8-16"><a href="#cb8-16"></a>  <span class="ex">map_lines</span> safe_link  <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap/Linkfile&quot;</span></span>
<span id="cb8-17"><a href="#cb8-17"></a><span class="kw">}</span></span>
<span id="cb8-18"><a href="#cb8-18"></a></span>
<span id="cb8-19"><a href="#cb8-19"></a><span class="ex">theme_guard</span> <span class="st">&quot;</span><span class="va">$BREW</span><span class="st">&quot;</span> <span class="st">&quot;Brew&quot;</span> <span class="kw">&amp;&amp;</span> <span class="kw">{</span></span>
<span id="cb8-20"><a href="#cb8-20"></a>  <span class="bu">cd</span> <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap&quot;</span> <span class="kw">&amp;&amp;</span> <span class="ex">brew</span> bundle</span>
<span id="cb8-21"><a href="#cb8-21"></a><span class="kw">}</span></span></code></pre></div>
<p>So we moved out all the checks and logging to a helper function. It is already good enough as an improvement, but we can do even better.</p>
<p>Bash provides an ability to evaluate arbitrary code by using <code>eval</code>. Please note though, that <code>eval</code> is a bane of shell programming and should be avoided like leprosy. In general, it’s okayish to use <code>eval</code> when you fully control what is passed to it. If you pass user input to <code>eval</code> then you have a problem, because now you need to deal with code injection.</p>
<p>We can use <code>eval</code> to get the value of a variable by a string.</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb9-1"><a href="#cb9-1"></a>$ <span class="va">CUSTOM_VAR=</span>42</span>
<span id="cb9-2"><a href="#cb9-2"></a></span>
<span id="cb9-3"><a href="#cb9-3"></a>$ <span class="bu">echo</span> <span class="st">'$CUSTOM_VAR'</span></span>
<span id="cb9-4"><a href="#cb9-4"></a><span class="va">$CUSTOM_VAR</span></span>
<span id="cb9-5"><a href="#cb9-5"></a></span>
<span id="cb9-6"><a href="#cb9-6"></a>$ <span class="bu">eval</span> echo <span class="st">'$CUSTOM_VAR'</span></span>
<span id="cb9-7"><a href="#cb9-7"></a><span class="ex">42</span></span></code></pre></div>
<p>While this is good enough, it’s possible to avoid <code>eval</code> in this particular case, when we just want to get the value of variable by name.</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb10-1"><a href="#cb10-1"></a>$ <span class="va">CUSTOM_VAR=</span>42</span>
<span id="cb10-2"><a href="#cb10-2"></a></span>
<span id="cb10-3"><a href="#cb10-3"></a>$ <span class="bu">echo</span> <span class="st">'$CUSTOM_VAR'</span></span>
<span id="cb10-4"><a href="#cb10-4"></a><span class="va">$CUSTOM_VAR</span></span>
<span id="cb10-5"><a href="#cb10-5"></a></span>
<span id="cb10-6"><a href="#cb10-6"></a>$ <span class="va">CUSTOM_VAR_REF=</span>CUSTOM_VAR</span>
<span id="cb10-7"><a href="#cb10-7"></a></span>
<span id="cb10-8"><a href="#cb10-8"></a>$ <span class="bu">echo</span> <span class="va">${!CUSTOM_VAR_REF}</span></span>
<span id="cb10-9"><a href="#cb10-9"></a><span class="ex">42</span></span>
<span id="cb10-10"><a href="#cb10-10"></a></span></code></pre></div>
<p>Though if you want to expand variables in arbitrary string (like, <code>$HOME/config.json</code>), you’ll have to use <code>eval</code>.</p>
<p>Right now we are passing two arguments to <code>theme_guard</code>: a guarding variable value and the name of a theme. But the code looks similar - the first one is prefixed with the <code>$</code> sign and is in uppercase, while the second one is in capital case. In order to deal with case, we can use <code>awk</code>.</p>
<div class="sourceCode" id="cb11"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb11-1"><a href="#cb11-1"></a><span class="kw">function</span><span class="fu"> theme_guard()</span> <span class="kw">{</span></span>
<span id="cb11-2"><a href="#cb11-2"></a>  <span class="va">key=$(</span><span class="bu">echo</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span> <span class="kw">|</span> <span class="fu">awk</span> <span class="st">'{print toupper($0)}'</span><span class="va">)</span></span>
<span id="cb11-3"><a href="#cb11-3"></a>  <span class="bu">local</span> <span class="va">guard_ref=</span><span class="st">&quot;guard_</span><span class="va">$key</span><span class="st">&quot;</span></span>
<span id="cb11-4"><a href="#cb11-4"></a>  <span class="va">guard=</span><span class="st">&quot;</span><span class="va">${!guard_ref}</span><span class="st">&quot;</span></span>
<span id="cb11-5"><a href="#cb11-5"></a>  <span class="kw">if [[</span> <span class="st">&quot;</span><span class="va">$ALL</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span> || <span class="st">&quot;</span><span class="va">$guard</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span><span class="kw"> ]]</span>; <span class="kw">then</span></span>
<span id="cb11-6"><a href="#cb11-6"></a>    <span class="ex">optional_theme</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span> <span class="co"># logging</span></span>
<span id="cb11-7"><a href="#cb11-7"></a>    <span class="bu">return</span> 0</span>
<span id="cb11-8"><a href="#cb11-8"></a>  <span class="kw">else</span></span>
<span id="cb11-9"><a href="#cb11-9"></a>    <span class="ex">inactive_theme</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span> <span class="co"># logging</span></span>
<span id="cb11-10"><a href="#cb11-10"></a>    <span class="bu">return</span> 1</span>
<span id="cb11-11"><a href="#cb11-11"></a>  <span class="kw">fi</span></span>
<span id="cb11-12"><a href="#cb11-12"></a><span class="kw">}</span></span>
<span id="cb11-13"><a href="#cb11-13"></a></span>
<span id="cb11-14"><a href="#cb11-14"></a><span class="ex">theme_guard</span> <span class="st">&quot;Repositores&quot;</span> <span class="kw">&amp;&amp;</span> <span class="kw">{</span></span>
<span id="cb11-15"><a href="#cb11-15"></a>  <span class="ex">map_lines</span> sync_repo <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap/Repofile&quot;</span></span>
<span id="cb11-16"><a href="#cb11-16"></a><span class="kw">}</span></span>
<span id="cb11-17"><a href="#cb11-17"></a></span>
<span id="cb11-18"><a href="#cb11-18"></a><span class="ex">theme_guard</span> <span class="st">&quot;Linking&quot;</span> <span class="kw">&amp;&amp;</span> <span class="kw">{</span></span>
<span id="cb11-19"><a href="#cb11-19"></a>  <span class="ex">map_lines</span> safe_link  <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap/Linkfile&quot;</span></span>
<span id="cb11-20"><a href="#cb11-20"></a><span class="kw">}</span></span>
<span id="cb11-21"><a href="#cb11-21"></a></span>
<span id="cb11-22"><a href="#cb11-22"></a><span class="ex">theme_guard</span> <span class="st">&quot;Brew&quot;</span> <span class="kw">&amp;&amp;</span> <span class="kw">{</span></span>
<span id="cb11-23"><a href="#cb11-23"></a>  <span class="bu">cd</span> <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap&quot;</span> <span class="kw">&amp;&amp;</span> <span class="ex">brew</span> bundle</span>
<span id="cb11-24"><a href="#cb11-24"></a><span class="kw">}</span></span></code></pre></div>
<p>We got a very small improvement - we just don’t need to pass the theme name to the <code>theme_guard</code> twice. But I find it satisfying anyway. Also, it will come handy a little bit later.</p>
<p>Now let’s go back to the variable declaration. It turns out that we can use <code>eval</code> to declare variables as well.</p>
<div class="sourceCode" id="cb12"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb12-1"><a href="#cb12-1"></a>$ <span class="bu">eval</span> <span class="st">'MEANING=42'</span></span>
<span id="cb12-2"><a href="#cb12-2"></a></span>
<span id="cb12-3"><a href="#cb12-3"></a>$ <span class="bu">eval</span> echo <span class="st">'$MEANING'</span></span>
<span id="cb12-4"><a href="#cb12-4"></a><span class="ex">42</span></span></code></pre></div>
<p>But defining variables based on the user input is dangerous. Fortunately, there is a <code>declare</code> program designed just for this task. Moreover, it allows declaring read-only variables.</p>
<div class="sourceCode" id="cb13"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb13-1"><a href="#cb13-1"></a>$ <span class="bu">declare</span> <span class="st">&quot;CUSTOM_VAR=42&quot;</span></span>
<span id="cb13-2"><a href="#cb13-2"></a></span>
<span id="cb13-3"><a href="#cb13-3"></a>$ <span class="bu">echo</span> <span class="va">$CUSTOM_VAR</span></span>
<span id="cb13-4"><a href="#cb13-4"></a><span class="ex">42</span></span>
<span id="cb13-5"><a href="#cb13-5"></a></span>
<span id="cb13-6"><a href="#cb13-6"></a>$ <span class="va">CUSTOM_VAR=</span>12</span>
<span id="cb13-7"><a href="#cb13-7"></a></span>
<span id="cb13-8"><a href="#cb13-8"></a>$ <span class="bu">echo</span> <span class="va">$CUSTOM_VAR</span></span>
<span id="cb13-9"><a href="#cb13-9"></a><span class="ex">12</span></span>
<span id="cb13-10"><a href="#cb13-10"></a></span>
<span id="cb13-11"><a href="#cb13-11"></a>$ <span class="bu">declare</span> -r <span class="st">&quot;CUSTOM_VAL=42&quot;</span></span>
<span id="cb13-12"><a href="#cb13-12"></a></span>
<span id="cb13-13"><a href="#cb13-13"></a>$ <span class="bu">echo</span> <span class="va">$CUSTOM_VAL</span></span>
<span id="cb13-14"><a href="#cb13-14"></a><span class="ex">42</span></span>
<span id="cb13-15"><a href="#cb13-15"></a></span>
<span id="cb13-16"><a href="#cb13-16"></a>$ <span class="va">CUSTOM_VAL=</span>12</span>
<span id="cb13-17"><a href="#cb13-17"></a><span class="ex">bash</span>: CUSTOM_VAL: readonly variable</span></code></pre></div>
<p>So let’s use this <code>declare</code> for our good.</p>
<div class="sourceCode" id="cb14"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb14-1"><a href="#cb14-1"></a><span class="va">ALL=</span><span class="st">&quot;true&quot;</span></span>
<span id="cb14-2"><a href="#cb14-2"></a></span>
<span id="cb14-3"><a href="#cb14-3"></a><span class="va">POSITIONAL=()</span></span>
<span id="cb14-4"><a href="#cb14-4"></a><span class="kw">while [[</span> <span class="va">$#</span> <span class="ot">-gt</span> 0<span class="kw"> ]]</span></span>
<span id="cb14-5"><a href="#cb14-5"></a><span class="kw">do</span></span>
<span id="cb14-6"><a href="#cb14-6"></a>  <span class="kw">if [[</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span> <span class="ot">!=</span> <span class="st">&quot;&quot;</span><span class="kw"> ]]</span>; <span class="kw">then</span></span>
<span id="cb14-7"><a href="#cb14-7"></a>    <span class="va">key=$(</span><span class="bu">echo</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span> <span class="kw">|</span> <span class="fu">awk</span> <span class="st">'{print toupper($0)}'</span><span class="va">)</span></span>
<span id="cb14-8"><a href="#cb14-8"></a>    <span class="bu">declare</span> -r <span class="st">&quot;</span><span class="va">$key</span><span class="st">=true&quot;</span></span>
<span id="cb14-9"><a href="#cb14-9"></a>    <span class="va">ALL=</span><span class="st">&quot;false&quot;</span></span>
<span id="cb14-10"><a href="#cb14-10"></a>  <span class="kw">fi</span></span>
<span id="cb14-11"><a href="#cb14-11"></a>  <span class="bu">shift</span></span>
<span id="cb14-12"><a href="#cb14-12"></a><span class="kw">done</span></span>
<span id="cb14-13"><a href="#cb14-13"></a><span class="kw">set</span> <span class="ex">--</span> <span class="st">&quot;</span><span class="va">${POSITIONAL[@]}</span><span class="st">&quot;</span> <span class="co"># restore positional parameters</span></span></code></pre></div>
<p>While this becomes a little bit harder to understand it saves us a lot of repetition. The only thing I would improve here immediately is to add a unique prefix to variable names, so user does not interfere with other variables. The final version looks like it.</p>
<div class="sourceCode" id="cb15"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb15-1"><a href="#cb15-1"></a><span class="va">ALL=</span><span class="st">&quot;true&quot;</span></span>
<span id="cb15-2"><a href="#cb15-2"></a></span>
<span id="cb15-3"><a href="#cb15-3"></a><span class="va">POSITIONAL=()</span></span>
<span id="cb15-4"><a href="#cb15-4"></a><span class="kw">while [[</span> <span class="va">$#</span> <span class="ot">-gt</span> 0<span class="kw"> ]]</span></span>
<span id="cb15-5"><a href="#cb15-5"></a><span class="kw">do</span></span>
<span id="cb15-6"><a href="#cb15-6"></a>  <span class="kw">if [[</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span> <span class="ot">!=</span> <span class="st">&quot;&quot;</span><span class="kw"> ]]</span>; <span class="kw">then</span></span>
<span id="cb15-7"><a href="#cb15-7"></a>    <span class="va">key=$(</span><span class="bu">echo</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span> <span class="kw">|</span> <span class="fu">awk</span> <span class="st">'{print tolower($0)}'</span><span class="va">)</span></span>
<span id="cb15-8"><a href="#cb15-8"></a>    <span class="bu">declare</span> -r <span class="st">&quot;guard_</span><span class="va">$key</span><span class="st">=true&quot;</span></span>
<span id="cb15-9"><a href="#cb15-9"></a>    <span class="va">ALL=</span><span class="st">&quot;false&quot;</span></span>
<span id="cb15-10"><a href="#cb15-10"></a>  <span class="kw">fi</span></span>
<span id="cb15-11"><a href="#cb15-11"></a>  <span class="bu">shift</span></span>
<span id="cb15-12"><a href="#cb15-12"></a><span class="kw">done</span></span>
<span id="cb15-13"><a href="#cb15-13"></a><span class="kw">set</span> <span class="ex">--</span> <span class="st">&quot;</span><span class="va">${POSITIONAL[@]}</span><span class="st">&quot;</span> <span class="co"># restore positional parameters</span></span>
<span id="cb15-14"><a href="#cb15-14"></a></span>
<span id="cb15-15"><a href="#cb15-15"></a><span class="kw">function</span><span class="fu"> theme_guard()</span> <span class="kw">{</span></span>
<span id="cb15-16"><a href="#cb15-16"></a>  <span class="va">key=$(</span><span class="bu">echo</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span> <span class="kw">|</span> <span class="fu">awk</span> <span class="st">'{print tolower($0)}'</span><span class="va">)</span></span>
<span id="cb15-17"><a href="#cb15-17"></a>  <span class="bu">local</span> <span class="va">guard_ref=</span><span class="st">&quot;guard_</span><span class="va">$key</span><span class="st">&quot;</span></span>
<span id="cb15-18"><a href="#cb15-18"></a>  <span class="va">guard=</span><span class="st">&quot;</span><span class="va">${!guard_ref}</span><span class="st">&quot;</span></span>
<span id="cb15-19"><a href="#cb15-19"></a>  <span class="kw">if [[</span> <span class="st">&quot;</span><span class="va">$ALL</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span> || <span class="st">&quot;</span><span class="va">$guard</span><span class="st">&quot;</span> <span class="ot">=</span> <span class="st">&quot;true&quot;</span><span class="kw"> ]]</span>; <span class="kw">then</span></span>
<span id="cb15-20"><a href="#cb15-20"></a>    <span class="ex">optional_theme</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span> <span class="co"># logging</span></span>
<span id="cb15-21"><a href="#cb15-21"></a>    <span class="bu">return</span> 0</span>
<span id="cb15-22"><a href="#cb15-22"></a>  <span class="kw">else</span></span>
<span id="cb15-23"><a href="#cb15-23"></a>    <span class="ex">inactive_theme</span> <span class="st">&quot;</span><span class="va">$1</span><span class="st">&quot;</span> <span class="co"># logging</span></span>
<span id="cb15-24"><a href="#cb15-24"></a>    <span class="bu">return</span> 1</span>
<span id="cb15-25"><a href="#cb15-25"></a>  <span class="kw">fi</span></span>
<span id="cb15-26"><a href="#cb15-26"></a><span class="kw">}</span></span>
<span id="cb15-27"><a href="#cb15-27"></a></span>
<span id="cb15-28"><a href="#cb15-28"></a><span class="ex">theme_guard</span> <span class="st">&quot;Repositores&quot;</span> <span class="kw">&amp;&amp;</span> <span class="kw">{</span></span>
<span id="cb15-29"><a href="#cb15-29"></a>  <span class="ex">map_lines</span> sync_repo <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap/Repofile&quot;</span></span>
<span id="cb15-30"><a href="#cb15-30"></a><span class="kw">}</span></span>
<span id="cb15-31"><a href="#cb15-31"></a></span>
<span id="cb15-32"><a href="#cb15-32"></a><span class="ex">theme_guard</span> <span class="st">&quot;Linking&quot;</span> <span class="kw">&amp;&amp;</span> <span class="kw">{</span></span>
<span id="cb15-33"><a href="#cb15-33"></a>  <span class="ex">map_lines</span> safe_link  <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap/Linkfile&quot;</span></span>
<span id="cb15-34"><a href="#cb15-34"></a><span class="kw">}</span></span>
<span id="cb15-35"><a href="#cb15-35"></a></span>
<span id="cb15-36"><a href="#cb15-36"></a><span class="ex">theme_guard</span> <span class="st">&quot;Brew&quot;</span> <span class="kw">&amp;&amp;</span> <span class="kw">{</span></span>
<span id="cb15-37"><a href="#cb15-37"></a>  <span class="bu">cd</span> <span class="st">&quot;</span><span class="va">$target</span><span class="st">/bootstrap&quot;</span> <span class="kw">&amp;&amp;</span> <span class="ex">brew</span> bundle</span>
<span id="cb15-38"><a href="#cb15-38"></a><span class="kw">}</span></span></code></pre></div>
<p>Just by extracting checks into separate function, by using <code>declare</code> to define variables and variable indirection to read value of variables we highly improved initial code. Less redundancy, clearer intention and ability to scale in terms of themes.</p>
<h1 id="epilogue">Epilogue</h1>
<p>Fortunately, our actions didn’t lead to Saruman taking control over the Shire, so we are good. This post is already long enough, so I am going to finish here. If you have any questions, just <a href="mailto:boris@d12frosted.io">email</a> me.</p>
      </section>
    </article>

    <div class="footer-separator"></div>
    <div class="footer">
      <a target="_blank" class="post-footer-sources" href="https://github.com/d12frosted/d12frosted.io/blob/master/posts/2018-11-04-revisiting-eru.org">Sources <i class="fa fa-code fa-lg"></i></a>
    </div>
  </body>
</html>
]]></summary>
</entry>

</feed>
